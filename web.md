# domiman 웹 원격제어 (`cheongbaek.github.io/domiman`)

브라우저에서 피제어 PC(낚시 매크로)를 조작한다. **모바일 앱(`domiman_m.py` +
Kotlin)의 메인 화면과 같은 기능**을 PC·휴대폰 한 화면으로 제공하며, 위젯·알림은
없다(브라우저에는 포그라운드 서비스가 없다).

**260913a부터 domichat 채팅도 같은 연결로 한다** — 상단 '채팅방 목록' 칸을 누르면
화면 전체가 방 목록으로 바뀐다(아래 '채팅' 절).

| 파일 | 역할 |
|---|---|
| `domiserver.py` [8. 웹 중계] | 중계가 **이 파일 안에** 있다(260914a 통합). 서버에는 `web` 계정으로 **127.0.0.1 에 붙어서** 말한다(260914d) |
| `web/` | 정적 웹앱(Vite + React + TS). GitHub Pages로 배포 |
| `.github/workflows/deploy-pages.yml` | `web/**` 변경 시 Pages 배포(`BASE_PATH=/domiman/`) |

```
브라우저 ──wss://domiman.duckdns.org:47822/ws──▶ domiserver.py (중계 + 채팅 서버)
  (GitHub Pages에서 받은 정적 파일)                     │  HubConn(가상 연결)
                                                        ▼
                                    방 domi_fishing_{PC} ◀── 피제어 PC (TLS 47821)
```

## 260914a — 따로 돌던 프로세스를 서버 안으로 넣었다

예전에는 `domiweb.py`가 옆에서 따로 떠서 **도로 이 서버에 127.0.0.1로 TLS 소켓을
열고** `web` 계정으로 로그인했다. 한 프로그램이 되면서 그 왕복이 통째로 사라진다.

중계는 소켓 대신 **`HubConn`(가상 연결)** 으로 서버 안에 자리를 잡는다 — 큐 하나를
달고 있을 뿐, 서버가 보기에는 평범한 로그인된 연결이라 입장 자격·방 비밀번호·
팬아웃·도배 제한·이미지 중계를 **기존 `handle_*` 가 그대로 처리한다.** 웹 전용
경로를 따로 만들지 않는 것이 요점이다(방 규칙의 주인을 둘로 늘리지 않는다).

없어진 것: 상류 접속·재접속 백오프·지문 고정(TOFU)·`web` 계정과 비밀번호·
`domiweb.json`·`already_online` 충돌. 브라우저 ↔ 중계 규격은 **한 글자도 바뀌지
않았다**(웹앱은 GitHub Pages에 이미 배포돼 있어 서버만 고칠 수 있다).

설정은 `domiserver.json` 의 `web`·`web_port`·`web_certfile`… 로 옮겼고, 옆에 옛
`domiweb.json` 이 남아 있으면 **첫 실행에 한 번 자동으로 옮겨 온다**
(`import_domiweb_config` — 손으로 적은 인증서 경로를 잃지 않기 위해서다).

## 한 파일 안에 있지만, 붙는 방식은 바깥 클라이언트와 같다 (260914d)

중계는 `domiserver.py` 안에 있다. 그렇다고 **서버 함수를 직접 부르지는 않는다** —
`WebUplink` 가 **127.0.0.1:47821 로 소켓을 열어 `web_id`/`web_pw` 계정으로 진짜
로그인**하고, 그 뒤로는 domichat 클라이언트와 똑같은 프레임만 주고받는다.

| | 260914a~c (가상 연결) | 지금 (127.0.0.1 로그인) |
|---|---|---|
| 서버에 말하는 법 | `HubConn` 을 만들어 `handle_*` 직접 호출 | 소켓 + `login` → 평범한 연결 하나 |
| 계정 | 이름만 `ONLINE` 에 올림(비밀번호 없음) | **진짜 계정**(`approve` 해 둬야 한다) |
| 방 목록 | DB(`rooms_snapshot`) 직접 읽기 | `rooms` 프레임으로 받음 |
| 서버 쪽 예외 | 가짜 연결(ready 강제·`last_rx` 수동 갱신·팬아웃 바이트 되풀기) | **없음** |

**왜 되돌렸나(사용자 확정):** 가상 연결은 계정을 없앤 것이 아니라 *계정 흉내*였다.
그럴 바에는 계정 하나로 진짜 붙는 편이 낫다 — 입장 자격·비밀번호·팬아웃·도배
제한·이미지 중계가 바깥 클라이언트와 **글자 하나 다르지 않은 길**을 타고, 서버가
자기 자신을 특별 대우하지 않는다. 중계가 죽어도 채팅 서버는 멀쩡하다.
대가는 계정 하나와 loopback 왕복인데 같은 기계라 무시할 수 있다.

**설치할 때 한 번 할 일:** 클라이언트에서 `web` 계정을 만들고 서버 콘솔에서
`approve web`, 그 다음 `set web_pw <비밀번호>`. 계정이 없거나 비밀번호가 다르면
중계를 **열지 않고**(채팅 서버는 그대로 돈다) 무엇을 해야 하는지 로그에 적는다.
서버 TLS 가 켜져 있으면 상류도 TLS 로 붙고, 자체 서명이라 **우리 인증서 지문과
직접 대조**한다(자기 자신이니 지문을 안다 — TOFU 파일이 필요 없다).

## 왜 중계인가 (설계 결정 — 되돌리지 말 것)

1. **브라우저는 raw TCP를 열 수 없다.** domichat은 길이접두 TCP 프레임이라
   그대로는 붙지 못한다 → WebSocket 한 겹이 반드시 필요하다.
2. **브라우저는 지문 고정(TOFU)을 할 수 없다.** domiserver의 자체 서명 인증서로는
   `wss://`가 **경고 없이 그냥 실패**한다(사용자가 예외를 누를 화면조차 없다).
   그래서 domiweb만 **브라우저가 신뢰하는 인증서**(Let's Encrypt)를 든다.
   GitHub Pages는 항상 https이므로 `ws://` 평문은 혼합 콘텐츠로 차단된다.
3. **domiserver는 같은 ID 동시 접속을 불허한다.** 브라우저마다 로그인시키면 기기
   하나만 쓸 수 있다 → 중계가 **연결 하나**(`HubConn`)로 자리를 잡고 브라우저
   여러 대를 그 하나에 **다중화**한다. 덕분에 **웹앱에는 계정도 비밀번호도
   없다**(공개 정적 사이트에 자격을 박는 문제가 구조적으로 사라진다).
   그 이름(`web_id`, 기본 `web`)은 `ONLINE`에 올려 두므로 **바깥에서 같은 ID로
   로그인하는 것도 막힌다** — 중계를 사칭할 통로를 남기지 않는다.
4. **domiman.py는 한 줄도 고치지 않는다.** 피제어 PC는 `{sender},Z,...`로 답하고
   sender는 서버가 찍는 `from`이므로, 중계가 끼어도 규격이 그대로다.

## 프로토콜

- 중계 ↔ 방: `domichat.md` 규격 그대로(방 `domi_fishing_{PC}`, 고정 비번).
  같은 프로세스 안이지만 **프레임은 그대로 흐른다** — `HubConn` 이 `handle_join`·
  `handle_msg` 를 그대로 통과하고, 팬아웃이 돌려주는 프레임을 큐로 받는다.
- 브라우저 ↔ 중계: JSON over WebSocket.
  - 받는 것: `hello` / `select{pc}` / `cmd{pc,body}` / `add_pc{pc}` / `del_pc{pc}`
  - 주는 것: `ready` / `snap` / `msg{pc,body}` / `pcs` / `pc` / `up` / `shot` / `err`
- **방에서 온 원문(`web,Z,...` 등)은 해석하지 않고 그대로 넘긴다.** 파싱은
  `web/src/lib/protocol.ts`가 한다 — 규격의 소유자를 늘리지 않기 위해서다.
  중계가 구분하는 것은 캐시를 위한 접두어 셋(상태 응답·수량 방송·보고)뿐이다.
- 예외는 **스크린샷**이다: `'B'` 이진 청크를 중계가 조립해 크기·sha256을 확인한
  뒤 완성된 PNG를 base64로 넘긴다(브라우저에 청크 조립 로직을 또 두지 않는다).
  사진은 **요청한 브라우저에게만** 준다(남이 찍은 3MB가 갑자기 뜨지 않게).
  채팅 이미지도 **같은 조립기**를 쓴다(`file_begin`의 방 이름으로 갈린다).

### 채팅 프레임 (260913a)

  받는 것: `rooms` / `room_open{room,pw?}` / `room_close{room}` /
           `chat{room,body,cid}` / `img_begin{room,fid,name,size,sha256}` /
           `img_chunk{fid,seq,b64}` / `img_end{fid}`
  주는 것: `rooms{list}` / `room{room,state,...}` / `chat{...}` / `chat_img{...}` /
           `chat_hist{room,items}` / `chat_err{room,msg}`

`room.state` = `joined|denied|closed|deleted|kicked|member|approve_res`.
`denied.reason`은 domichat 규격 그대로(`bad_pw_room`·`not_allowed`·
`await_approval`·`blocked`)이며 문장으로 바꾸는 표는 `web/src/lib/chat.ts`에 있다.

## 웹 중계의 함정 (이미 값을 치른 것들)

| 항목 | 이유 |
|---|---|
| **브라우저용 TLS도 1.2 고정** | 연결마다 수신 스레드와 송신 스레드가 한 소켓을 나눠 쓴다 — 1.3의 NewSessionTicket/KeyUpdate가 record layer를 깬다(`_pin_tls12` 와 같은 이유, 같은 구조) |
| 발신 스로틀 `12건/10초` | 서버의 `MSG_BURST=20/10초`는 **연결 하나당** 걸리는데, 브라우저 여러 대의 명령이 중계 연결 **하나로 합쳐진다.** 한 프로그램이 되어도 `handle_msg` 를 그대로 통과하므로 이 여유는 그대로 필요하다 |
| 중계 연결도 `last_rx` 를 갱신한다 | `maintenance_loop` 가 15초마다 `ping` 을 보내고 45초 무응답이면 연결을 회수한다. `_pump_loop` 가 프레임을 받을 때마다 `last_rx` 를 갱신하지 않으면 **중계가 스스로 회수당한다**(실제 연결이 `serve_conn` 에서 하는 일과 같다) |
| 중계 프레임은 **한 스레드**(`_uplink_loop`)에서만 내보낸다 | `handle_msg` 의 도배 카운터(`conn.msg_times`)는 연결 하나를 자기 스레드만 건드린다고 보고 짜여 있다 |
| 명령 화이트리스트(`WEB_CMD_RE`) | 누구나 붙는 공개 중계다. 방에 흘려보낼 문자열을 domiman 명령 규격으로만 제한해 채팅방 스팸 통로가 되지 않게 한다 |
| 설정을 `utf-8-sig`로 읽음(승계) | PowerShell `Set-Content -Encoding UTF8`은 **BOM을 붙인다.** 그냥 utf-8로 읽으면 옛 `domiweb.json` 이 조용히 안 읽혀 인증서 경로가 비고 평문 ws로 열린다 |
| 인증서 mtime 감시 → 자동 재적재 | Let's Encrypt는 60~90일마다 갱신된다. 재시작이 필요한 구조면 어느 날 조용히 만료된다 |
| 포트를 못 열어도 **서버는 계속 돈다** | 채팅 중계가 웹 때문에 죽어서는 안 된다. `start_web_relay()` 는 `None` 을 돌려주고 로그만 남긴다 |
| **지목한 PC의 방에만 입장** | 시작할 때 목록의 방을 전부 잡아 두면 보지도 않는 PC의 수량 방송을 사이클마다 받고, 그 방 참가자 목록에도 `web`이 늘 떠 있게 된다. 동시에 여러 PC를 돌릴 일이 없다 |
| 마지막 사람이 떠나면 퇴장 + 캐시 폐기 | 다시 들어갈 때 옛 상태를 잠깐 보여주면 '지금 값'으로 오해한다(입장 직후 `S` 질의로 새로 받는다). 대신 **수량은 다음 방송까지 `–`로 보인다**(최대 한 폴링 사이클) — 바로 보려면 '실시간 수량확인'을 쓴다 |
| 방이 생기면 `room_new` 로 바로 붙는다 | 그 PC가 domichat에 접속한 적이 없으면 방이 없다. 예전에는 60초마다 목록을 다시 받아야 알 수 있었는데, 한 프로그램이 되면서 **방 생성 통지가 그대로 들어온다.** 60초 재시도는 그 통지를 놓쳤을 때의 안전망으로 남겼다 |

## 채팅은 **다른 사이트로 떼어 냈다** (260914e)

채팅은 이제 `https://cheongbaek.github.io/domichat/` 에 있다. 이 앱(`/domiman/`)에는
'채팅방 목록' 칸이 **없다** — 하단 안내줄의 '채팅 열기' 링크뿐이다.

| | 낚시 원격제어 | 채팅 |
|---|---|---|
| 주소 | `cheongbaek.github.io/domiman/` | `cheongbaek.github.io/domichat/` |
| 소스 | 이 저장소 `web/` | **`cheongbaek.github.io` 저장소의 `domichat-src/`** (빌드 결과가 같은 저장소 `domichat/` 에 떨어지고 브랜치 그대로 배포된다 — 워크플로가 없다) |
| 붙는 곳 | 같은 중계(`domiserver.py` [8]) — 프레임 규격도 같다 |

아래 절은 그 채팅 화면의 규칙이며, 옮겨간 곳에서도 **그대로 유효**하다(파일만
복사됐다). 규격을 고치면 두 곳을 같이 봐야 한다: 중계(`domiserver.py` [8])와
`domichat-src/src/`.

## 채팅 화면의 규칙 (260913a — 지금은 /domichat/ 에 있다)

상단 '채팅방 목록' 칸 → **화면 전환**(창이 아니다 — 휴대폰에서 더 많이 쓴다).
목록에서 방을 누르면 들어가고, 방 안에서는 글과 이미지를 주고받는다. 낚시 제어
화면으로는 `←`로 돌아오며, **그동안에도 제어 상태는 계속 갱신된다**(중계는 여전히
그 PC의 방을 보고 있다).

**폭에 따라 배치가 갈린다(모바일 우선이지 모바일 전용이 아니다):**

| 폭 | 배치 |
|---|---|
| < 900px (휴대폰) | 한 번에 하나 — 목록 ↔ 채팅방을 오간다. 방 머리의 `←`가 목록으로 돌아간다 |
| ≥ 900px (PC) | **목록(300px)과 대화가 나란히** 선다. 목록에서 바로 다른 방으로 옮길 수 있고, 열려 있는 방은 목록에 표시된다(왼쪽 강조선). 방 머리의 `←`는 숨고 오른쪽에 **[나가기]** 가 선다 — 목록으로 '돌아가는' 일이 없어졌으니 남는 동작은 **방을 뜨는 것**뿐이다(보는 사람이 없어지면 중계가 그 방에서 나온다) |

두 화면은 **항상 함께 그려 두고 CSS가 감춘다**(`.pane.hide`). 그래야 넓은 화면에서
방을 옮길 때 목록이 다시 그려지지 않고, 좁은 화면에서도 코드가 한 갈래로 유지된다.

| 정한 것 | 이유 |
|---|---|
| **입장만 한다 — 방 만들기·방장 도구는 없다**(사용자 확정) | 웹은 중계의 계정 하나(`web`)를 쓰므로 방장이 될 일이 없다. 승인 대기·블랙리스트·비번 변경·방 삭제는 domichat(PC)에서 한다 |
| 입장 자격은 **`web` ID 기준** | 사후 승인 방은 방장이 **`web`을 승인**해야 들어간다. 그래서 목록 화면에 내 ID를 크게 적어 둔다 |
| 다른 브라우저가 보낸 말도 '내 말'로 보인다 | 계정을 공유하는 구조의 필연. 웹앱에 자격을 두지 않기로 한 대가이며 제어 기능과 같다 |
| **구독·알림 없음** | 브라우저에는 포그라운드 서비스가 없다. 보는 사람이 없으면 중계가 방에서 나오고, 서버는 대화를 저장하지 않으므로 그동안의 대화는 공백으로 남는다(domichat.md §4와 같은 규칙) |
| **제어용 방(`domi_fishing_*`)은 목록에서 감추고 입장도 막는다** | ① 사이클마다 오는 수량 방송이 대화창을 뒤덮는다 ② 그 방에 아무 문자열이나 흘리면 명령 화이트리스트(`CMD_RE`)를 우회하는 통로가 된다 |
| 방 비밀번호는 **메모리에만** | 디스크에 남기지 않는다(domichat도 '구독한 방만' 기억하고 웹에는 구독이 없다). 중계 쪽은 마지막 사람이 방을 닫을 때 함께 버리고, 브라우저 쪽은 탭을 닫으면 사라진다. 상류가 끊겼다 붙을 때 **말없이 재입장**하는 데 쓴다 |
| 대화 기록은 **중계가 방마다 80줄** 들고 있다 | 나중에 붙은 브라우저·재입장에 그동안의 대화를 줘야 하기 때문(한 연결을 여러 브라우저가 나눠 쓰는 구조라 '입장 시점'이 브라우저마다 다르다). **이미지는 넣지 않는다** — base64가 메모리를 먹고, 사람은 그 자리에 없었던 사진까지 볼 이유가 없다 |
| 이미지는 브라우저가 **PNG로 바꿔** 올린다 | domichat.py와 같은 규칙(받는 쪽 tkinter가 PNG/GIF만 그린다). 긴 변 2560px로 줄이고, 그래도 상한을 넘으면 더 줄여 다시 인코딩한다 |
| 올린 이미지는 **중계가 자기 브라우저들에게 되돌려준다** | 서버는 보낸 연결을 팬아웃에서 제외한다(`exclude=conn`). 그래서 올린 본인도, **같은 중계에 붙은 다른 브라우저도** 상류에서는 받지 못한다 |
| 업로드는 **base64 48,000자 조각**으로 올린다 | 중계의 `WEB_MAX_RX`가 64KB다(원본 36,000바이트). 중계가 그것을 모아 방에는 64KB `'B'` 청크로 다시 나눠 보낸다 |
| 발신 스로틀이 **두 겹** | 상류 스로틀(`SEND_BURST`)은 연결 전체를 지키는 것이고, `CHAT_BURST=8/10초`는 **브라우저 한 대**가 그 몫을 다 먹어 제어 명령이 밀리는 것을 막는다 |
| 이미지 청크는 `_uplink_loop` 가 특별히 받는다 | `'B'` 프레임은 JSON 핸들러(`HANDLERS`)에 없다. 그래서 큐에 `("_chunk", fid, seq, data)` 로 실어 보내고 그 자리에서 `relay_file_chunk` 로 넘긴다 — 바깥 클라이언트의 이진 프레임이 가는 길과 같다 |
| `web_chat` 설정으로 끌 수 있다 | 공개 중계에 채팅을 얹으면 **누구나 중계 계정으로 공개방에 말할 수 있다**(명령 화이트리스트가 막아주던 범위 밖이다). 그게 싫으면 `domiserver.json`의 `"web_chat": false` — 목록·입장·발신이 전부 거절된다 |
| 중계가 **`ready`에 `chat: true`를 실을 때만** 웹앱에 칸이 뜬다 | 웹앱은 Pages로 먼저 배포되고 중계는 나중에 올라갈 수 있다. 그 사이에 버튼만 떠 있으면 눌러도 아무 방이 안 나오는 죽은 칸이 된다 — **서버가 준비되면 저절로 나타난다** |

**옛 서버(ver 1)에는 이미지를 보내지 않는다** — `welcome.ver`를 보고 `< 2`면 그
자리에서 거절한다. 'B' 프레임을 받는 즉시 연결을 끊는 서버가 있어 **1초 간격
재접속 고리**에 빠진 사고가 domichat.md에 남아 있다(상류가 끊기면 큐에 남은 청크도
`drop_chunks()`로 버린다 — 같은 이유).

## 웹앱 쪽 함정

- **서비스워커를 넣지 않는다.** 같은 오리진(`cheongbaek.github.io`)에 남은 옛 PWA
  워커가 화면을 캐시에서 돌려주던 사고가 `find_wc`에 기록돼 있다. `main.tsx`가
  기존 등록을 걷어내는 그물까지 둔다.
- **스냅샷 재생은 응답 대기(pending)를 풀지 않는다**(`applyBody(body, hist=true)`).
  나중에 붙은 브라우저가 옛 응답으로 pending을 소모하면 UI가 잘못 열린다.
- **수량 방송(`,Z,N,*`)도 pending을 풀지 않는다** — 요청 없이 사이클마다 온다.
- 모바일 브라우저는 백그라운드 탭의 소켓을 얼린다 → `visibilitychange`에서 즉시
  재접속하고 `select`로 상태를 다시 받는다(앱의 `resync`와 같은 취지).
- 사진 Blob URL은 새 사진을 받을 때·창을 닫을 때 `revokeObjectURL`한다(안 풀면
  사진마다 2MB가 남는다).
- 테마는 세 상태(system/light/dark)다. 색은 **모두 bare `:root`에 정의**하고
  다크에서 같은 토큰만 덮어쓴다 — 미디어쿼리 안에만 정의하면 토글이 깨진다.
- 접속 주소는 `web/src/lib/relay.ts`의 `DEFAULT_WS` 한 줄이며, 개발 중에는
  `?ws=ws://localhost:47822/ws`로 덮어쓴다.

## BGOD 설치 절차

**계정을 만들 필요가 없다**(260914a). 중계가 서버 자신의 일부라 로그인을 하지
않는다 — 옛 `web` 계정은 그냥 두어도 되고 `deluser web` 해도 된다.

1. **호스트명·인증서**: DuckDNS(`domiman.duckdns.org` → 211.196.44.3) +
   Posh-ACME(DNS-01, `-UseSerialValidation` 필수 — DuckDNS는 TXT를 하나만 둔다).
   `Submit-Renewal`을 매일 도는 작업으로 걸어 둔다.
2. `domiserver.json` 에 인증서 경로를 적는다
   (`web_certfile` 은 **`fullchain.cer`**, `web_keyfile` 은 `cert.key`).
   옛 `domiweb.json` 이 옆에 있으면 첫 실행에 자동으로 옮겨 오므로 손댈 것이 없다.
3. `domiserver.py` 를 평소처럼 띄운다 — **중계는 같이 뜬다**(끄려면 `set web 0`).
   콘솔 `web` 으로 수신 주소·브라우저 수·피제어 PC 상태를 볼 수 있고,
   `web add/del <PC ID>` 로 목록을 고친다.
4. 브라우저로 `https://domiman.duckdns.org:47822/` → `domiserver ... 웹 중계 —
   살아 있습니다`와 **경고 없는 자물쇠**가 보이면 인증서가 먹은 것이다.
   여기서 실패하면 웹앱도 못 붙는다.
5. GitHub: `Settings → Pages → Source = GitHub Actions`.

※ 관리 창(`domiserver_gui.py`)으로 띄워도 중계가 같이 뜬다(`start_server` 가
`start_web_relay()` 를 부른다). **둘을 동시에 띄우지 말 것** — 47821·47822 를
두 번 열 수 없다.

## 검증 기록 (260914d — 중계가 127.0.0.1 로 로그인)

- 평문 서버: 중계가 `web` 계정으로 붙는 것을 **서버 로그(`[로그인] web`)로 확인**한
  뒤 채팅 **22항목** 전부 통과(목록·입장·대화·cid 에코·이미지 양방향 sha256·승인
  왕복·방 삭제·제어용 방 거절·재입장 대화 복원·마지막 사람 퇴장·낚시 제어 회귀·
  길이/연타 차단).
- **TLS 서버**(자체 서명, 지문 대조): 상류 소켓이 실제로 TLS 이고 로그인 성공,
  브라우저까지 방 목록 왕복, **12초 방치 후에도 연결 유지**(읽기 타임아웃 함정).
- 실제 Chromium **28항목**(430px·1280px, 라이트/다크) 전 흐름 통과.

## 검증 기록 (260914b — 채팅, 통합된 서버 기준)

격리 서버 사본 하나(평문 47899 + 웹 47898, 중계가 그 안에 있다)에 피제어 PC 역할
domichat 클라이언트와 브라우저 흉내(RFC 6455 최소 구현)를 붙여 **22항목**:
- 방 목록(제어용 방 숨김·유형·입장 가능 여부), 공개방 입장, 발신·수신, **cid 에코**
- 이미지 **양방향 sha256 일치**(받기 120KB / 보내기 150KB), 올린 이미지를 **같은
  방의 다른 브라우저도** 받는 것(서버가 보낸 쪽을 팬아웃에서 제외하므로 중계가 직접 준다)
- 비밀번호 방(틀린 비번 → `bad_pw_room`, 맞는 비번 → 입장), 사후 승인
  (`await_approval` → 방장 승인 → `approve_res` → 입장), 방 삭제, 제어용 방 거절
- 나중에 붙은 브라우저에게 대화 되돌려주기, **마지막 사람이 나가면 방에서 나오고
  남은 사람이 있으면 지키는 것**
- 회귀: 낚시 제어 경로(입장·`S` 질의·상태 응답·수량 방송), 명령 화이트리스트,
  메시지 길이·연타 차단

실제 Chromium으로 **28항목**(휴대폰 폭 430px + PC 1280px, 라이트/다크):
상단 칸 → 목록 → 비번 창(오답·정답) → 채팅방 → 발신(✓ 표시) → 수신 → 이미지
받기/보내기 → 크게 보기 → 목록 → 낚시 화면 복귀(살림망 표시 유지), 그리고 폭별
배치(430px 한 칸 / 1280px 두 칸, 목록에서 바로 방 옮기기, 열린 방 표시,
`←`↔[나가기] 교체). 채팅을 끈 중계(`web_chat: false` 또는 옛 버전)에 붙여도 **칸은
그대로 있고** 들어가면 이유가 적히는 것까지 확인했다 — 상태에 따라 칸이 사라지면
쓰는 쪽에서는 UI가 고장 난 것으로 보인다.

**이 과정에서 고친 것:**
- 상단 바를 **접히게**(`flex-wrap`) 했다 — 칸이 하나 늘면서 430px에서 연결 상태
  글자가 세 줄로 쪼개지고 살림망 칸이 화면 밖으로 잘렸다.
- 이미지 버튼은 **글자('사진')** 다 — 그림 문자(🖼)는 글꼴에 따라 두부(□)로 나온다.
- 방 이름 **찾기 칸은 뺐다** — domichat 리스트에도 없고 방이 몇 개 안 된다.
- 통합본은 방 목록을 **DB에서 바로 만든다**(`rooms_snapshot`). 옛 중계처럼 서버에
  `rooms` 프레임을 보내 답을 기다리지 않으므로 왕복이 사라졌고, 옛 중계에서 겪던
  '입장 요청이 겹쳐 `S` 질의가 두 번 나가는' 자리도 없다(입장 자체에는 그대로
  5초 중복 방지를 둔다 — `submit` 은 비동기라 답이 오기 전에 또 청할 수 있다).

## 검증 기록 (260914a — 통합 후 e2e, 41항목 전부 통과)

격리 폴더에 `domiserver.py` 사본을 두고(실서버 DB를 건드리지 않는다) 평문 47899 /
웹 47898로 띄워, 피제어 PC 역할의 domichat 클라이언트와 브라우저 역할의 WebSocket
클라이언트를 실제로 붙여 확인했다:
- 방 개설 → 중계 자동 입장 → **입장 직후 `S` 자동 질의**까지 왕복
- 명령 `N` 전달, 상태 응답·수량 방송·보고 **원문 그대로** 전달 + 캐시
- 규격 밖 명령·목록에 없는 PC 차단
- 스크린샷 300KB: `file_begin` → `'B'` 청크 → `file_end` 조립, **바이트 완전 일치**
- 명령 16건 연속: 스로틀이 10초 창을 넘겨 내보내 **`rate_limited` 0건**
- 피제어 PC 종료 → `online=false` 통지 / 마지막 브라우저 퇴장 → 방 퇴장 + 캐시 폐기
- `ping_sec=3`·`pong_timeout_sec=9` 로 줄인 채 12초 방치 — **중계 연결이 회수되지
  않음**(`last_rx` 갱신이 듣는다)
- 방이 없는 PC는 `no_room`, 그 PC가 뒤늦게 켜지면 `room_new` 로 **즉시 입장**
- 바깥에서 같은 ID(`web`)로 로그인 시도 → `already_online` 거절
- 평범한 GET → 안내 페이지 200
- `domiweb.json` 승계: 포트·이름·PC 목록·인증서 경로가 옮겨오고 상류 전용 키
  (`server`/`pw`)는 안 옮겨오며, **두 번째 실행에서는 다시 덮어쓰지 않는다**

※ 이 검증은 전부 **loopback(127.0.0.1) 한 프로세스 안**이라 공인 IP·포트포워딩·
공인 인증서가 필요 없다. `wss` 실제 핸드셰이크는 아래 260902a 기록을 승계한다
(브라우저용 TLS 경로는 그대로 옮겨왔고 `_pin_tls12` 로 합쳐졌을 뿐이다).

## 검증 기록 (260902a — 통합 전, domiweb.py 시절)

격리 서버(`domiserver.py` 사본, 포트 47899 평문) + 피제어 PC 흉내로:
- 명령 왕복(S/G/P/W/N/V/C/T/Y/I), 상태·수량 방송·보고 전달
- 브라우저 2대 동시 접속 — 방 메시지는 둘 다, **사진은 요청자만**
- 스크린샷 150KB **sha256 일치**, 규격 밖 명령 차단, PC 목록 추가·삭제
- `wss`(TLS 1.2) 핸드셰이크, 인증서 파일 교체 시 재적재
- 실제 Chromium으로 라이트/다크 · PC(1280px)/모바일(430px) 두 폭에서 전 흐름
  (수량확인 → 시작/중지 → 스크린샷 창 → 해상도 대화상자 → 예약 종료 2단계)
  **콘솔 오류 0**
