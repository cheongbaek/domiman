# 낚시 매크로 프로젝트 컨텍스트

테일즈러너 낚시 자동화 매크로. 살림망 퀴즈(물고기 실루엣 매칭)를 자동으로
풀고 낚시를 재시작한다.

**메인 파일은 `domiman.py`(tkinter GUI, 자기완결형).** `낚시.py`는 CLI 이전
버전으로 더 이상 사용하지 않을 예정(로직 원본 참고용). domiman.py는
낚시.py의 로직을 이식한 것이라 아래 문서의 좌표·함정·사양이 그대로 적용된다.
GUI 특이사항: 자동화는 워커 스레드(stop_event), 로그는 stdout 리다이렉트
(LogWriter 큐→Text 위젯), 상태 문구는 엑셀 J8:K18 매핑, 접속 끊김 시
종료하지 않고 대기(재감지→재시작 가능), 낚시 중에는 설정 컨트롤 잠금.
exe 패키징은 domiman.spec으로 빌드(아래 [런처/코어 분리] 참고).

**종료는 `os._exit(0)` 필수(함정):** `on_exit`은 설정/로그 저장만 마치고
`os._exit(0)`로 즉시 끝낸다. `root.destroy()` 후 정상 인터프리터 종료로 두면
torch/easyocr/OpenMP 네이티브 스레드 정리를 기다리느라 (로그 저장 여부와
무관하게) 메인 스레드가 수 초 블록돼 창에 '응답 없음'이 뜨고 늦게 꺼졌다.
os._exit는 그 정리를 건너뛴다. 저장은 os._exit 전에 동기로 끝내둘 것
(원격 Q 응답도 `wait=True`로 먼저 보낸 뒤 on_exit 호출).

---

## 🖥 다해상도 지원 (260925c, 구현됨 — 좌표를 건드리기 전에 이 절부터)

게임 '화면 설정'의 **7가지 해상도**를 지원한다. 좌표는 **지금도 전부 FHD
기준으로 적고**, 변환이 알아서 옮긴다. 새 좌표를 추가할 때 할 일은 하나뿐 —
**앵커 계열을 붙이는 것**(`Pt(x, y, "hud")` / `Rg(x, y, w, h)`).

| key | 라벨 | 렌더 | UI 배율 s | 비고 |
|---|---|---|---|---|
| `1080` | 1920 x 1080 | 1920x1080 | 1.0 | |
| `1440` | 2560 x 1440 (늘린 FHD) | 1920x1080 | 1.0 | 렌더는 FHD. WGC 강제 |
| `1760x990` | 1760 x 990 | 1760x990 | 1.0 | 16:9 |
| `1680x1050` | 1680 x 1050 | 1680x1050 | 1.0 | 16:10 |
| `1280x1024` | 1280 x 1024 | 1280x1024 | 1.0 | 5:4 |
| `1280x960` | 1280 x 960 | 1280x960 | 1.0 | 4:3 |
| `1024x768` | 1024 x 768 | 1024x768 | **0.8** | 4:3, 유일하게 UI가 줄어든다 |

### 핵심 — UI는 성질이 다른 **두 계열**로 갈린다 (260925a/b 실측)

2026-09-25 스크린샷 30장(6해상도 × 낚시화면/살림망/퀴즈/확인팝업/미끼·낚싯대
리스트)을 픽셀로 재서 확정했다. 각 계열 안에서는 **오차 0.5px 이내**로 맞는다.

**① 팝업 계열 (`anchor="popup"`, 기본값) — 크기 고정 + 화면 중앙.**

```
x = W/2 + (fhd_x - 960)
y = H/2 + (fhd_y - 540)      w, h 는 스케일하지 않는다
```

살림망·낚시 퀴즈·확인 팝업·보유 미끼/낚싯대 창이 **모든 해상도에서 절대 픽셀
크기가 같다.** 실측(중앙에서의 dx, dy — 6해상도 전부 동일):

| 요소 | 크기 | dx | dy |
|---|---|---|---|
| 마이룸보내기 | 88x22 | +177.5 | -297.5 |
| 살림망 닫기X | 33x34 | +255.0 | -308.5 |
| 퀴즈 닫기X | 34x34 | +257.5 | -205.5 |
| '확인' 버튼 | 68x25 | -0.5 | +31.0 |
| 리스트 타이틀바 | 720x35 | -2.5 | -166.0 |
| 이름 바 4열 | 133x17 | -246.5 / -83.5 / +79.5 / +242.5 | 1행 -39.0, 2행 +118.5 |
| '사용하기' 4열 | 60x23 | 〃 | 1행 +2.5, 2행 +159.0 |
| 다음 ▶ / 이전 ◀ | 22x28 | +335.5 / -339.5 | +24.5 |

> **함정: 1024x768에서도 팝업은 줄어들지 않는다.** 아래 ②의 HUD만 0.8배가
> 된다. 화면 전체를 배율 하나로 다루려 들면 반드시 깨진다.

**② HUD 계열 (`anchor="hud"`) — 화면 가장자리 + UI 배율 s. 크기도 s배.**

```
s = min(1.0, W/1280, H/960)
x = W/2 + (fhd_x - 960)*s        w, h 도 각각 *s
y = H   - (1080 - fhd_y)*s
```

실측 근거(살림망확인 버튼): 1024x768만 71x16, 나머지 5해상도는 전부 **89x20으로
동일**. 중앙에서의 dx는 1024x768이 +33, 나머지는 전부 +42(=42x0.8). 바닥에서의
거리는 59.5 vs 74.5(=74.5x0.8). 우하단(자동시점 OFF)·좌상단(팜레벨 바)도 같은
법칙으로 맞는다.

> **s의 식은 아직 반쯤 추측이다.** 관측 6개로는 `min(1,W/1280,H/960)` /
> `min(1,W/1280)` / `min(1,H/960)` 셋을 구별할 수 없다 — 1024x768이 양쪽 다
> 정확히 0.8이라 전부 같은 답을 낸다. 지원 7종에서는 셋이 완전히 일치하므로
> 무해하지만, **800x600 같은 더 작은 해상도를 추가하면 반드시 다시 실측할 것.**

### 좌표 변환은 2단이다 (`to_screen` / `_region_to_client` / `_crop_frame`)

1. **레이아웃** — `layout_point` / `layout_region`: FHD 좌표 → **렌더 해상도**
   좌표. 위 ①/② 공식. 앵커 계열은 상수에 붙어 있고 `anchor_of`가 읽는다.
2. **스트레치** — 렌더 해상도 → 실제 화면: `ox + x/Rw*cw`, `oy + y/Rh*ch`.
   260828a의 그 공식 그대로다.

**렌더가 FHD면 1단계가 항등이라 260828a까지 검증된 좌표·동작이 글자 그대로
보존된다.** 늘린 QHD도 렌더가 FHD라 마찬가지다. 이 성질을 깨지 말 것 —
회귀 테스트가 이걸 직접 확인한다(아래 '검증').

### 캡처 방식은 해상도와 **분리**됐다 (`CAPTURE_MODE`)

예전 `CURRENT_RESOLUTION = "1080p"/"1440p"`는 이름만 해상도였고 실제로는
**캡처 방식**이었다. 이제 둘을 나눈다:

- `CURRENT_RESOLUTION` = 렌더 해상도 key(위 표). **좌표 레이아웃**을 정한다.
  사용자가 고르거나 자동 추정한다.
- `CAPTURE_MODE` = `"direct"`(pyautogui 화면 캡처) | `"wgc"`(창 직접 캡처).
  **사용자가 고르지 않는다** — `detect_capture_mode()`가 모니터만 보고 정한다.
  주 모니터 1920x1080 100% → direct(옛 1080p), 그 밖 → wgc(옛 1440p).
  수동으로 해상도를 골라도 캡처 방식은 이렇게 다시 잡는다.

WGC 패키지가 없으면 **거부하지 않고** direct로 물러선다(예전엔 1440p 설정을
거부해 사용자가 손쓸 방법이 없었다). 가림에 약해진다는 경고만 찍는다.

### 해상도 자동 추정 (`_estimate_render_resolution`)

창 클라이언트 크기 + 모니터 DPI 배율로 렌더 해상도를 맞힌다.

- **배율 해석을 먼저 본다.** 게임은 DPI-unaware라 고배율 모니터에서 창이 OS에
  의해 통째로 확대된다. 우리 프로세스는 Per-Monitor v2(`SetProcessDpiAwareness(2)`)라
  `GetClientRect`가 **물리 픽셀**을 준다 → 1024x768 렌더가 150% 모니터에서는
  1536x1152로 잡힌다. 배율로 나눈 후보를 넣는 것이 **'작은 해상도 + 고배율'**
  조합을 살리는 핵심이다.
- 크기로 못 맞추면 **화면비**로 고른다(전체화면 늘림). 늘린 QHD(2560x1440)가
  여기로 떨어져 1920x1080으로 잡히는 것이 노림수다.
- **`RENDER_CANDIDATES`에 2560x1440을 넣지 말 것(함정).** 넣으면 '늘린 FHD'를
  네이티브 QHD로 오인해 레이아웃이 통째로 어긋난다. 게임의 렌더 해상도 목록에
  QHD는 없다.
- 추정이 틀릴 수 있는 자리는 남아 있다(4:3에서 1280x960 vs 1024x768이 125%
  배율과 겹친다). 그래서 **수동 선택 7칸**을 남겨 뒀다 — 틀리면 직접 고르면 된다.

### 검증 (260925c)

`layout_point`/`layout_region`/`_estimate_render_resolution`을 domiman.py에서
그대로 꺼내(AST로 최상위 정의만 추출) 실측값과 대조했다.

- 클릭 좌표 21건(6해상도 × 살림망확인·마이룸보내기·확인·사용하기·화살표)이
  **전부 실제 버튼 사각형 안**에 떨어졌다.
- FHD 렌더(`1080`/`1440`)에서 1단계가 **항등**임을 좌표 6개·영역 10개로 확인.
- 해상도 추정 12케이스(125%·150% 가상화, 늘린 QHD 포함) 전부 기대값 일치.
- 1024x768(배율 0.8) 스크린샷에 실제 코드의 영역을 겹쳐 눈으로도 확인
  (수량·획득시간·낚시버튼 글자·퀴즈 실루엣·보기 그리드·미끼 이름 8칸·
  사용하기 8칸·화살표·확인 팝업 문구 — 전부 정확).

### 아직 못 본 것 · 딸려오는 것

- ⚠️ **`REGION_DISCONNECT`(접속 끊김 팝업)만 다른 해상도에서 못 찍었다.**
  해상도마다 인터넷을 끊어 볼 수가 없어, 같은 시스템 다이얼로그 틀인 '확인'
  팝업(5해상도 실측 완료)과 같은 팝업 계열로 보고 FHD 실측값을 그대로 뒀다.
  판정이 부분매칭('서버와')이라 조금 어긋나도 읽히지만, **접속 끊김을 못 잡는
  증상이 보이면 여기부터 의심할 것.**
- ⚠️ **안드로이드 앱은 새 해상도 이름을 모른다.** `_status_string`의 해상도
  필드는 옛 `1080`/`1440`을 그대로 쓰지만 새 해상도는 `1024x768` 꼴로 나간다.
  앱의 `resolutionLabelOf`는 그 둘만 알아서 **'감지 실패'로 표시**한다(표시만
  그렇고 동작은 정상). 고치려면 `MainScreenViewModel.kt`·`MainScreen.kt`를
  손보고 APK를 다시 빌드해야 한다.
- `COORD_BAIT_NEXT_BTN`을 **1291 → 1296**으로 옮겼다. 실측 화살표 중심이
  dx=+335.5(=FHD 1295.5)인데 옛 값은 +331이라 22px짜리 버튼의 왼쪽 꼭짓점
  가장자리에 걸쳤다(들어가긴 해서 동작은 했다).
- `COORD_BAIT_PREV_BTN = (620, 565)`(이전 페이지 ◀)를 실측으로 확정해 추가만
  해 뒀다. 지금 경로에서는 쓰지 않는다(`BAIT_MAX_PAGE_MOVES=4`로 앞으로만 넘김).
- `BAIT_USE_BTNS`를 실측 격자의 규칙값(열 간격 163, 행 간격 156.5)으로 정리했다.
  옛 값도 전부 버튼 안이었으므로 버그 수정이 아니라 여유 확보다.
- **1024x768은 OCR 여유가 준다.** 살림망 수량 크롭이 130x36 → 104x29로 작아진다.
  `_ink_span_fhd`는 크롭 실제 폭으로 정규화하므로 `INK_DIGIT2_MIN=26` 기준은
  그대로 유효하지만, 원본 픽셀이 0.8배다. **이 해상도에서 수량·획득시간 판독을
  라이브로 재검증할 것.**

### 같이 바뀐 것 — 회수 성공 뒤 '확인'은 **Enter 한 번**

`run_fishing_routine`의 `4. 완료 확인`이 `click_real(COORD_CONFIRM_BTN)`에서
`press_key(VK_RETURN)`으로 바뀌었다. **FHD·QHD 포함 전 해상도 공통.**
좌표에 기대지 않으니 팝업이 살짝 다른 자리에 떠도 영향을 받지 않는다.
경로(ctypes `keybd_event`)·대기(0.5초)·`_mark_own_input`(취침 모드가 자기
입력에 안 풀리게)은 ESC와 완전히 같다. `COORD_CONFIRM_BTN` 상수는 되돌릴 때를
위해 남겨 뒀다(5해상도 실측 완료).

---

## 📑 이 파일이 문서의 전부다 (md는 이것 하나)

옛 `domichat.md`·`mobile.md`·`web.md`·`배포_가이드.md`는 **전부 이 파일 아래쪽에
합쳤다**(2026-09-15). 다른 md를 찾지 말 것 — 없다. 찾을 내용은 여기 있다:

> **코드 주석이 `domichat.md` 를 가리킬 때:** `domiman.py`·`domichat.py`·
> `domiman_m.py` 주석에 남은 "domichat.md 참고"는 이제 아래
> **`# 📄 domichat / domiserver`** 절을 말한다(주석까지 고치면 파일 수십 곳을
> 건드려야 해 문서 쪽에서 이어 준다). `mobile.md`·`web.md` 도 마찬가지다.

| 찾는 것 | 어디 |
|---|---|
| 경로·빌드 명령(안드로이드/PC판) | 바로 아래 **📍 빠른 참조** |
| 낚시 자동화 로직·좌표·OCR 함정 | 이 파일 앞부분 (`## 현재 아키텍처` ~ `## 취침 모드`) |
| 채팅 서버 프로토콜·프레임 규격 | `# 📄 domichat / domiserver` |
| 안드로이드 앱 구조·UI 스펙·빌드 함정 | `# 📄 도미맨 모바일 앱 (Android)` |
| 웹앱(GitHub Pages)·웹 중계 | `# 📄 도미맨 웹 원격제어` |
| PC판 exe 빌드·설치 배포 | `# 📄 PC판 빌드 & 배포 가이드` |

---

## 📍 빠른 참조 — 경로와 빌드 명령 (찾지 말고 여기서 복사해 쓸 것)

**이 표를 못 찾아 매번 뒤지는 일이 없도록 문서 맨 앞에 둔다.** 자세한 배경·함정은
아래 `# 📄 도미맨 모바일 앱`(안드로이드)·[런처/코어 분리](PC판 exe).

| 무엇 | 경로 |
|---|---|
| **안드로이드 소스(권위)** | `macro\domiman-android\` — git 추적, GitHub `cheongbaek/domiman`. **여기를 고친다** |
| **Kotlin 코드** | `macro\domiman-android\app\src\main\java\com\example\domiman\` (20개 `.kt`) |
| **안드로이드 빌드 위치** | `C:\Users\windo\dev\domiman-android` — **빌드 전용 거울.** 여기서 고치지 말 것 |
| 빌드 결과 APK(원본) | `…\dev\domiman-android\app\build\outputs\apk\debug\app-debug.apk` |
| **배포 APK ①(폰으로 옮길 것)** | `C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk` |
| **배포 APK ②(리포 옆 사본)** | `macro\domiman.apk` — gitignore(`*.apk`)라 커밋 안 됨 |
| 모바일 Python 원본 | `macro\domiman_m.py` (Chaquopy로 APK에 들어간다) |
| 그 사본(반드시 동기화) | `macro\domiman-android\app\src\main\python\domiman_m.py` |
| PC판 exe 설치본 | `C:\Program Files\domiman\domiman.exe` |
| PC판 빌드 인터프리터 | `macro\venv314\Scripts\python.exe` (PyInstaller 있음) |

**왜 빌드만 `dev`인가:** AGP/Chaquopy는 네이티브 빌드 도구라 **경로에 한글이 있으면
빌드를 거부한다.** `macro`는 `…\한국교통대학교\…` 아래라 안 된다. 그래서 소스는
리포에 두고 빌드만 ASCII 경로로 **단방향 복사**해서 돌린다.

### 안드로이드 APK 빌드·배포 (통째로 복사해 실행)

```powershell
$src = "C:\Users\windo\OneDrive - 한국교통대학교\문서\Python\macro\domiman-android"
$dst = "C:\Users\windo\dev\domiman-android"

# 0) domiman_m.py 를 고쳤다면 프로젝트 안 사본에 먼저 반영 (gradle 자동 동기화 없음)
Copy-Item "C:\Users\windo\OneDrive - 한국교통대학교\문서\Python\macro\domiman_m.py" `
          "$src\app\src\main\python\domiman_m.py" -Force

# 1) 소스만 미러링(빌드 캐시는 그대로 둬야 빠르다)
robocopy $src $dst /E /XD build .gradle .idea

# 2) 빌드 — JAVA_HOME 을 반드시 지정한다(PATH 의 java 는 25라 AGP 가 거부)
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"   # = JDK 21
cd $dst; .\gradlew.bat assembleDebug

# 3) 배포 위치로 덮어쓰기(수동 — gradle 복사 태스크는 일부러 두지 않았다)
Copy-Item "$dst\app\build\outputs\apk\debug\app-debug.apk" `
          "C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk" -Force
```

**확인 요령:** `assembleDebug` 로그가 전부 `UP-TO-DATE` 면 **소스가 안 바뀐 것**이다
(= 새로 만들어진 APK가 없다). 실제로 새 APK가 나왔는지는 산출물과 배포본의
`Get-FileHash` 를 비교하면 확실하다.

**빌드 함정(재발 방지):** ① 한글 경로 거부(위) ② Chaquopy가 구성 단계에서 외부
프로세스를 돌려 Gradle Configuration Cache와 충돌 → `gradle.properties`에
`org.gradle.configuration-cache=false` ③ `buildPython` 자동탐지 실패(이 PC의
`py -3.13`이 Microsoft Store 스텁) → `app/build.gradle.kts`에 python.org 경로 명시
④ 미러는 단방향 — 미러에서 고치면 리포에 안 남고 다음 미러링에 덮어써진다.

---

**런처/코어 분리 + 수동 업데이트 (구현됨):** GitHub 리포
`cheongbaek/domiman`(main 브랜치, Public)에 `domiman.py`/`version.txt`를
올려두고, GUI의 `⟳` 버튼(제어PC 이름 버튼 우측 끝)으로 수동 업데이트를
확인·적용한다. 버전 규약은 `APP_VERSION`(domiman.py [2-1] 섹션) =
"YYMMDD"+알파벳(a,b,c...) 문자열이며 고정 자릿수라 사전식 비교 그대로
날짜+알파벳 순서와 같다. `version.txt`가 더 크면 `domiman.py` 원본 전체를
받아 `os.replace`로 원자적 교체 후 재시작.
- exe 빌드 시 **컴파일 대상은 `launcher.py`(실행 파일 본체=domiman.exe)이고
  `domiman.py`는 `datas`로만 동봉**해 매 실행마다 `runpy.run_path`로 그대로
  읽어서 돌린다(`domiman.spec` 참고). 이렇게 분리한 이유(함정): 실행 중인
  exe 자체는 Windows에서 덮어쓸 수 없어(파일 잠금) exe만으로는 자체 업데이트가
  불가능 — 로직 전체를 진짜 실행 파일이 아닌 옆의 평범한 `.py` 데이터 파일에
  두면 그 파일은 언제든 자유롭게 덮어쓸 수 있다. 업데이트는 결국 이 loose
  `domiman.py`만 교체하는 것으로 끝난다.
- `domiman.py`는 `runpy`로 동적 로드되어 **PyInstaller 정적 분석 대상이
  아니므로**, 그 파일이 쓰는 의존성(pyautogui/requests/numpy/tkinter 등)은
  `domiman.spec`의 `hiddenimports`에 명시해야 묻어들여진다. 새 최상위
  `import`를 domiman.py에 추가하면 스펙의 hiddenimports도 같이 챙길 것
  (안 그러면 스크립트 모드에선 되는데 exe에서만 `ModuleNotFoundError`).
- **⟳ 업데이트는 `domiman.py`만 갈아치우므로, 새 의존성은 "이미 설치된 exe에
  들어 있는 것"에서만 고를 수 있다(설계 제약).** 번들에 없는 패키지를 쓰면 그
  버전은 재설치할 때까지 **시작조차 못 한다.** 그래서 새 import를 넣기 전에
  **설치된 exe를 직접 열어 확인한다**(재사용 방법):
  ```
  venv314\Scripts\python.exe   # PyInstaller가 있는 빌드용 인터프리터로
  # CArchiveReader(exe)로 'PYZ.pyz'를 뽑고 ZlibArchiveReader(...).toc 를 본다
  ```
  260821a에 이렇게 실측한 결과(설치본 `C:\Program Files\domiman\domiman.exe`,
  모듈 5492개): `base64` **있음** · `tkinter.filedialog` **있음** ·
  `matplotlib` **없음**. → 스크린샷 기능이 matplotlib을 못 쓰고 tkinter+cv2로
  간 근거가 이것이다. 스펙에는 그래도 `base64`·`tkinter.filedialog`를 적어
  뒀다(다음 재빌드에서 확실히 남게).
  ※ 순수 stdlib 일부(`struct` 등)는 PYZ가 아니라 `_internal\base_library.zip`
  에 들어가므로 PYZ 목록에 없다고 빠진 것이 아니다.
- 프리징 여부와 무관하게 `os.path.abspath(__file__)`은 실제 `domiman.py`
  경로로 정확히 해석됨(runpy가 `__file__`을 그 경로로 설정) — exe에서도
  `getattr(sys, 'frozen', False)`가 그대로 True로 유지되므로(부트로더가
  `sys` 전역에 심는 속성) 기존 frozen 분기 로직들은 그대로 동작한다.
- exe에서 재시작할 땐 `subprocess.Popen([sys.executable])`(인자 없이
  launcher 자신을 다시 띄움 — 재기동된 launcher가 방금 교체된 새
  domiman.py를 다시 읽음), 스크립트 모드는 기존처럼
  `subprocess.Popen([sys.executable, target])`.
- `launcher.py`는 거의 안 바뀌는 최소 코드(경로 해석 + runpy 디스패치 +
  domiman.py 없을 때 안내 메시지박스)만 담당. DPI awareness는 domiman.py
  자체가 이미 module-level에서 설정하므로 launcher.py에서 중복 설정하지
  않음.
- 새 버전 배포 절차: ① domiman.py 수정 ② `version.txt`를 다음 알파벳으로
  올림 ③ 두 파일만 커밋·푸시. 각 PC는 `⟳` 클릭 한 번으로 반영(재설치·재빌드
  불필요 — launcher.py/의존성 DLL 자체가 바뀌는 경우에만 재설치 필요).

---

## 현재 아키텍처 (중요)

### 해상도 — 7종 지원, 계열별 변환 (260925c)
**상세는 문서 앞쪽 `## 🖥 다해상도 지원` 절이 권위 문서다.** 여기서는 요약만:
- 좌표는 전부 **FHD 기준으로 적고**, `layout_point`/`layout_region`이 앵커
  계열(`popup` 중앙고정 / `hud` 가장자리+배율)에 따라 렌더 해상도로 옮긴다.
  그 다음 창 클라이언트 크기로 화면 좌표까지 환산한다(2단).
- 렌더가 FHD(=`1080`·늘린 QHD `1440`)면 1단계가 **항등**이라 260828a까지
  검증된 동작이 그대로 보존된다.
- 인식 로직(윤곽선 매칭·OCR)은 해상도와 무관하게 **완전히 동일**하다.

### 캡처 방식 자동 감지 = 모니터 기반 (`detect_capture_mode`)
사용자에게 묻는 것은 **렌더 해상도**뿐이고, 캡처 방식은 **게임 창이 있는
모니터**로 자동 판별한다(창을 못 찾으면 지금 값 유지).
- **주 모니터 · 1920x1080 · 100% 배율 → `direct`** (pyautogui 직접 캡처 가능)
- **그 외(보조 모니터/고배율/비1080p) → `wgc`** (WGC로 창 직접 캡처)
- `_pick_game_hwnd`가 진짜 창 선택(숨은 창 제외), `MonitorFromWindow` +
  `GetMonitorInfo`로 모니터 물리 크기·주모니터 여부, `GetDpiForMonitor`로 배율.
- 최소화 상태에서도 모니터는 정확히 잡히므로 판정 가능(4케이스 검증 완료).
- `windows-capture`가 없으면 거부하지 않고 `direct`로 물러선다(경고만).

**왜 창 크기가 아니라 모니터인가 (중요):** 게임은 **DPI-unaware**라
(`GetDpiForWindow`가 어느 모니터든 96=100% 고정), 창 클라이언트 크기가
상황에 따라 `1920x1080`(가상화)로도 `2400x1350`(물리)로도 **튄다**.
따라서 **캡처 방식** 판정에는 크기를 쓰지 않는다. 모니터 물리 크기만이
유일하게 신뢰할 신호다. 과거 노트의 "QHD=2400x1350 고정" 전제는 틀렸다.
(**렌더 해상도 추정**에는 창 크기를 쓰되, 그 흔들림을 흡수하려고 DPI 배율로
나눈 해석까지 후보에 넣는다 — `_estimate_render_resolution`. 그래서 틀릴 수
있고, 그래서 수동 선택 7칸이 남아 있다.)

### CNN은 제거됨
과거 QHD는 학습된 CNN(`fish_model.pth`)으로 분류했으나, 원인이 "확대로 인한
실루엣 뭉개짐"이 아니라 "확대 화면에 FHD 좌표를 그대로 쓴 좌표 어긋남"임을
확인. WGC로 선명한 원본을 받아 1080p로 축소하면 FHD 로직이 그대로 동작하므로
CNN·torch·torchvision·result_qhd 폴더 의존성을 모두 제거했다. **되살리지 말 것.**

---

## OCR 정확도 — 엔진이 아니라 **들어가는 그림**이 문제다 (260815c, 실측)

숫자 판독이 크게 틀리는 사례(`417/480`→`40/4`, `15초`→`1초`)를 두고 "easyocr을
바꿔야 하나"를 검토했는데, **측정 결과 엔진 문제가 아니었다.**

사용자 제공 `20260815121853.jpg`(정답: 살림망 414/480, 최소·최대 15초)로 잰 값:

| 입력 상태 | 살림망 | 최소/최대 시간 |
|---|---|---|
| 원본(1080p) | `414/480` ✅ | `15초` ✅ |
| QHD→1080p 축소 경로 | `414/480` ✅ | `15초` ✅ |
| **0.6배로 흐리게** | `44480` ❌ | `15소`·`75초` ❌ |
| **0.5배로 흐리게** | `diuzo` ❌ | `초` ❌ |

즉 **선명하면 어떤 파이프라인도 100% 맞히고, 0.5~0.6배로 뭉개지면 무엇을 해도
못 맞힌다.** 0.6배에서의 실패 모양(`44480` = 자릿수 누락)이 사용자가 본 `40/4`와
같다 → **화면이 그만큼 흐린 상태로 캡처되고 있다는 뜻**이다. 원인은 셋 중 하나:
게임 창이 1920x1080보다 **작아서** 확대돼 캡처되거나, 창 크기·DPI가 어긋나 영역이
밀렸거나, 그 프레임이 갱신 애니메이션 중이었거나.

**그래서 한 일(엔진 교체가 아니라 입력·판정 보강):**
1. **원본 프레임에서 자른다** — 예전엔 1080p로 축소한 프레임을 잘랐다(QHD면 정보를
   버리고 시작). 좌표만 배율 환산해 원본에서 자르므로 화면이 클수록 더 선명해진다.
2. **숫자 영역은 x3 확대 + 흑백** 후 읽는다. 10가지 전처리를 4단계 열화에 돌려
   고른 값(x3+흑백 7/12, 그대로 6/12, x4 4/12).
   **숫자 화이트리스트(allowlist)는 넣지 않는다 — 측정에서 오히려 나빠졌다**
   (9케이스 중 3→2. `414/480`을 `473480`으로 만드는 등 슬래시를 잃는다).
   좋아 보이는 아이디어라도 실측이 아니라고 하면 넣지 않는다.
3. **첫 성공을 그대로 쓰지 않고 여러 프레임의 다수결**로 고른다(수량·획득 시간 공통).
4. **최대치가 바뀐 것처럼 보이면 한 번 더 확인**한다(`417/480`→`40/4`는 최대치가
   바뀐 것처럼 보인다). 낚싯대 교체로 최대치가 진짜 바뀔 수 있으므로 **버리지 않고
   같은 값이 두 번 나오는지만** 본다 — 진짜 교체는 통과하고 한 번 튄 값은 걸러진다.
5. **판독 실패·수상한 값이 나오면 그 순간의 크롭을 `ocr_dump/`에 PNG로 남긴다.**
   화면을 볼 수 없는 상태에서 "영역이 밀린 건지, 프레임이 튄 건지, 정말 뭉갠 건지"를
   가릴 유일한 방법이다(최대 40장).

**다음에 또 틀리면:** `ocr_dump/`의 그림을 먼저 본다. 글자가 또렷한데 틀렸다면
그때는 엔진(RapidOCR 등)이나 **고정 폰트 숫자 템플릿 매칭**을 검토할 차례고,
그림 자체가 뭉개져 있다면 엔진을 바꿔도 소용없다 — 게임 창을 1920x1080 이상으로
키우는 것이 답이다.

**→ 260822a에 정확히 그 일이 일어났다.** 저장된 크롭이 **또렷한데도** 틀렸고,
원인은 엔진이 아니라 이 절이 고른 **x3 전처리 자신**이었다. 답은 엔진 교체가
아니라 글자 폭으로 자릿수를 재는 것이었다 — 아래 절 참고.

---

## 힘들게 알아낸 함정들 (재발 방지)

### 1. 숨은 중복 창 함정 (가장 중요)
게임은 같은 제목 `'Tales Runner'` 창을 **두 개** 만든다:
- 진짜 렌더링 창: 가시성 1, 클라이언트 2400x1350, 화면상 (2035, -337) 등
- 숨은 내부 창: **가시성 0**, 크기 1920x1080, 위치 (0,0)

제목만으로 찾으면 숨은 창(1920x1080, (0,0))을 잡아서 좌표 변환이 무력화된다
(`ClientToScreen(0,0)`이 (1,1)을 반환해 오프셋이 안 더해짐).
**해결: 창 선택 시 반드시 `IsWindowVisible` 체크 + 클라이언트 영역이 가장 큰
창을 고른다.** `bring_game_to_front`와 `_pick_game_hwnd` 둘 다 이 방식.
(`_find_game_hwnd`는 '보이는 창 중 첫 번째'를 고르는 옛 함수라 260828a에
**삭제**했다 — 창 선택은 `_pick_game_hwnd` 하나로 모은다.)

### 2. 좌표 변환 (`to_screen`) — **FHD/QHD 공통** (260828a)
좌표는 항상 FHD(1920x1080 공간) 기준으로 저장. 클릭 시 실제 화면 좌표로 변환:
- `GetWindowRect`(2403x1353)가 아니라 **`GetClientRect`(2400x1350) +
  `ClientToScreen`**을 써야 3px 테두리 오차가 없다.
- 공식: `sx = ox + fx/1920*cw`, `sy = oy + fy/1080*ch`
  (ox,oy=ClientToScreen(0,0), cw,ch=클라이언트 크기)
- 검증됨: FHD(1007,1006) → 실측 살림망 버튼 (3292,926)와 2~4px 오차로 일치.
- top이 음수(-337)일 수 있음(창이 화면 위로 삐져나감). 공식은 음수 정상 처리.
- DPI: 시작 시 `SetProcessDpiAwareness(2)` 설정. GetClientRect/ClientToScreen/
  SetCursorPos 모두 물리 픽셀로 일관됨.

**FHD 모드도 이 변환을 쓴다 (260828a, 실제 증상으로 고침 — 되돌리지 말 것):**
예전 `to_screen`은 `CURRENT_RESOLUTION == "1080p"`이면 **변환을 건너뛰고 FHD
좌표를 화면 좌표로 그대로** 돌려줬다(= 클라이언트 영역이 화면 (0,0)에
1920x1080으로 딱 붙어 있다는 전제). 그래서 **QHD는 창이 움직여도 정확한데 FHD만
창이 조금 움직이면 클릭이 전부 어긋나** 루틴이 진행되지 않았다.
- 증상이 헷갈리는 이유: **수량 파싱은 멀쩡했다.** 감시 모드 읽기는 FHD에서도
  WGC로 창을 직접 잡아(창 기준) 창이 어디 있든 잘 읽는다 — 즉 "숫자는 잘 읽는데
  버튼만 헛클릭"이 정상적인 증상 조합이다. 판독 쪽을 의심할 일이 아니다.
- 자르기도 같이 고쳤다: 1080p의 `grab_region_rgb`(pyautogui)는 **찍을 자리도**
  창을 따라가야 한다(`_fhd_region_to_client`). 안 그러면 창이 움직였을 때
  퀴즈 인식·성공 판정이 조용히 실패한다. `capture_game_png`의 pyautogui
  폴백(원격 스크린샷)도 같은 변환을 쓴다.
- 창이 전제 위치(0,0 · 1920x1080)에 있으면 오프셋 0·배율 1이라 **결과가 예전과
  글자 그대로 같다** — 검증된 좌표는 그대로 유효하다(9개 좌표로 옛 코드와
  동일함을 확인, QHD 2400x1350·top -337 케이스도 동일).
- `_client_geometry()`가 유일한 근거이고 창은 `GAME_HWND`(=`bring_game_to_front`가
  골라 둔 것)를 먼저 쓴다. 그것이 **죽었거나 최소화(GetClientRect가 0x0)면**
  `_pick_game_hwnd`로 다시 찾는다(매 클릭마다 EnumWindows를 돌지 않기 위한 순서).
  창을 아예 못 찾으면 예전 동작(FHD 좌표 그대로)으로 폴백하고 경고를 찍는다.
- 창 클라이언트가 움직이거나 크기가 바뀌면 `[좌표] 게임 창 클라이언트 (x,y)
  WxH 기준으로 변환합니다.`를 **바뀔 때 한 번만** 찍는다(클릭마다 찍으면 로그가
  그것만으로 가득 찬다). 다음에 "클릭이 어긋난다"는 보고를 받으면 이 줄부터 볼 것.

### 3. WGC 캡처
- `windows_capture.WindowsCapture(window_name="Tales Runner")` — **정확 일치**
  필요(부분매칭 안 됨, 대소문자·공백 정확히).
- 프레임: `frame.frame_buffer`는 (H,W,4) BGRA numpy.
- 백그라운드 실행: `cap.start_free_threaded()` (블로킹 `start()`와 구분).
  ※ 이 메서드명은 패키지 버전에 민감. 캡처가 안 되면 여기부터 의심.
- 캡처는 루틴 시작 시 켜고 끝나면 끔(대기 중 CPU 절약). `GameCapture` 클래스가
  최신 프레임 보관, `get_frame_1080()`으로 요청 시 축소본 생성.

### 4. OCR 오인식
- easyocr이 '살림망'을 자주 '산림망'으로 읽음(conf도 낮음, ~0.18).
  → 완전일치 금지. **`'림망'` 부분매칭** 사용(공백 제거 후).
- 버튼 글자도 따옴표/공백이 섞임: "'낚시 취소", '마미롬보내기'(마이룸) 등.
  → 항상 부분매칭 + `.replace(" ","")` 전처리.

---

## 검증된 좌표·영역 (FHD 기준, **전 해상도 공통**)

아래 값은 전부 FHD 기준이고, 앵커 계열에 따라 변환된다(`## 🖥 다해상도 지원`).
괄호 안이 계열 — `hud`는 화면 하단중앙+배율, 표기 없으면 `popup`(화면 중앙 고정).

버튼 클릭 좌표:
- 낚시 취소/시작: (1086, 988)  `hud` — 토글 버튼(글자만 바뀜)
- 살림망 확인:    (1007, 1006) `hud`
- 미끼 리스트:    (903, 913)   `hud`
- 낚싯대 리스트:  (814, 917)   `hud`
- 마이룸 보내기:  (1138, 245)
- 확인(팝업):     (958, 575) — **지금은 누르지 않는다(Enter로 대체, 260925c)**
- 미끼/낚싯대 다음 ▶: (1296, 565) — 옛 1291에서 보정(260925b)
- 미끼/낚싯대 이전 ◀: (620, 565) — 실측 확정, 현재 미사용

인식 영역 (x,y,w,h):
- 질문 물고기 좌: (742, 406, 78, 69)
- 질문 물고기 우: (830, 407, 78, 69)
- 보기 그리드:    (972, 433, 226, 296)  — 4행 3열, 최대 10칸
- 성공 판정 텍스트:(832, 503, 255, 38)
- 낚시 버튼 글자: (1040, 983, 88, 40) `hud`

OCR로 읽는 정보 (감시 모드용, 아래 참고):
- 살림망 수량:      중앙 (890, 1007), OCR 예 '17/920' (conf 1.00)
- 최소 획득 시간:   중앙 (975, 933),  OCR 예 '20초'  (conf 0.97)
- 최대 획득 시간:   중앙 (1075, 933), OCR 예 '70초'  (conf 0.97) — 만료 교차 확인용

---

## 타이머 0 = 살림망 감시 모드 (구현됨)

`watch_tank_mode()`로 구현. 아래 사양대로 동작한다.

**진입:** 타이머 입력(시작 시 · `t` 메뉴 둘 다)에서 `0` 또는 `o`/`O`를 가로채
`watch_tank_mode()` 호출. 무한 루프라 반환하지 않으며 메뉴의 `q` 또는 Ctrl+C로
종료. ntfy로는 `o`만 사용(ntfy `0`은 dual_input에서 ''로 변환되어
'지금 실행' 관례와 겹치므로). OCR 영역(실측 검증 완료):
`REGION_TANK_QTY=(825,989,130,36)`, `REGION_MIN_TIME=(940,916,72,34)`,
`REGION_MAX_TIME=(1035,916,80,34)`(낚싯대 만료 교차 확인용, `minsec==1`일 때만 읽음).
(수량은 자릿수 1~3에 따라 중앙정렬로 폭이 변해 1자리에서 잘리므로 좌우로 넓힘.
 읽기는 프레임 여러 번 재시도해 갱신 애니메이션/깜빡임을 건너뜀.)

**감시 모드 읽기는 FHD/QHD 모두 WGC 사용(중요):** 일반 낚시 루틴은 FHD=pyautogui,
QHD=WGC지만, **감시 모드는 두 모드 다 WGC로 게임 창을 직접 캡처**한다
(`_ensure_watch_capture`가 FHD에서도 `GameCapture` 생성·기동, `_watch_grab_region`이
WGC 프레임에서 crop). 이유: pyautogui는 화면 좌표를 직접 찍어 **다른 창이 하단
숫자 영역을 가리면 실패**하는데, 감시 모드는 장시간 무인 동작이라 가림·포커스에
강한 WGC가 필수. WGC로 메인/서브 모두 정확히 읽힘 검증됨(53/920 등). WGC 패키지가
없으면 pyautogui로 폴백. (단, 창이 최소화되면 WGC도 프레임을 못 받으니 감시 중
게임은 최소화하지 말 것.)

**동작(감시 루프):**
1. 매 사이클마다 두 영역을 OCR로 읽는다:
   - 살림망 수량 (890,1007): 정규식 `(\d+)\D+(\d+)`로 current/max 추출
     (슬래시가 공백·붙음·세로줄로 깨질 수 있으니 유연하게)
   - 최소 획득 시간 (975,933): 정규식 `(\d+)`로 초 추출
2. **회수 조건: `current >= max - 5`** (가득차기 5칸 전).
   만족하면 `run_fishing_routine()` 실행(기존 회수 루틴 그대로 재사용).
   판정은 `_tank_needs_collect(qty)` / `TANK_COLLECT_MARGIN = 5`로 빼두어
   낚싯대 교체 직후 확인과 같은 기준을 공유한다(아래 교체 섹션 참고).
3. **폴링 간격 = 최소 획득 시간(초) 그대로**(1.0배).
   max-5 버퍼가 있어 한 사이클에 최대 1마리 → 그대로 폴링해도 절대 못 넘김.
   낚싯대 교체로 최소 획득 시간이 바뀌면 다음 사이클부터 자동 반영.
4. 회수 후 수량은 리셋되므로 그대로 감시 계속(무한 루프).

**메뉴 진입(폴링 대기 중):** 폴링 간격 대기는 `time.sleep`이 아니라
`wait_with_keycheck`(키보드+ntfy 큐 동시 감시)로 한다. 대기 중 아무 일 없으면
다음 사이클로 넘어가고, 키가 눌리면 **감시를 일시정지**(WGC 캡처 정지, CPU 절약)한
뒤 메뉴(`_watch_menu_keyboard`)를 띄운다. 메뉴 옵션:
`r`(로그)·`n`(ntfy)·`d`(해상도 재감지)·`q`(종료)·`Enter`(재개),
그리고 `t`(타이머 재설정)는 **감시 모드를 끝내고 일반 타이머 모드로 전환**한다
(`handle_timer_change`로 새 주기 입력·즉시실행 처리 후 `run_timer_loop`로 넘어가며,
감시로 복귀하지 않는다. 단 `t` 후 `0`/`o`를 넣으면 감시 모드로 재진입).
`r`/`n`/`d`/`Enter`는 메뉴를 빠져나오면 루프 top의 `_ensure_watch_capture`가
캡처를 다시 켜 감시를 재개한다. ntfy 원격 명령은 `_watch_handle_ntfy`가 즉시
처리(입력 블로킹 없음). 메인 타이머 대기 루프는 `run_timer_loop()`로 추출돼
`__main__`과 감시 모드 `t`가 공유한다. 일반 모드 메뉴 로직
(`process_menu_choice`)과 토글 동작은 동일하게 맞춰 재사용성을 지킨다.

**안전장치(중요):**
- 파싱 실패하거나 값이 비정상이면 그 사이클은 버리고 **직전 정상 폴링 간격 유지**.
  (못 읽었다고 간격을 0으로 만들면 CPU를 태움)
- **`cur > mx`(예: 520/470)는 '비정상'이 아니다** — 최대치가 더 작은 낚싯대로
  바꾼 뒤의 정상적인 초과 상태다. 파싱에서 버리면 안 되고(그러면 영구 정체),
  회수 조건에 걸려 바로 회수돼야 한다. 아래 교체 섹션의 '초과 상태를 파싱에서
  버리면 안 된다' 항목 참고.
- 숫자·최소획득시간은 낚싯대 교체 중에도 항상 화면에 떠 있음(확인됨).

---

## 미끼·낚싯대 자동 교체 (감시 모드 전용, 구현됨)

미끼 소진('미끼가 부족합니다' 팝업) 또는 낚싯대 기간 만료('! 아이템 기간
만료' 팝업)가 뜨면 낚시가 멈춰 살림망 수량이 정체된다. 감시 루프가 이를
감지해 자동으로 교체하고 낚시를 재개한다.

**트리거 (미끼/낚싯대 서로 다름, 함정 — 만료 팝업 OCR은 폐기됨):**
- **미끼**: 수량 파싱에 성공한 매 사이클마다 `REGION_NO_BAIT=(890,499,142,36)`을
  OCR해 '미끼가'/'부족' 부분매칭. 팝업 글자 conf가 0.3~0.5대로 낮아 완전일치는
  금지인데, 그만큼 헛걸릴 위험도 커서 **낚시 버튼이 '낚시 시작'(대기 중)인지까지
  AND로 확인**한 뒤 `run_bait_swap_routine()`으로 간다(260822a):
  `if self.bait_swap and _detect_no_bait_popup() and is_fishing_active() is False:`
  미끼가 정말 떨어졌으면 낚시가 멈추므로 버튼은 '낚시 시작'이다(표본 실측:
  `popup_20260719015400`은 팝업 감지 O + 버튼 '낚시시작' conf 1.00 → 통과).
  버튼 OCR은 **팝업이 보일 때만** 나가므로 평시 부담은 그대로다 — 순서를 바꾸지 말 것.
- **낚싯대**: 아래 두 조건의 **OR**.
  `if self.rod_swap and (minsec == 1 or qty[0] > qty[1]):`
  1. **최소·최대 획득 시간이 둘 다 정확히 `1`(초)** — 낚싯대 교체가 필요하면
     게임이 획득 시간을 '1초'로 표시하는 것을 이용. 최소는 이미 매 사이클 읽는
     값을 재사용하고, **최대는 `minsec == 1`일 때만** 추가로 읽는다(평시 OCR
     부담 그대로).

     **최소만 보면 안 된다 — easyocr이 '11초'를 '1초'로 읽는다(실측, 260813a):**
     리포의 `20260813001332.jpg`(최소 11초/최대 12초인 **정상** 낚시 화면)에서
     최소가 '1초'로 읽혀 낚싯대 교체가 헛발동했다. **확신도로는 못 거른다** —
     틀린 '1초'가 0.57~0.95, 맞는 '11초'가 0.51~0.54로 오히려 뒤집힌다. 영역을
     넓혀도 그대로다(잘림이 아니라 인식 오류). **확대 배율로도 못 고친다 —
     배율마다 서로 다른 값에서 깨진다**(여기 적혀 있던 "x2·x3는 '11초', x4는
     '1초'"는 260815c에서 흑백 변환을 넣기 **전** 측정이라 지금과 정반대다.
     현재 파이프라인은 x3에서 '1초' 확신도 **0.99**로 확정적으로 틀린다.
     260822a의 실측표를 볼 것). 이 오인식은 아래 260822a 절의 **글자 폭 심판**
     으로 잡는다.
     → **낚싯대 기간이 만료되면 최소·최대가 둘 다 '1초'** 라는 성질로 교차
     확인한다. 표본 실측: `expire_20260719025713`은 1초/1초, 정상 화면은
     30/60·20/70·11/12로 **최대가 항상 더 크다.** 최대가 1이 아니면 최소의
     '1'은 오인식이므로 교체하지 않고, 그 값을 폴링 간격에도 쓰지 않는다
     (안 그러면 간격이 1초→`max(3.0,…)`=3초로 떨어져 OCR을 헛돈다).
     최대가 **판독 실패**면 보류한다 — 진짜 만료면 다음 사이클에도 최소가
     1로 읽혀 곧 다시 확인되지만, 멀쩡한 낚싯대를 갈아 끼우면 되돌릴 수 없다.
     오인식 로그는 상태가 바뀔 때까지 **한 번만** 찍는다(`misread_warned`).

     **한 번 읽고 갈면 안 된다 — `_confirm_rod_expiry` (260815b, 실측 사고):**
     최소·최대가 **둘 다 15초**인 정상 화면에서 둘 다 '1초'로 읽혀 낚싯대를
     헛교체한 사례가 나왔다(사용자 제공 `20260815121853.jpg`). **그 스크린샷을
     지금 좌표·배율로 다시 읽으면 '15초'가 확신도 0.99~1.00으로 정확히 나온다**
     — 좌표·전처리 문제가 아니라 **실시간 캡처에서 한 프레임이 튄 것**이라,
     '최대도 1초'라는 교차 확인만으로는 못 거른다(둘 다 같은 프레임에서 튄다).
     그래서 **교체 조건 자체를 세 개의 AND로** 만들었다(260822a):
     `최소 = 1초  AND  최대 = 1초  AND  낚시 버튼 = '낚시 시작'(대기 중)`
     ① **낚시 버튼이 '낚시 시작'인지 먼저 본다.** 낚싯대 기간이 만료되면 낚시가
        멈추므로 버튼은 반드시 '낚시 시작'이 된다. 반대로 오인식은 대개 낚시가
        **잘 돌고 있는** 화면에서 나므로 버튼이 '낚시 취소'다. 기록에 남은 헛교체
        사고 두 건이 이 조건 하나로 다 막힌다(실측: `expire_20260719025713`
        '낚시시작' 1.00 / `20260813001332` '낚시취소' 0.99 / `20260815121853`
        '낚시취소' 1.00). `is_fishing_active()`가 **None(판독 불가)이면 교체하지
        않는다** — '모르겠다'를 '대기 중'으로 취급하면 조건을 하나 잃는다.
     ② 0.4초 뒤 **다시 읽어** 최소·최대가 계속 1초인지 확인(`ROD_EXPIRY_CONFIRM_READS=2`).
        ①과 중복이 아니다 — 낚시가 **실제로 멈춘** 상태(미끼 소진 등)에서 획득
        시간이 한 프레임 튀는 경우는 ①로 못 걸러지기 때문이다. 값을 읽는 쪽을
        아무리 조여도 완벽할 수 없으니 **원인이 다른 신호**를 함께 요구한다.
     ~~③ 수량이 오래 멈춰 있으면 진행 중 표시를 무시하고 교체~~
        (`ROD_EXPIRY_FROZEN_CYCLES`)는 **제거됐다(260822a)** — 그것이 바로 ①을
        우회하는 구멍이었다.

     ~~'! 아이템 기간 만료' 팝업 OCR~~ 방식은 폐기됐다(함정): 낚싯대가 아닌
     **다른 아이템의 만료 팝업에도 반응하는 오탐**이 있었고 이름 필터('낚' 한
     글자 부분매칭)로도 못 막았다. → **낚싯대와 무관한 원인에도 걸리는 신호를
     트리거로 쓰지 말 것.** ('낚시가 멈췄다' 같은 원인 불문 신호도 같은 이유로
     안 된다 — 실제로 그렇게 고쳤다가 되돌린 적 있다.)
  2. **`cur > mx`** (예: `570/420`) — 최대치가 더 작은 낚싯대로 바뀌어 살림망이
     **넘친** 상태. 이때는 '낚시 시작'을 눌러도 낚시가 걸리지 않아 **회수만으로는
     재개하지 못한다.** 최대치가 더 큰 낚싯대로 갈아 끼워 초과 상태를 벗어난다.

     **②에도 낚시 버튼 AND가 걸린다(260822b — 실제 사고로 추가):**
     `if qty[0] > qty[1] and is_fishing_active() is False:`
     260822a가 ①에만 게이트를 달았더니, 정작 진행 중인 낚시를 끊은 것은
     **게이트가 없던 ②**였다. ②는 확인 절차가 **하나도** 없어서 수량 오인식
     한 번이 곧바로 교체가 됐다. 조건의 논리는 ①과 같다 — **초과 상태에선
     낚시가 걸리지 않으므로 버튼은 '낚시 시작'이어야 한다.** 낚시가 잘 돌고
     있다면 그 `cur > mx` 판독은 오인식이다.
     초과가 진짜인데 버튼을 못 읽어 막혀도 갇히지 않는다: 초과는 회수 조건
     (`cur+5>=mx`)의 부분집합이라 바로 아래 회수 분기가 받아 살림망을 비운다.
     막힐 때는 그 순간의 OCR 원문을 로그에 찍고 크롭을 `ocr_dump/`에
     `tank_overflow_*`로 남긴다(다음에 원인을 그림으로 볼 수 있게).

  **②는 `cur + 5 >= mx`(`_tank_needs_collect`)가 아니다 — 그 넓은 조건은
  260811a에서 제거됐고 되살리지 말 것(함정):** 회수 조건 전체를 트리거로 쓰면
  **살림망이 찼을 뿐 멀쩡한 낚싯대까지** 갈아 끼운다. 실제 로그에서
  `467/470`·`469/470`·`470/470`마다 교체→회수→교체를 반복했다. `cur > mx`는
  낚싯대 최대치가 **실제로 모자란** 상태만 가리켜 그 오작동이 없다.
  `cur <= mx`면 회수만 진행한다.

  **② 는 반드시 회수 분기보다 먼저 검사한다(함정):** ②는 회수 조건의
  부분집합이라, 순서가 뒤면 회수 분기의 `continue`가 삼켜 낚싯대 검사에
  **도달조차 못 한다.** 실제로 이 순서 때문에 `570/470`에서 교체가 안 돌았다.
  현재 순서: **낚싯대 → 회수 → 미끼 → 낚시정지 감지.**

**리스트 진입도 통일됨(함정 — 팝업 버튼을 더 이상 클릭하지 않음):** 두
루틴 모두 **ESC 3번(0.5초 간격, 혹시 떠 있을 팝업 종류를 모르니 무조건 정리)
→ 고정 좌표 클릭으로 리스트 직접 열기**로 바뀌었다. 팝업 안의 버튼을 찾아
클릭하던 방식(팝업이 이미 닫혀 있거나 다른 팝업이 겹쳐 있으면 실패)보다
안정적.
- 미끼: `COORD_BAIT_LIST_BTN = (903, 913)` (실측, 기존 팝업 속 '보유 미끼'
  버튼 (1004,578)은 폐기)
- 낚싯대: `COORD_ROD_LIST_BTN = (814, 917)` (재실측, 기존 (815,916)에서 소폭 보정)

**미끼 교체 (`run_bait_swap_routine`):**
1. `bring_game_to_front` 후 ESC×3 → `COORD_BAIT_LIST_BTN` 클릭으로 리스트 직접 진입.
2. `_find_cards_by_pattern(BAIT_TARGET_PATTERN)`으로 현재 페이지 탐색.
   미발견 시 오른쪽 화살표 (1291, 565)로 페이지 넘김 — **최대 4번**(총 5페이지).
3. 발견하면(좌상단 우선) 해당 칸 사용. 끝까지 실패하면 ntfy 경고 후
   **좌상단 (0,0) 칸을 대신 사용**(대기하지 않고 강행).

**낚싯대 교체 (`run_rod_swap_routine`):** `bring_game_to_front` 후 ESC×3 →
`COORD_ROD_LIST_BTN` 클릭으로 리스트 직접 진입.
`ROD_TARGET_PATTERN = r"스타|[장잠][미며]|[매메미민][직적졕]|[푸무꾸][른튼름슨촌릎]"`
('매직 스타 낚싯대'/'푸른 장미검 낚싯대'의 조각들 중 **하나라도** 걸리면 채택 —
원래 `r"스타|장미검"`였으나 '장미검'이 '장미경'으로 오인식돼 통째로 놓쳐서 각
이름의 짧고 특징적인 음절들로 흩었고(260815a), 260909a에 **실측 오인식을 그대로
OR로 넓혔다**: 적중 12/12·12/12 · 오검출 0/60. 위 260909a 절 참고). 페이지 넘김
없음(현재 페이지에서만 탐색). 못 찾으면 ntfy 긴급 경고(priority 5) 후
ESC → 낚시 시작만 시도.

**낚싯대 교체 직후 살림망 확인 (`_restart_or_collect_after_rod_swap`, 함정):**
낚싯대마다 **최대 살림망 개수가 다르다.** 새로 든 낚싯대의 최대치가 기존보다
작으면 이미 쌓인 물고기가 그 최대치를 넘은 상태(예: `520/470`)가 되고,
그 상태에선 **'낚시 시작'을 눌러도 낚시가 걸리지 않는다.** 교체 후 무조건
'낚시 시작'을 누르던 예전 방식은 그래서 클릭이 먹히지 않은 채 수량만 정체됐다.
지금은 '낚시 시작'을 누르기 **직전에** `read_tank_quantity()`로 한 번 읽고 갈린다:
- 여유 있음(예: `165/470`) → 평소대로 `COORD_FISHING_BTN` 클릭해 재개.
- 회수 조건(예: `467/470`)이거나 최대치 초과(예: `520/470`) → '낚시 시작'을
  **누르지 않고** 곧바로 `run_fishing_routine()`. 회수 루틴은 맨 앞에서
  `is_fishing_active()`로 버튼 글자를 읽어 '낚시 시작'(대기 중)이면 취소
  절차를 스스로 생략하므로, 별도 감시 조건 없이 바로 불러도 안전하다.
- 판독 실패 → 평소 동작(그냥 시작)으로 폴백.

판정 기준은 감시 루프와 공유(`_tank_needs_collect` / `TANK_COLLECT_MARGIN = 5`).
읽기 전 0.5초 대기는 교체한 낚싯대 기준으로 수량 표시가 갱신될 시간. 보고 순서는
`rs` → `y,r`/`x,r` → (회수 시) `s` → `g`/`f`.

**초과 상태(`cur > mx`)를 파싱에서 버리면 안 된다 (이 기능 최대의 함정):**
`read_tank_quantity`는 `cur > mx`를 **그대로 돌려준다.** 예전의
`0 <= cur <= mx` 가드는 이걸 OCR 오인식으로 보고 버려 `None`을 돌려줬는데,
그러면 스스로 못 빠져나오는 고리가 된다:

> 초과 → 낚시가 안 걸림 → 수량이 줄지 않음 → 계속 초과 → 매 사이클 파싱 실패
> → 회수 조건 판정까지 도달 못 함 → 회수 안 됨 → 초과 유지

**초과는 교체 직후 한순간의 값이 아니라 회수할 때까지 계속 남는 상태**라,
교체 루틴 쪽 호출에서만 열어주는 걸로는 부족하다 — 260809a가 `allow_overflow`
인자를 만들어 그 호출에서만 켰다가, 감시 루프는 기본값(초과 거부)으로 읽어
그대로 정체돼 260809c에서 인자를 없애고 전역으로 고쳤다. 파싱이 초과를 늘
받아들이는 덕분에 교체 루틴이 초과를 놓쳐도(예: 갱신 전 최대치를 읽어 '여유
있음'으로 판단) **감시 루프가 다음 사이클에 스스로 회수해 복구한다.**

**값 범위로 오인식을 거르는 가드는 두지 말 것(위 버그의 교훈):** 대체 가드로
`cur <= mx * 3` 상한을 잠깐 뒀다가 뺐다. 잡는 범위가 원리적이지 않다 —
`520/47`(max 절삭)은 우연히 걸리지만 앞자리가 빠진 `52/470`은 그냥 통과한다.
실효는 없으면서 "예상 못 한 정상 값을 조용히 버릴 위험"만 `cur <= mx`와 똑같이
남는다. 오인식은 프레임 4회 재시도와 감시 루프의 매 사이클 재파싱으로 씻겨
나가므로 그쪽에 맡긴다. 남은 조건은 `mx > 0` 하나이고, 이건 휴리스틱이 아니라
구조적 무의미값 차단이다(max가 0이면 `cur >= mx-5`가 항상 참이라 회수 무한 반복).

**판독 실패 진단:** 마지막 OCR 원문을 전역 `_last_tank_ocr`에 남기고, 감시
루프의 기존 '수량 파싱 실패' 로그 한 줄에 함께 찍는다(`is_fishing_active()`의
실패 로그와 같은 방식 — 줄 수는 늘지 않음).

**공통 마무리 (`_use_card` / `_use_card_and_restart`):** '사용하기' 클릭 →
클릭 후 **ESC 2번(0.5초 간격)** 까지가 `_use_card`, 거기에 '낚시 시작' =
`COORD_FISHING_BTN` (1086, 988) 클릭까지 붙인 것이 `_use_card_and_restart`다.
미끼 교체는 뒤엣것을 그대로 쓰고, 낚싯대 교체는 앞엣것만 쓴 뒤 위의 살림망
확인이 시작/회수를 결정한다.
ESC가 2번인 이유(중요): 리스트 창은 팝업 **위에 겹쳐** 뜨므로 ESC 1번은
리스트 창만 닫고 밑에 깔린 팝업이 최상단 모달로 남아 '낚시 시작' 클릭을
삼킨다(실측: 교체가 무한 반복되는 로그로 확인). ESC는 `press_esc()`
(ctypes `keybd_event` + '[Key] ESC' 로그) 사용.

**카드 이름 판독 (`_find_cards_by_pattern`):** 보유 미끼/보유 낚싯대 창은
**그리드가 동일**(실측 수 px 차이)해 좌표를 공유한다. '갯지렁이'가
'개지렇미'로 깨지는 등 오인식이 잦아 유연한 정규식 사용
(`BAIT_TARGET_PATTERN = r"[금급].?지[렁렇령런랑]|[금급][캠켓것개캣컷][지찌]|[활왕항팔창황][금급]"`
— '갯'은 실측 5/5 전부 오인식이고 '금'이 5/5 살아남아 260822c에 '금' 축으로
바꿨고, 260909a에 나머지 실측 오인식을 OR로 넓혔다(5/5 · 오검출 0). 위 260909a
절 참고 — **'갯지렁'류를 넣으면 1페이지 '갯지렁이 훈제 구이'에 걸린다**).
**좌표는 크롭 실제 크기로 정규화해야 한다**(260909a 함정 — 안 하면 QHD에서 2행이
통째로 버려진다). 이름 바 8칸을 덮는
`REGION_BAIT_NAMES=(640,483,640,202)`를 통째로 `_watch_grab_region`(WGC)으로
읽고, 매칭 글자의 중심을 `BAIT_NAME_ROW_Y=(503,660)` / `BAIT_COL_X=(715,878,
1041,1204)`에 스냅해 (행,열) 결정. 글자가 하나도 안 읽히면(창 애니메이션 중
프레임) 0.5초 후 1회 재시도.

**좌표 (FHD 기준, 실측):** '사용하기' 버튼 2행x4열 `BAIT_USE_BTNS` =
1행 (708,544) (874,543) (1038,540) (1209,541) /
2행 (714,702) (877,699) (1042,702) (1201,701). (낚싯대 창도 동일 좌표 재사용)
클릭은 전부 `click_real`(to_screen 변환)이라 QHD·창 위치와 무관하게 정확.

## 낚시 정지 감지 · 자동 재개 (구현됨)

미끼/낚싯대 교체 트리거에 안 걸리는 원인으로 낚시가 조용히 멈춰버리면
살림망 수량이 그대로 정체된다. `COORD_FISHING_BTN=(1086,988)` 버튼은
낚시 중엔 '낚시 취소', 대기 중엔 '낚시 시작'으로 글자만 바뀌는 토글이라
이 글자를 읽어 상태를 판별한다.

**OCR 위치 (`REGION_FISHING_BTN=(1040,983,88,40)`, `낚시상태버튼OCR검증.py`로
실측 후 여유를 두어 확대):** 낚시 중인 라이브 화면에서 "'낚시 취소'"
(conf 0.92, 앞뒤에 따옴표가 붙어 나옴), 낚시 대기 중 스크린샷에서
'낚시 시작'(conf 0.999)이 **거의 같은 좌표**에서 잡혀 "두 버튼 위치가 같다"는
전제를 확인했다.

**판정은 '취소'/'시작' 부분매칭이 아니라 `_fishing_state_from_text`가 한다
(260815b, 함정):** easyocr이 **'낚시 시작'을 '낚시시직'으로 읽는** 일이 잦아
부분매칭으로는 판독 실패가 됐다. 후보가 '낚시 취소'/'낚시 시작' **둘뿐**이므로,
앞의 '낚시'를 떼고 **뒤 두 글자만** 두 후보와 대조해 겹치는 글자가 많은 쪽을
고른다('시직'→'시작'과 '시' 하나 겹침, '취소'와는 0). '시'는 '낚시'에도 들어가
그대로 세면 늘 '시작'이 유리해지므로 **반드시 앞부분을 떼고 본다.** 오인식
종류를 나열하는 방식보다 새 오인식에도 강하다(검증: 13가지 표본).
둘 다 안 맞으면 None(호출측이 무시하되 원문을 로그로 남김).

**감시 루프 트리거:** 살림망 수량(`(cur,mx)`)이 **3회 이상 연속 동일**하면
(정상적으로도 몇 사이클 안 잡힐 수 있어 약한 신호 — 그래서 곧바로 재개하지
않고) `is_fishing_active()`로 한 번 더 확인한다. `True`(여전히 낚시 중)면
그냥 정상 상황이므로 아무 것도 안 함. `False`(대기 중)로 확인되면
`_resume_fishing()` 호출. 동일 카운트는 매 사이클 초기화(qty가 바뀌면
리셋)되며, 3회 이상인 동안은 매 사이클 계속 재확인(더 빨리 잡기 위해
정확히 3번째만 보지 않음).

**실시간 수량확인 = 로컬 버튼·원격 N 공용 로직 `_tank_check_and_resume`
(260728a에서 통합):** 회수 루틴 시작 때처럼 **게임 창을 앞으로 불러**
살림망을 새로 읽고(3초 렌더 대기), **동시에 낚시 취소/시작 버튼을 확인해
'낚시 시작'(대기 중)이면 `_resume_fishing()`으로 눌러 재개**한다('낚시
취소'=진행 중이면 그대로 둠). 한 배경 스레드 안에서 창 호출→캡처→읽기→
낚시상태 확인까지 끝내 캡처 소유권 경쟁(과거 `_query_tank` 콜백 직후
finally의 `game_capture.stop()`이 상태확인 스레드와 TOCTOU로 충돌하던
문제)을 없앴다. **재개는 `is_fishing_active() is False`(정확히 '낚시 시작'=
대기 중)일 때만** — 진행 중(True)이나 판독 불가(None)면 클릭하지 않는다.
**항상 창 호출 방식으로 동작(260728b, 워커 가동 무관):** 예전엔
`self._running()`(감시 워커 가동)이면 캐시(`_last_tank`)만 반환하고 창을 안
건드렸으나, 워커 스레드는 살아 있는데 게임 낚시가 조용히 멈춘 경우 창을
안 불러 확인/재개가 안 되는 문제가 있어 그 캐시 분기를 제거했다. 이제 신호가
오면 무조건 창을 불러 새로 읽고 상태 확인 후(정상 진행 중이면 재개 클릭은
안 나가므로 워커의 정상 낚시는 방해 안 함) 재개한다.
- `on_tank_check`(로컬)는 결과를 로그로 출력, 원격 N 명령은 `reply("N,...")`
  로 응답 — 둘 다 `_tank_check_and_resume(report)`를 공유(옛 `_query_tank`는
  삭제됨). 원격 N도 워커 미가동이면 PC 창을 불러 재개까지 수행한다.
- **회수해야 할 수량이면 재개 클릭을 생략한다(260809c):** 읽은 값이
  `_tank_needs_collect`면 — 특히 최대치 초과면 — '낚시 시작'을 눌러도 낚시가
  안 걸리므로 헛클릭 대신 로그로 알리기만 한다. 감시 워커가 돌고 있으면
  다음 사이클에 회수 루틴이 받아가고, 회수가 끝나면 거기서 낚시가 재개된다.

**재개 동작 (`_resume_fishing`):** ESC 3번(0.5초 간격, 혹시 떠 있을 팝업
정리) → `COORD_FISHING_BTN` 클릭. 미끼/낚싯대 교체 루틴의 새 진입 방식과
동일한 패턴(무조건 ESC로 정리 후 고정 좌표 클릭)이라 함수로 공유.

**살림망 수거 루틴(`run_fishing_routine`) 시작부에도 같은 확인 적용:**
기존엔 무조건 "Enter+ESC 4번 반복 → `COORD_FISHING_BTN` 클릭(낚시 취소)
→ `COORD_TANK_BTN` 클릭(살림망 확인)" 순서였는데, 이 루틴이 **낚시 중이
아닐 때** 불려도 그대로 실행돼 버튼을 잘못 눌렀다(취소해야 할 게 없는데
같은 좌표를 누르면 오히려 낚시가 새로 시작돼 버림). 이제 맨 앞에서
`is_fishing_active()`로 먼저 확인해 `False`(대기 중)로 **확실히** 판명된
경우에만 Enter/ESC 반복과 취소 클릭을 생략하고 바로 `COORD_TANK_BTN`으로
건너뛴다. `True`(진행 중)나 `None`(판독 불가)이면 안전하게 기존 그대로.

**`skip_cancel=True`면 판독조차 하지 않고 생략한다:** 낚싯대 교체 →
`_restart_or_collect_after_rod_swap` → 회수로 들어오는 경로가 이 인자를
넘긴다. 교체 루틴이 방금 ESC로 화면을 정리했고 '낚시 시작'도 누르지
않았으므로 닫을 창도 취소할 낚시도 없는 것이 **확실**하다 — OCR로 되물을
이유가 없고, 취소 좌표를 헛눌러 낚시가 새로 시작되는 위 함정도 피한다.
다른 경로(감시 루프의 일반 회수)는 기존 `is_fishing_active()` 판단 그대로.

**접속 끊김 감지 (감시 모드 전용):** 게임이 튕기면 로비 배경 + '서버와
접속이 끊어졌습니다.' 대화상자만 남아 **수량 파싱이 계속 실패**한다.
→ 수량 파싱 **실패** 분기에서 `REGION_DISCONNECT=(769,386,258,40)`을 OCR해
'서버와' 부분매칭(타이틀 conf 0.64; 본문은 0.01이라 사용 금지). 판독되면
'게임이 튕겼습니다' 출력 + ntfy 긴급 알림(priority 5) 후 **매크로 종료**
(`sys.exit(1)`). 미판독이면 평소처럼 다음 사이클 재시도.
('프로그램 종료' 버튼 중심 (960,607), conf 0.97 — 현재는 클릭하지 않음)


---

## ⚠️ 260822a — 오탐의 진짜 원인은 **x3 확대 전처리**였다 (실측, 해결됨)

'최소 획득 시간 1초' 오탐이 "모든 상황에서 간헐적"으로 나서 못 쓸 지경이라는
보고를 받고 실측했다. **엔진 문제도, 화면이 흐린 문제도 아니었다 —
`_prep_number_ocr`의 x3 확대가 얇은 글자를 뭉개고 있었다.**

**증거 ① 정적 이미지에서 100% 재현되는 오인식.** `20260813001332.jpg`(실제
11초/12초)를 지금 파이프라인으로 읽으면 최소가 **'1초' 확신도 0.99**. 프레임
튐이 아니라 결정적 오인식이라 `_confirm_rod_expiry`의 0.4초 재판독도 그냥
통과한다. 원인은 CTC 인식기가 **붙어 있는 같은 글자를 하나로 합치는** 것
('11'→'1'). 그래서 '10·12·15초'는 맞히고 '11초'만 무너진다.

**증거 ② 설치본 `ocr_dump/`의 '판독 실패' 크롭 13장 중 12장이 사람 눈에
또렷했다.** x3로 읽으면 **슬래시가 숫자 1로 바뀐다**(`244/470`→`2441470`,
`44/470`→`441470`). 그러면 `(\d+)\D+(\d+)`가 안 맞아 실패가 된다. 원본
배율로 읽으면 12/12 정확. (남은 1장은 게임 패널이 화면에 아예 없던 프레임 —
사람도 못 읽는다. 나머지 28장은 캡처가 거의 새카맸다: 색 2가지·평균 26.6.)

**증거 ③ 어느 전처리도 혼자서는 안전하지 않다.** 획득 시간 16케이스
(1·5·10·11·12·15·20·30·40·60·70초, QHD 원본 포함) + 수량 크롭 13장:

| 전처리 | 획득 시간 | 수량 | 혼자서 틀리는 것 |
|---|---|---|---|
| 원본+컬러 | 15/16 | 12/13 | '11초'→'1초' |
| **원본+흑백** (주 경로, 260822c) | 15/16 | 12/13 | '70초'→'7초' |
| x3+컬러 | 15/16 | 3/13 | '20초'→'2초' |
| **x3+흑백** (폴백, 260822c 이전엔 주 경로) | 15/16 | **0/13** | '11초'→'1초' |

전부 15/16이면서 **서로 다른 곳에서** 깨진다. → **"더 좋은 배율/엔진으로
갈아치우기"는 답이 아니다.** 두 경로를 함께 쓰고 심판을 두어야 한다.

### 해결 = 글자 폭으로 자릿수를 직접 재서 OCR을 심판한다 (`_ink_span_fhd`)

게임 글꼴이 고정폭이라 **잉크 폭(흰 글자의 좌우 끝 거리)이 자릿수를 그대로
알려준다.** 밝기 임계를 170~230으로 훑어도 값이 흔들리지 않았다(230에서만 ±2):

| 실제 값 | FHD 기준 잉크 폭 |
|---|---|
| 1초 · 5초 | **21 · 22px** |
| 10·11·12·15초 | 29px |
| 20·30·40·60·70초 | 30~32px |

1자리 21~22 vs 2자리 29~32로 **7px 이상 벌어져 완전히 분리**되므로 경계를 그
중간(`INK_DIGIT2_MIN = 26`)에 둔다. QHD 원본도 크롭 실제 폭으로 정규화해
30.1·32.0으로 같은 구간에 들어온다(화면 크기와 무관).

`_read_gain_time`의 동작:
1. **원본+흑백으로 읽는다**(260822c에서 x3와 자리를 바꿨다 — 아래 절 참고).
2. 판독값의 **자릿수가 폭과 어긋나면 같은 프레임을 원본+흑백으로** 다시 읽는다.
3. 그것도 어긋나면 그 표를 버린다.
4. 표가 **과반이 아니면 None.** 예전엔 1표씩 갈리면(1초 1표 / 15초 1표)
   `max(set(...), key=count)`가 `set` 순서대로 임의로 골라 **튄 값 하나가
   낚싯대를 갈 수 있었다.**

**폭을 못 재면(글자가 아예 없으면) 판단을 보류하고 판독값을 수용한다** — 폭이
없다고 멀쩡한 판독을 버리면 진짜 만료를 놓친다. 진짜 '1초'는 흰 픽셀이 80개라
언제나 측정된다.

수량(`read_tank_quantity`)은 폭 대신 **`(\d+)\D+(\d+)` 성립 여부**를 심판으로
쓴다(슬래시가 숫자로 바뀌는 실패라 정규식 자체가 신호다). 실패한 경우에만
원본+흑백으로 한 번 더 읽으므로 정상 화면에서는 추가 비용이 없다.

### 그리고 조건에 **낚시 버튼 상태**를 AND로 넣었다 (이게 제일 강하다)

판독을 아무리 조여도 100%는 못 되니, **원인이 다른 신호**를 하나 더 요구한다:

    낚싯대 교체 = 최소 1초 AND 최대 1초 AND 버튼 '낚시 시작'
    미끼 교체   = '미끼가/부족' 팝업 AND 버튼 '낚시 시작'

근거는 단순하다 — **교체가 필요한 상황이면 낚시가 이미 멈춰 있다.** 그런데
오인식은 낚시가 **잘 돌고 있는** 화면에서 나온다. 보유 표본이 그대로 갈린다:

| 표본 | 획득 시간 | 낚시 버튼 | 결정 |
|---|---|---|---|
| `expire_20260719025713` (진짜 만료) | 1초/1초 | **'낚시시작' 1.00** | 교체 O |
| `20260813001332` (실제 11초/12초) | 11/12 | '낚시취소' 0.99 | 교체 X |
| `20260815121853` (실제 15초/15초) | 15/15 | '낚시취소' 1.00 | 교체 X |
| `popup_20260719015400` (미끼 없음) | — | **'낚시시작' 1.00** | 미끼 교체 O |

**기록에 남은 헛교체 사고 두 건이 이 한 줄로 다 막힌다.** 게다가 값 판독과
독립된 신호라, 앞으로 나올 새로운 오인식에도 그대로 통한다.

`is_fishing_active()`가 **None(판독 불가)이면 교체하지 않는다** — '모르겠다'를
'대기 중'으로 취급하면 조건을 하나 잃는다. 버튼 OCR은 후보가 '낚시 취소'/
'낚시 시작' 둘뿐이고 `_fishing_state_from_text`가 뒤 두 글자만 대조하는 방식이라
튼튼하다(라이브 40/40 정확).

이 조건이 들어오면서 **`ROD_EXPIRY_FROZEN_CYCLES`(수량이 오래 멈춰 있으면 진행
중 표시를 무시하고 교체)는 제거했다** — 그것이 바로 버튼 조건을 우회하는
구멍이었다. 함께 검토했던 '연속 사이클 요구'도 이 조건이 더 강하고 싸서
넣지 않았다(교체가 한 사이클 늦어지는 비용도 없다). 버튼이 영구히 안 읽히는
사태의 탈출구는 트리거 ②(`cur > mx`)가 버튼을 보지 않는 것으로 남아 있다.

### 검증

- 표본 **23항목 전부 정확**(획득 시간 16 + 수량 7). 특히 진짜 만료 표본
  `expire_20260719025713`(1초/1초)은 그대로 **만료판정 True** — 오탐을 줄이면서
  진짜 신호를 놓치지 않는 것이 이 작업의 조건이었다.
- `ocr_dump/`의 판독 실패 크롭 **12/13 회복**(개량 전 0/13).
- 라이브(5초/10초 낚싯대, 게임 실행 중) **107사이클**: 최소 5초 107/107, 수량
  판독 실패 0회, 만료 오탐 0회. 1사이클 비용 수량 0.27초 / 획득 시간 0.85초로
  예전과 같다.
- **교체 '결정'까지 표본 8개로 검증**: 진짜 만료만 교체 O, 나머지 7개 전부 X.
  더해서 **"오인식이 정말 일어났다면"** 시나리오로 정상 낚시 화면 4개에서
  `_confirm_rod_expiry()`를 직접 불러 **4/4 전부 버튼 조건에서 막히는 것**을
  확인했다(이 검사가 핵심이다 — 판독이 뚫려도 결정은 안 뚫린다).
- 미끼 쪽도 표본 확인: 진짜 팝업 화면(`popup_20260719015400`)은 통과하고, 정상
  낚시 화면은 팝업이 안 잡혀 애초에 진입하지 않는다. `bait_*` 표본은 팝업이
  아니라 **보유 미끼 리스트 화면**이므로 팝업 감지 대상이 아니다(헷갈리기 쉬움).

### 260822b — ①만 막고 ②를 열어 둔 대가 (실제 사고, 진단 함정)

260822a를 배포한 직후 낚싯대 교체가 **무한 반복**됐다. 낚시는 잘 돌고 있는데
교체 로직이 들어와 리스트 창을 열고(진행 중이라 교체는 무시됨) 끝에 낚시 버튼을
눌러 **잘 되던 낚시를 취소**시켰다. 그러면 낚시 정지 감지가 올바르게 재개하고,
다시 같은 일이 반복됐다.

**로그가 범인을 숨겼다(이게 핵심 교훈):** `run_rod_swap_routine()`이 시작 문구로
`'최소 획득 시간 1초 감지'`를 **하드코딩**해 찍고 있었다. 그래서 트리거 ②로
들어와도 로그에는 ①처럼 보였고, "버튼 확인이 왜 안 되나" 하며 ①만 파게 됐다.
→ **호출 사유가 둘 이상인 루틴에서 사유를 고정 문구로 찍지 말 것.** 지금은
`run_rod_swap_routine(reason)`으로 받아 `낚싯대 기간 만료(획득 시간 1초/1초)` /
`살림망 최대치 초과 114/570`처럼 찍는다.

**단서는 로그 안에 있었다:** `_confirm_rod_expiry`의 차단 로그가 **한 줄도
없었다**(①이 시도조차 안 됐다는 뜻) + 교체 직후
`_restart_or_collect_after_rod_swap`이 같은 살림망을 다시 읽어
`[살림망 114/570] 여유 있음`을 찍었다 — **교체를 유발한 `cur > mx` 판독이
틀렸다는 증거가 바로 다음 줄에 있었다.** 판독을 조인 것과 별개로, **확인 절차가
없는 트리거를 하나라도 남겨 두면 그 하나로 전부 무너진다.**

검증: 교체 결정 재현 9케이스 전부 기대대로(낚시 진행 중 + 초과 오인식 4건 차단,
낚시 멈춤 + 진짜 초과 2건 교체, 진짜 만료 교체, 만료 오인식 차단, 정상 무동작).


### 260822c — x3를 폴백으로 내리고, 이름은 '살아남는 음절'로 찾는다

**① `112/570`을 `'712/570'`으로 읽어 낚싯대 교체가 또 나갔다(1→7 치환).** 진단
크롭(`tank_overflow_*`)을 전처리 6가지로 다시 읽으니 **현재 주 경로(x3+흑백)만
틀렸다**:

| 전처리 | 판독 |
|---|---|
| 원본+컬러 / 원본+흑백 / x2+흑백 / x3+컬러 / x4+흑백 | `112/570` ✅ (원본은 확신도 1.00) |
| **x3+흑백** | **`712/570` (0.87)** ❌ |

이로써 x3가 유발한 실패가 **세 종류**가 됐고(`'11초'`→`'1초'`, 슬래시→`1`,
`1`→`7`), **세 번 모두 원본 배율은 정확했다.** → **주 경로를 원본+흑백으로
뒤집고 x3는 폴백으로 내렸다.** x3를 고른 260815c 측정은 표본을 **인위적으로
0.5~0.6배로 흐리게 만들어** 비교한 것이라 실제 실패 모드(선명한 화면에서의
오인식)를 대변하지 못했다. **열화를 인위적으로 만들어 고른 값은 실제 실패
표본으로 반드시 다시 검증할 것.**

부수 효과: `ocr_dump`의 판독 실패 크롭 회복이 12/15 → **14/15**(남은 1장은
게임 패널이 화면에 없던 프레임).

**② 카드 이름은 easyocr로 정확히 읽을 수 없다(실측 상한 65%).** '낚싯대/미끼
이름이 하나같이 뭉개진다'는 보고를 받고, 정답을 아는 셀 15개(`20260822041218`
낚싯대 7 + `20260822041216` 미끼 8)에 **전처리 14가지 × easyocr 파라미터 7가지
= 21조합**을 돌렸다.

- **어떤 조합도 정확히 읽지 못했다 — 0/15.** 글자 일치율 최고는
  `x3+반전이진화(흰 글자→검정 글자) + beamsearch`의 **65.0%**(현재 51.4%).
- 오인식은 계통적이다: `낚싯대`→`낡짓대/낭짓대/낯짓대/낚시대`,
  `지렁이`→`지렇이/지렇미`, `황금`→`왕금/광금/활금/팔금`.
- **영역을 통째로 읽는 실제 코드 경로**에서 다시 재보니 전처리를 바꿔도 목표
  매칭에 이득이 없고 시간만 2~4배 늘었다(원본 1.3초 → x3반전 5.2초).
  → **이름 영역 전처리는 그대로 둔다.** 실측이 이득 없다고 하면 넣지 않는다.

**그래서 고친 것은 패턴이다.** 목표 칸('황금 갯지렁이')을 전처리 5가지로 읽은
결과:

    팔금개지렇미 · 활금개지렇미 · 왕금켓지렇이 · 왕금것지렇이 · 활금캠지렇미

**`갯`은 5번 중 한 번도 맞지 않고(개/켓/것/캠), `금`은 5번 전부 살아남았다.**
옛 패턴 `[갯개]지[렁렇령]`은 **5번 중 2번만** 걸려 대상을 놓치고 좌상단 폴백을
쓰곤 했다(사용자가 본 증상). 새 패턴은 **살아남는 음절을 축으로** 잡는다:

| 후보 | 적중 | 오검출(판독 40건) |
|---|---|---|
| 옛 `[갯개]지[렁렇령]` | 2/5 ❌ | 0 |
| **새 `금.{0,2}지[렁렇령]`** | **5/5** ✅ | 0 |
| `[갯개것켓캠컷캣]지[렁렇령]` | 5/5 | 0 |

뒤엣것도 같은 성적이지만 **오인식 종류를 나열하는 방식이라 새 오인식이 나오면
또 뚫린다** — 그래서 앞엣것을 골랐다. 낚싯대 패턴은 손대지 않았다: `푸른`이
푸튼/푸릎/푸른/무른으로 흔들리지만 **`장미`가 항상 살아남아** 이미 5/5·오검출
0이다(그 때문에 260815a에 조각들로 흩어 놓은 설계가 옳았음이 확인됐다).

**이름을 읽는 일반 원칙(재사용):** 게임 UI의 작은 한글은 **정확히 읽으려 하지
말고, 여러 전처리에서 살아남는 음절을 찾아 그것을 축으로 매칭한다.** 어느 음절이
살아남는지는 반드시 실측으로 정한다 — '갯'처럼 사람 눈에 뚜렷한 글자가 0/5로
무너지고 '금'이 5/5로 살아남는 일이 실제로 일어난다.

검증: `_find_cards_by_pattern`을 두 스크린샷에 그대로 돌려 낚싯대 (1,0)·미끼
(1,2) 모두 정확히 찾았다. 표본 판독 23항목·교체 결정 9케이스는 그대로 전부 통과.

**재현할 때 알아야 할 것(함정):** 개량 **전** 코드로도 5초/10초 낚싯대에서는
556프레임 연속 측정에서 오탐이 **0회**였다. 이 오탐은 **'11초'처럼 같은 숫자가
붙은 2자리 값**에서만 나므로, 1자리 낚싯대를 끼운 채로는 아무리 오래 돌려도
재현되지 않는다. "지금 화면에서 정상"을 근거로 문제가 없다고 결론 내리지 말 것.

---

## ⚠️ 260909a — 이름을 못 찾은 진짜 원인은 **크롭 배율**이었다 (실측, 해결됨)

'푸른 장미검/매직 스타 낚싯대를 잘 인식하지 못한다'는 보고를 받고 실측했다.
**패턴 문제도, 오인식 문제도 아니었다 — `_find_cards_by_pattern`이 크롭 픽셀을
FHD 오프셋에 그대로 더하고 있었다.**

**증거: 라이브 5회 전부 미발견.** 보유 낚싯대 창을 띄운 채 실제 함수를 그대로
부르면 `[]`가 5/5로 돌아왔고(→ 좌상단 '대나무 낚싯대' 폴백), **배율만 나눈
버전은 5/5 정확**했다. 미끼도 같다(황금 갯지렁이: 현행 3/3 미발견 / 보정 3/3 적중).

**원인:** `_watch_grab_region`은 260815c부터 1080p 축소본이 아니라 **WGC 원본
프레임에서** 자른다. 이 PC는 게임 클라이언트가 2400x1350이라 크롭이
**801x253**(FHD 영역 640x202의 **1.252배**)로 나오는데, 그 좌표를 보정 없이 쓰면:

| 조각 | 무보정 cy | 행오차 | 판정 | 보정 시 |
|---|---|---|---|---|
| 대나무낚셋대 (1행1열) | 506 | 3.0 | 1행1열 | 1행1열 |
| 판크무드빗자루 (1행**3**열) | 506 | 3.0 | 1행**4**열 ❌ | 1행3열 |
| 매직스타낚시대 (2행1열) | 704 | **44.0** | **버림** | 2행1열 (오차 0.5) |
| 무릎장미경낚시대 (2행2열) | 704 | **44.0** | **버림** | 2행2열 (오차 0.5) |

- **2행이 통째로 버려진다** — 오차 `(660-483)×0.252 = 44.6` > 허용 30. 목표 세
  개(매직 스타·푸른 장미검·황금 갯지렁이)가 **모두 2행**이라 영구 미발견이었다.
- **1행도 열이 밀린다** — 3열이 4열로 잡혀 **옆 칸의 '사용하기'를 누른다.**
- 클라이언트 폭이 약 2245px을 넘으면 2행이 깨지는 계산이라 **FHD(배율 1.0)에서는
  증상이 없었다.** 그래서 여태 안 잡혔다 — "FHD에서 잘 되니 로직은 맞다"는
  추론이 틀리는 자리다.
- 고친 뒤 FHD 결과는 배율이 1.0이라 **곱셈이 ×1.0이 되어 예전과 글자 그대로
  같다.** 검증된 좌표·행Y·열X는 그대로 유효하다.

**교훈(재사용):** `_watch_grab_region`으로 자른 그림의 좌표를 쓰는 곳은 **반드시
크롭 실제 크기로 정규화**해야 한다. 그 docstring은 "호출부는 전부 정규화하도록
되어 있다"고 적어 뒀지만 이 함수만 예외였다 — 새로 크롭을 쓰는 코드를 넣을 때
이 절을 먼저 볼 것.

### 그리고 OR을 실측 오인식으로 넓혔다 (배율과 별개의 개량)

보유 목록을 전수 판독해(낚싯대 7종×전처리 12가지, 미끼 **33종 5페이지**×5가지)
나온 오인식을 그대로 OR에 넣었다.

    ROD_TARGET_PATTERN  = r"스타|[장잠][미며]|[매메미민][직적졕]|[푸무꾸][른튼름슨촌릎]"
    BAIT_TARGET_PATTERN = r"[금급].?지[렁렇령런랑]|[금급][캠켓것개캣컷][지찌]|[활왕항팔창황][금급]"

- 낚싯대: 매직→매직/미적, 장미→장미/잠미, 푸른→푸른·푸튼·푸름·무릎·무른·꾸슨·꾸촌,
  검→경/검/겉/거. **'스타'는 12/12로 한 번도 깨지지 않았다**(가장 튼튼한 축).
  적중 **12/12 · 12/12**, 오검출 0/60. 옛 패턴은 푸른 장미검을 9/13만 잡았다.
- 미끼: 황금 갯지렁이의 실제 판독은 `항금캠지렇이·활금것지렇미·n금캠지렇미·창급것지꼬이`
  로 '금'조차 '급'으로 깨진다. 세 갈래 OR로 **5/5 · 오검출 0**(옛 패턴 4/5).
- **단음절 OR로 되돌리지 말 것:** 옛 낚싯대 패턴의 `푸` 한 글자는 '무/꾸'로 깨지면
  무력하면서 앞으로 '푸'가 든 다른 낚싯대가 생기면 오검출이 된다. 두 글자 묶음이
  더 넓고 동시에 더 안전하다.

**미끼에 '갯지렁'류를 OR로 넣으면 안 된다 (실측, 이 작업 최대의 함정):** 보유
목록에는 **'갯지렁이 훈제 구이'(1페이지 1행3열)** 와 **'다이아 갯지렁이'(4페이지
1행4열, 현재 사용 중)** 가 따로 있다. 옛 패턴 `[갯개]지[렁렇령]`을 쓰면 이 둘이
프로덕션 경로에서 4/5·4/5로 걸리고, 미끼 교체는 페이지를 순서대로 훑어 **처음
걸린 칸을 쓰므로 1페이지의 훈제구이를 집는다.** 같은 이유로 `금.{0,2}`처럼 사이를
2글자까지 벌리는 것도 되돌리지 말 것 — '레볼루션 지렁이'가 `허금루선지렇미`로
읽혀 걸린다(1글자로 조이면 목표 적중은 그대로면서 이 오검출이 사라진다).
**후보가 2개뿐인 낚싯대와 달리 미끼는 33종이 서로 비슷해, OR을 넓힌 만큼 이웃
미끼를 잡는다** — 넓히기 전에 반드시 전체 목록에 돌려 오검출을 셀 것.

**검증:** 실제 QHD 크롭 6장(낚싯대 리스트 + 미끼 1~5페이지)을 수정된
`_find_cards_by_pattern`에 그대로 통과시켜 **6/6 기대대로** — 낚싯대 2행1열(매직
스타)·미끼 3페이지 2행4열(황금 갯지렁이)을 찾고, 함정 페이지(1·4)와 빈 페이지
(2·5)에서는 `[]`.

### 보유 미끼 전체 목록 (260909a 실측, 5페이지 33종)

향후 '어느 미끼로 교체할지 지정' 기능의 기준 목록. 수량은 그때 값.

| | 1열 | 2열 | 3열 | 4열 |
|---|---|---|---|---|
| p1 1행 | 삼각 주먹떡밥 | 크릴 새우 필라프 | ⚠️갯지렁이 훈제 구이 | 황제 |
| p1 2행 | 혼돈의 | 범생이 | 장군 | 탐험가 |
| p2 1행 | 여왕 | 무당 | 선무당 | 페스티벌 |
| p2 2행 | 언더월드 | 감수성이 풍부한 | 은하수 | 승리의 |
| p3 1행 | 어둠에 물든 | 별빛 | 레볼루션 | 혼이 나간 |
| p3 2행 | 우주 여행자 | 선사인 | 데저트 | ⭐**황금 갯지렁이** |
| p4 1행 | 해왕강림 | 라스트 카오스 | 무지개 왕관 | ⚠️다이아 갯지렁이(사용 중) |
| p4 2행 | 애니빌 | 도스트 | 꽃기사 | 유기농 밭지렁이 |
| p5 1행 | 프로페서 | — | — | — |

**미끼 지정 기능은 정규식이 아니라 '최근접 이름 매칭'으로 갈 것(실측 근거):**
목록 33종을 알고 있으므로 판독을 정규식으로 거르는 대신 **알려진 이름 중 문자
유사도가 가장 높은 것**을 고르면 프로덕션 경로 **32/33**(전처리 5종 165판독
기준 94.5%)이 정확하다. 정규식으로는 원리적으로 못 가리는 쌍이 있다 —
'무당'의 판독 `무탕지렇미`는 '선무당'의 판독 `선무탕지렇미`의 **부분문자열**이라,
부분매칭으로는 둘을 절대 구분할 수 없고 실제로 '무당'은 고유 조각이 **없다**.
(유일한 오분류는 '애니빌'→'별빛'. 임계값·2위와의 격차 확인이나 전처리 다수결로
보강할 자리다.) '사용 중'인 미끼는 버튼이 '사용하기'가 아니므로 대상에서 제외해야
한다.

---

## ⚠️ 원격 제어는 ntfy를 버리고 domichat으로 옮겼다 (260815a, 구현됨)

**ntfy 관련 코드는 domiman.py에서 전부 제거됐다.** 아래 'ntfy 원격 제어 프로토콜'
절의 **메시지 규격(S/G/P/Y/W/Q/V/T/C/N, `,Z,...` 응답, `,Z,F,*` 보고)은 글자 하나
다르지 않게 그대로 유효**하지만, 전송 방식과 GUI는 다음과 같이 바뀌었다.

| 항목 | 바뀐 내용 |
|---|---|
| 전송 | ntfy.sh 채널 공유 → **domiserver(자체 채팅 서버) 소켓 연결**. 클라이언트 계층은 `domichat.py`에서 복제(규격 기준은 `domichat.md`) |
| 방 | 피제어 PC마다 방 하나: `domi_fishing_{피제어 ID}`. **비밀번호 방**(고정 비번 `domi_fishing_9714` — 일반 domichat 사용자 오진입 방지용) |
| 방장 | 피제어 PC. 실행 시 **없으면 만들고 있으면 입장**(`room_name_taken` 오면 join) 후 구독 |
| 제어 | 대상 ID로 방 이름을 계산해 그 방에 입장·구독 → 예전처럼 `S` 질의로 상태 동기화 |
| 식별자 | **PC 이름 설정 항목 폐지.** 발신자는 domichat 로그인 ID(`MY_ID`)이며 서버가 찍어주므로 위조 불가. 예전 ntfy 이름 설정은 무시 |
| 자기 발신분 | `from == MY_ID` 로 걸러낸다(서버는 발신자에게도 에코) |
| 종료 감지 | 피제어 PC가 꺼지면 `member{in:false}` 를 받아 제어를 자동 해제. 정상 종료는 즉시(on_exit이 `CHAT.logout()`을 먼저 보낸다), 강제 종료는 서버 타임아웃(최대 45초) |
| 동시 역할 | **허용.** 내 방(피제어)과 남의 방(제어)에 동시에 있을 수 있다. 예전 "원격 중 자기 명령 무시" 규칙은 폐기 |
| GUI | 'ntfy/SMS에 표시될 이름' 자리 → **[로그인] 버튼**, 그 옆 칸 → 접속 IP 표시 / 'ntfy 채널 이름' 자리 → **[로그아웃] 버튼**, 그 옆 칸 → 로그인 ID 표시 / 'ntfy 메시지' → **'domichat 메시지'**(원격 제어 모드에서는 이 칸이 **[스크린샷] 버튼**으로 바뀐다 — 아래 전용 절) |
| 자격 | IP·ID·PC 리스트는 `domiman_config.json`, 비밀번호는 `domiman_secrets.dat`(DPAPI 암호화). 로그아웃하지 않으면 재실행 시 자동 로그인. 저장 위치는 `DOMIMAN_DIR` 환경변수로 바꿀 수 있다(테스트용) |
| 잠금 | 로그인/로그아웃은 **낚시 중에도 잠그지 않는다**(원격이 끊겼을 때 복구 수단). 원격 제어 중에만 봉인 |
| 안드로이드 앱 | **당장 동작하지 않는다.** ntfy 기반이라 나중에 포팅해야 한다(확정) |

서버가 꺼져 있어도 낚시는 계속된다 — 로그인 실패는 원격 기능만 막고 재연결 백오프로
계속 시도한다.

---

## 원격 스크린샷 (260821a, 구현됨)

제어 PC가 피제어 PC의 **게임 화면을 사진으로 받아 본다.** domichat의 이미지 전송
규격(`file_begin` → `'B'` 청크 → `file_end`)을 그대로 쓴다.

**GUI: 'domichat 메시지' 체크박스 칸을 원격 모드에서만 버튼으로 바꿔 끼운다.**
직접 제어 모드에서는 체크박스가 그대로 살아 있고, 원격 제어 모드에서는 같은 grid
칸(row 4, col 0)에 **[스크린샷]** 버튼이 들어온다(`_update_remote_widgets`가
`grid()`/`grid_remove()`로 교체, `_enter_remote`·`_exit_remote`에서 호출). 그
체크박스는 원격에서 어차피 늘 봉인되므로 죽은 칸을 두는 대신 자리를 내준다.
버튼은 응답 대기 중이거나 사진을 기다리는 중이면 봉인(`_apply_ui_locks`).

**창 폭이 튀는 것을 막아야 한다(실측 함정):** '스크린샷' 버튼이 'domichat 메시지'
체크박스보다 좁아서, 그냥 바꿔 끼우면 원격 진입/복귀 때마다 **창 너비가 20px씩
줄었다 늘었다 한다**(`resizable(False, False)`라 내용이 폭을 정한다). 그래서
`_build_widgets`에서 `f.columnconfigure(0, minsize=cb_chat.winfo_reqwidth() + 12)`
로 그 칸의 최소 폭을 **체크박스 기준으로 못 박았다.** 픽셀을 상수로 적지 않고
`winfo_reqwidth()`로 재는 이유는 폰트·DPI가 달라도 따라가게 하려는 것.
두 버튼('스크린샷'·'실시간 수량확인')은 `sticky="w"`로 각자 제 폭을 쓴다 —
`"ew"`로 늘리면 칸 전체를 채워 아래 버튼과 폭이 어긋난다.

**흐름:**
1. 제어 → `{대상},I` 발송(`pending` 15초 = 다른 명령과 동일).
2. 피제어 → 즉시 `,Z,I` ack. 이어서 배경 스레드가 **실시간 수량확인과 똑같은
   절차**로 찍는다: `bring_game_to_front` → 3초 렌더 대기 → `capture_game_png()`
   → `chat_send_image()`로 **자기 방(상태 보고용)** 에 업로드.
   캡처·전송이 불가능하면 `,Z,I,fail`을 보내 요청자가 헛되게 기다리지 않게 한다.
   OCR 준비는 **따지지 않는다** — 글자를 읽지 않고, 해상도 감지가 실패한 화면을
   눈으로 확인하는 것이 이 기능의 존재 이유다.
3. 제어 → ack를 받으면 대기 상태(`shot_wait`, 상태 문구 '강태공이 사진을 찍고
   있습니다')로 들어가 `_on_file`이 청크를 모은다. 크기·sha256을 확인한 뒤
   `ScreenshotWindow`를 띄운다. **기다리는 중이 아니거나 제어 대상이 보낸 것이
   아니면 버린다**(메모리 보호).

**대기 시간은 세 단계다(고정 시간 하나로 두지 말 것):** ack는 다른 명령과 같은
15초(`pending`), 그 뒤 **전송 시작까지 `SHOT_START_TIMEOUT=15`초**, 시작된 뒤에는
청크가 흐르는 동안 계속 기다리다 **`SHOT_STALL_TIMEOUT=10`초 멈추면** 포기한다
(`_check_shot_timeout`, 250ms 주기). 사진은 2MB 남짓이라 느린 회선에서 15초를
넘길 수 있는데, 다 받아놓고 잘리는 것이 최악이다. 진행이 있으면 기다리고 멈추면
접는 방식이라 느린 회선과 죽은 전송을 동시에 만족한다.

**캡처는 1920x1080 고정(`capture_game_png`):** 감시 모드와 같은 WGC 경로로 찍고
(가림·포커스에 강함), WGC가 없으면 1080p에서만 pyautogui로 폴백한다. QHD 원본을
그대로 보내지 않는 이유는 ① 매크로가 실제로 보는 그림과 같아 좌표 어긋남을 눈으로
진단할 수 있고 ② 해상도에 따라 전송량이 들쭉날쭉해지지 않기 때문(원본은 PNG가
2배 가까이 커진다).

**표시 창 `ScreenshotWindow`에 matplotlib을 쓰지 않는다(함정):** `domiman.spec`의
`excludes`에 들어 있고, 무엇보다 **⟳ 업데이트는 `domiman.py` 한 파일만 갈아치우므로
exe 사용자는 새 의존성을 받을 수 없다**(재설치 전까지 `ModuleNotFoundError`). 이미
번들에 있는 tkinter와 cv2만으로 같은 일을 한다 — Canvas + `PhotoImage`(base64 PNG)
표시, `[1:1 보기/창에 맞춤]` 토글, `[저장]`, `[클립보드 복사]`(cv2로 BMP 인코딩 후
앞 14바이트를 떼어 CF_DIB — `GlobalAlloc`/`GlobalLock`의 `restype` 지정 필수).
**Pillow에 의존하지 않는다**(easyocr 딸림으로 번들에는 있지만 쓰지 않는다).
'저장'의 `tkinter.filedialog`는 **지연 임포트 + 실패 시 로그 폴더 고정 경로 폴백**
— 그 서브모듈이 없는 옛 번들에서도 창은 뜨고 저장은 된다(스펙에는 추가해 뒀다).

**ChatClient에 이미지 배관이 붙었다(domichat.py에서 복제):** `_raw_send`가 bytes/
튜플 프레임을 받고, `_recv_obj`가 `'B'`를 `{"t":"bin",...}`으로 디코드하며,
`send_chunk`/`_drop_pending_chunks`가 추가됐다. **두 가지는 반드시 지킬 것:**
① `welcome`의 `ver`를 보고 `< 2`면 아예 보내지 않는다(옛 서버는 `'B'`를 받으면
연결을 끊는다). ② 세션이 끊기면 큐에 남은 청크를 **버린다** — 되돌려 넣으면
재접속마다 다시 나가 1초 간격 재접속 고리에 빠진다(domichat.md의 실제 사고).

**모바일에도 들어왔다(260821~, 260828a 커밋에 포함 — 실기기 검증은 아직):**
`domiman_m.py`에 `cmd_screenshot`/`_begin_shot_wait`/`_on_file`(청크 조립·sha256
확인)과 `{"ev":"screenshot"}` 이벤트가 있고, 앱은 `ScreenshotActivity` +
`ui/screenshot/{ScreenshotViewModel,ScreenshotViewer}`로 보여준다(클립보드 복사는
`FileProvider`+`res/xml/file_paths.xml`). **홈 위젯의 '즉시 회수' 칸이 '스크린샷'
칸으로 바뀌었다** — 그 Activity는 `taskAffinity=""`+`singleInstance`라 뒤로 가기를
누르면 앱을 거치지 않고 곧바로 홈으로 돌아간다. 이 부분은 컴파일까지만 확인했고
실제 사진 왕복은 휴대폰에서 눌러봐야 한다.

**검증(260821a, 실제 domiserver 경유 e2e):** 격리 서버(포트 47899, 평문)에 두
클라이언트를 붙여 실제 게임 화면 PNG **2.23MB = 청크 35개**를 왕복시켜, 받은
바이트가 원본과 **완전히 같음**을 확인했다(sha256 불일치는 거르고, 제어 대상이
아닌 발신자의 이미지는 무시). 전송 시작·정지 타임아웃, 축소/1:1 표시, 저장 폴백도
같이 확인했다. 레이아웃은 두 모드를 실제로 띄워 캡처해 눈으로 확인(창 폭 동일).

**프로토콜을 또 고칠 때 쓸 e2e 방법(재사용):** `domiserver.py`를 **다른 폴더로
복사해** 거기서 돌리면 `BASE_DIR`이 그 폴더가 되어 `domiserver.json`/`.db`도 그
폴더 것을 쓴다 — **실서버 DB를 건드리지 않는다.** 그 폴더에
`{"tls": false, "port": 47899}`를 두면 인증서(openssl) 없이 뜨고, domiman의
`connect_secure`가 평문으로 알아서 폴백한다. 가입은 콘솔 승인제라 막히므로
`db_init()` 후 `users` 테이블에 `hash_pw()`로 직접 넣는다. 서버를 띄울 때는
`main()`이 아니라 `accept_loop`만 스레드로 돌린다(`repl()`이 stdin을 읽어 막힌다).
**`PYTHONIOENCODING=utf-8`을 꼭 준다** — 서버 로그의 `—`를 cp949 콘솔에 찍다가
`UnicodeEncodeError`로 죽는다(실제로 겪음). 클라이언트 쪽은 `DomimanApp`을 통째로
띄우지 않고 `_on_file`·`_begin_shot_wait` 같은 메서드만 껍데기 객체에 붙여 쓰면
GUI 없이 수신 경로를 그대로 검증할 수 있다.

---

## 살림망 수량 상시 방송 (260828a, 구현됨 — PC 발신 + 모바일 위젯 수신)

예전에는 제어 측이 `N`으로 **물어볼 때만** 수량이 건너갔다. 그래서 휴대폰·제어
PC는 버튼을 누른 순간의 값만 볼 수 있었다. 감시 루프는 어차피 사이클마다 수량을
읽으므로, **읽은 값을 그대로 흘려보내면** 받는 쪽은 요청 왕복 없이 현재 수량을
계속 알 수 있다(폴링이 아니라 밀어내기).

**발신(`note_tank_qty`)** — 수량을 읽는 두 곳이 이 함수 하나로 모인다:
감시 루프(`_watch_loop`, 사이클마다)와 실시간 수량확인(`_tank_check_and_resume`).
하는 일은 ① `_last_tank` 기록 ② `,Z,N,cur,mx` (판독 실패면 `,Z,N,fail`) 방송.
- **초과 상태(`cur > mx`)도 그대로 보낸다** — 파싱이 초과를 정상 값으로 취급하는
  것과 같은 이유다(read_tank_quantity 절 참고). 받는 쪽이 걸러선 안 된다.
- **`chat_send(..., quiet=True)`로 조용히 보낸다(중요):** 사이클마다 `[발신]`
  한 줄이 찍히면 로그가 그것만으로 가득 찬다. `quiet`는 이 용도로 추가된
  인자이며, 몇 초에 한 번이 아닌 발신에만 쓴다(명령·응답·보고는 그대로 찍는다).
- 서버는 메시지를 **저장하지 않고 방에 뿌리기만** 하므로(domiserver에 msgs
  테이블이 없다) 사이클마다 보내도 쌓이는 것이 없다. 로그인 전이면 `chat_send`가
  그대로 무시하므로 큐에 고이지도 않는다.

**PC는 보내는 것까지만 한다(사용자 확정):** '항상 갱신되는 수량 표시'는
**모바일 위젯**의 몫이다. 그래서 domiman.py 쪽에는 전용 표시 위젯을 두지 않았고
(창 크기를 건드리지 않으려는 이유도 있다 — 체크박스↔스크린샷 전례),
받는 쪽은 **상태가 바뀔 때만 로그 한 줄**을 남긴다(`_handle_remote_tank`):
연결 직후 첫 값 · 판독 실패 ↔ 회복. 값이 오를 때마다 찍으면 로그가 그것만으로
가득 차기 때문이다. 지금 값을 확인하려면 예전처럼 '실시간 수량확인'(N)을 쓴다.
- 방송은 **`pending`(응답 대기)을 건드리지 않는다** — 명령 응답을 기다리는 중에
  방송이 끼어들어도 그 대기를 깨면 안 된다.
- `N` 응답을 받은 직후에는 `remote_tank`를 그 값으로 맞춰 둔다. 안 그러면 뒤이어
  올 방송이 같은 상태를 한 번 더 찍는다.
- 제어 대상이 아닌 발신자의 방송은 버린다(보고 처리와 같은 규칙).

### 모바일 수신 (260828a, 구현됨) — 위젯이 상시로 갱신된다

**요구는 두 개였고 서로 당긴다:** 위젯 수량은 항상 최신이어야 하고, 백그라운드는
"필요 이상의 리소스를 먹지 않고 적절한 선에서 살림망 변화만 감지"해야 한다.
그래서 **값은 오는 대로 받고, 그리는 일만 묶는다.**

`domiman_m.py`:
- `dispatch()`가 무명 방송 `,Z,N,*`를 `("tank", 필드)`로 분류한다. **`reply`가
  아니다** — `_on_msg`의 `resolve_pending()`은 `reply`에만 걸려 있으므로, 명령
  응답을 기다리는 중에 방송이 끼어들어도 그 대기를 풀지 않는다(끼어들어 풀면
  예약종료 창 같은 UI가 잘못 열린다).
- `dispatch_result`가 `{"ev":"tank","tank":[cur,mx]|null,"tank_fail":bool}`을
  만든다. **알림 키·상태문구를 붙이지 않는다** — 사이클마다 오는 신호에 알림을
  달면 알림창이 그것만으로 찬다.
- N 응답과 방송이 **같은 파서**(`parse_tank_fields`)를 쓴다. 두 경로가 어긋나면
  '새로고침으로 본 값'과 '위젯이 보여주는 값'이 달라진다.

Kotlin(`DomimanRepository`):
- `handleEvent`는 `ev == "tank"`를 **맨 앞에서 가로채 끝낸다** — 알림·상태·
  `_events`(화면) 흐름에 태우지 않는다. 태우면 8줄짜리 모바일 로그가 수량으로
  도배되고(`MainScreenViewModel`이 `tank`를 로그에 찍는다) UI도 헛돈다.
- `onTankBroadcast`가 비용을 묶는다: ① 값이 같으면 즉시 반환(prefs·RemoteViews
  둘 다 안 건드림) ② `DomimanWidgetProvider.hasInstances()`가 false면(홈에 위젯이
  없으면) 아무 것도 하지 않음 ③ 값이 바뀌어도 재그림은
  **`TANK_WIDGET_MIN_INTERVAL_MS = 30초`에 한 번**. 묶는 동안 들어온 값은
  `pendingTankText`에 덮어써 두고 창이 열리면 마지막 값 하나만 그린다 —
  **지연될 뿐 버려지지 않는다.** 살림망은 몇 시간에 걸쳐 차는 값이라 초 단위
  재그림은 의미가 없다.
- **N 응답은 즉시 그린다**(사람이 새로고침을 눌러 기다리고 있으므로). 방송만
  묶는다 — 둘의 차이는 '사람이 기다리는가'다.
- 로그아웃·세션 정리에서 `cancelTankFlush()`로 대기 중인 재그림을 접는다(늦게
  깨어나 죽은 세션의 값을 그리지 않게).

**PC는 매 사이클 보내고 조이지 않는다:** 휴대폰 쪽 소켓은 이미 열려 있어(포그라운드
서비스) 작은 텍스트 한 줄을 더 받는 비용은 사실상 없고, 값 판독은 어차피 사이클마다
하고 있다. 비싼 쪽은 **위젯 재그림·prefs 쓰기**이므로 상한은 거기에 뒀다. 트래픽까지
줄여야 하면 발신부(`note_tank_qty`)에 '값이 같으면 건너뛰기'를 넣으면 되지만, 그러면
휴대폰이 늦게 붙었을 때 다음 변화까지 옛 값을 들고 있게 된다(그래서 안 넣었다).

**검증(260828a):** 발신 규격(성공·실패·초과·quiet·로그인 전 무발신)과 수신
처리(첫 값 로그 → 값만 오를 때 침묵 → 실패 전환 로그 → 실패 연속 침묵 → 회복
로그 → pending 불변 → 타 발신자 무시 → 로컬 모드 무시 → N 응답 뒤 중복 침묵)를
GUI 없이 메서드만 붙인 껍데기 객체로 전부 확인했다. 창 크기가 바뀌지 않는지도
개량 전/후 GUI를 실제로 만들어 `winfo_reqwidth/reqheight`로 재봤다(595x399 동일).

---

## ⚠️ 260911a — 수량 오탐의 원인은 **배율 하나에 의존한 것**이었다 (실측, 해결됨)

`714/470`으로 읽혀 회수 루틴이 헛돈다는 보고를 받고 설치본 `ocr_dump/`를 열었다.
**엔진 문제가 아니었다.**

**증거 ① 오탐 크롭 8장이 전부 같은 값이었다.** `tank_overflow_*` 8장을 눈으로
보니 **전부 `114/470`**(한 장만 `114/420`)이고 전부 `714/470`으로 읽혔다.
발생 시각 간격이 **정확히 83분**(18:44 → 20:07 → 21:30 → 22:53 → 00:16) —
살림망이 114를 지나는 사이클마다 어김없이 재현됐다는 뜻이다. 즉 프레임이 튄
것이 아니라 **그 글자 배열에 대한 결정적 오인식**이다.

**→ 그래서 기존 `retries=4` 프레임 재시도는 원리적으로 무력하다.** 같은 전처리로
몇 번을 다시 읽어도 같은 답이 나온다. 게다가 `read_tank_quantity`는 최대치가
직전과 같으면 **첫 프레임에서 즉시 반환**하고 있어 다수결이 아예 돌지도 않았다.

**증거 ② 갈림은 전처리 배율에서 났다.** 그 8장을 12가지로 재보니:

| 전처리 | 정확 |
|---|---|
| 원본+컬러 · **원본+흑백(당시 주 경로)** | **0/8** |
| x2·x3·x4 (흑백/컬러/최근접/오츠/반전이진 전부) | **8/8** |

260822c의 `112/570`은 **정반대**였다(x3가 틀리고 원본이 맞았다). 즉 **어느 한
배율도 혼자서는 안전하지 않다** — 이 문서가 260822a에 이미 적어 둔 결론인데,
정작 수량 경로는 '원본 먼저, 정규식 실패 시에만 x3'라 **한 배율의 값을 그대로
내보내고 있었다**. `714/470`은 정규식이 맞으므로 폴백이 영원히 발동하지 않는다.

### 해결 = 서로 다른 배율로 읽어 **값이 겹칠 때만** 받는다 (`_read_tank_once`)

    원본 -> x3 : 두 값이 같으면 채택            (정상 경로, OCR 2회 0.28초)
    다르면 x2 추가 -> 2표 이상 모인 값 채택      (0.40초)
    2표를 못 모으면 None (그 사이클은 버리고 다음에 다시 읽는다)

- 오탐 8장 **8/8 정답**(실제 코드 경로 `read_tank_quantity`로 재확인, 0.4초).
- 라이브 5회 연속 정상(0.30초/회). 폴링 간격이 최소 획득 시간(≥5초)이라 부담 없음.
- 판독 실패 크롭 80장에서는 합의가 **0/80**인데 이건 손해가 아니다 — 원본 단독은
  그중 17장에서 값을 내지만 그 값들이 `44/330`·`4/1470`·`44/470` 같은 **오인식**
  이고, 합의는 그것들을 거부한다. **'틀린 값'보다 '값 없음'이 안전하다** —
  감시 루프는 직전 폴링 간격을 유지하고 다음 사이클에 다시 읽으면 그만이다.

**프레임 재시도와 배율 합의는 역할이 다르다(헷갈리지 말 것):** 프레임 재시도는
갱신 애니메이션·깜빡임을 건너뛰는 장치이고, **오인식을 거르는 것은 배율 합의**다.
결정적 오인식은 프레임을 바꿔도 그대로 나오므로 재시도로는 못 잡는다.

**엔진(OCR 모델) 교체는 필요 없다는 결론:** 같은 easyocr이 확대 계열 전처리
10가지 전부에서 8/8로 맞힌다. 실패는 (엔진, 배율) 조합 하나에서만 났다. 고정 폰트
숫자 템플릿 매칭은 여전히 '언젠가의 더 나은 답'이지만(속도도 빠르다), 이 오탐을
없애려고 지금 도입할 이유는 없다.

**아직 넣지 않은 두 번째 방어선(제안):** 살림망 수량은 사이클당 최대 1 증가하는
계수기이므로, 직전 값 대비 증가량이 경과 시간으로 설명되지 않으면(예: 114 -> 714)
**오인식으로 보고 한 번 더 읽는** 시간적 타당성 검사를 둘 수 있다. 값 범위 가드
(`cur<=mx` 류, 절대 금지)와 달리 '버리는' 것이 아니라 '재확인'이라 영구 정체를
만들지 않는다. 지금은 배율 합의만으로 알려진 실패가 전부 막히므로 보류했다.

---

## 취침 모드 = 클릭이 통과하는 검은 오버레이 (260909b, 구현됨)

화면을 검게 덮은 채로 **낚시·회수 루틴이 그대로 돌게** 하는 기능. GUI의
**'로그 내보내기' 버튼 자리를 '취침 모드'로 대체**했고(내보내기 기능은 완전히
제거, `로그 저장` 체크박스는 별개 기능이라 유지), 직접 제어 모드는 **이 PC**를,
원격 제어 모드는 **피제어 PC**를 암막으로 만든다(새 명령 `B`).

**화면 보호기로는 안 된다(설계 제약):** 보호기는 어떤 입력이든 들어오면 스스로
종료하도록 만들어져 있고, 매크로의 입력은 `SetCursorPos`+`mouse_event`/
`keybd_event`로 **드라이버 수준에 주입돼 사람 입력과 구분되지 않는다.** 그래서
'보호기를 유지한 채 클릭'은 원리적으로 불가능하다. 근본 해결(입력을 안 보내고
`PostMessage`로 창에 메시지만 던지기)은 게임이 무시하므로 검토에서 제외했다.
→ 대신 **입력에 깨지지 않는 암막**을 우리가 그린다.

### `BlackoutOverlay` — 실측으로 확정된 3가지, 하나라도 어기면 깨진다

1. **HWND는 `GetAncestor(winfo_id(), GA_ROOT)`로 얻는다.** `GetParent`는 최상위
   창에서 부모가 아니라 **소유자**(앱 root)를 돌려준다. 그것에 스타일을 붙이면
   정작 검은 창은 그대로 남아 **"보이지도 않으면서 클릭만 삼키는 창"** 이 된다
   — 실제로 이 버그로 화면은 안 검은데 사용자·매크로 클릭이 **둘 다 먹혔다**
   (약 90초간 마우스가 안 먹혀 사용자가 "클릭 작용이 되지 않음"을 보고했다).
   실측: `winfo_id`=`TkChild`, `GA_ROOT`=`TkTopLevel`.
2. **`WS_EX_LAYERED`는 tkinter의 `attributes("-alpha", 0.999)`가 붙이게 한다.**
   `SetWindowLong`으로 직접 붙이면 레이어 표면이 비어 **창이 보이지 않는다**
   (레이어드 창은 표면을 채워줘야 그려진다). 나머지
   `WS_EX_TRANSPARENT|NOACTIVATE|TOOLWINDOW`만 SetWindowLong으로 추가한다.
   검증된 최종 exstyle = `0x080800A8`.
3. **`SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE=0x11)`를 반드시
   건다 — 1080p 대응의 핵심.** 안 걸면 **화면 캡처에 이 검은 창이 잡혀 1080p
   모드의 퀴즈 인식이 전멸한다**(1080p의 `grab_region_rgb`는 pyautogui = 화면
   캡처). 실측: 미적용이면 pyautogui가 검은픽셀 **100%**, 적용하면 **0%**
   (화면에는 그대로 검게 보인다). WGC(1440p·감시 모드)는 창을 직접 캡처하므로
   원래 무관하다. 실패하면 경고를 찍는다.

`_reassert()`가 0.25초마다 z순서를 다시 주장한다 — `bring_game_to_front`가
게임을 앞으로 불러도 암막이 위에 남게. 클릭은 `WS_EX_TRANSPARENT` 덕에 그대로
게임으로 통과한다(`WindowFromPoint`가 게임 창을 돌려주는 것으로 확인).

**4. `HWND_TOPMOST` 재적용으로는 안 된다 — `HWND_TOP`이어야 한다(260911a, 실제
증상으로 고침):** 게임은 포그라운드가 되면 **스스로 `WS_EX_TOPMOST`가 된다**
(실측: 호출 전 False → 후 True). 그러면 암막과 게임이 같은 최상위 무리에 들어가는데
**이미 최상위인 창에 `HWND_TOPMOST`를 다시 걸어도 무리 안 순서는 바뀌지 않는다.**
그래서 "취침 모드가 처음엔 되는데 루틴을 한 번 돌고 나면 **암막 위에 게임창만
덩그러니** 뜬다"는 증상이 났다(실측 z: 오버레이 3 / 게임 2). `HWND_TOP`은 최상위
무리의 **맨 위로** 올려주므로 복구된다(실측 z 2/3으로 역전). `NOTOPMOST →
TOPMOST` 토글도 되지만 한 순간 최상위를 내려놓아 깜빡일 수 있어 쓰지 않는다.
위치·크기는 `SWP_NOMOVE|SWP_NOSIZE`로 건드리지 않는다(배치는 `_place`가 한 번만).

### 해제 조건

| 조건 | 구현 |
|---|---|
| **실제** 키보드/마우스/터치 | `WH_KEYBOARD_LL`+`WH_MOUSE_LL` 훅에서 `INJECTED` 플래그가 **없는** 이벤트 |
| 매크로 자기 입력은 무시 | 플래그 + `_mark_own_input()` 시간 창(0.15초) 이중 방어. `click_real`·`press_key`가 표시 |
| 매크로 중지 | `_stop_fishing` |
| 원격 제어 | `G/P/Y/W/Q/V/T/C` 수신 시(`_handle_command` 앞부분). **`S`·`N`·`I`·`B`는 유지** — 상태/수량 질의·스크린샷은 읽기 성격이라 무인 상태에서 계속 어두워야 한다 |
| 프로그램 종료 | `on_exit` (검은 화면을 남긴 채 끝내지 않는다) |
| 훅 설치 실패 | `GetLastInputInfo` 폴링으로 폴백 — **해제 수단이 아예 없어져 사용자가 검은 화면에 갇히는 것**이 최악이므로 폴백을 반드시 남긴다 |

**`_mark_own_input`의 창을 늘리지 말 것(0.15초):** 훅은 입력을 주입하는 순간
동기로 불리므로 창이 길 필요가 없다. 반대로 길게 잡으면 루틴이 도는 동안
(클릭 간격이 0.5초라) 창이 거의 항상 열려 있어 **사용자가 급히 키를 눌러도
암막이 안 풀린다** — 해제 수단이 사실상 사라진다.

**저수준 훅 콜백의 함정:** `CallNextHookEx`의 `argtypes`/`restype`를 지정하지
않으면 64비트에서 `OverflowError`가 나 콜백마다 예외가 터진다(로그 242KB를
찍은 적 있다). 훅 콜백은 짧아야 하고(300ms 넘으면 Windows가 훅을 떼어낸다)
`self.seen` 계수만 올린 뒤 해제는 `root.after(0, …)`로 **메인 스레드에 넘긴다**
(tkinter는 메인 스레드 전용).

### 프로토콜: `B` (취침 모드)

- 명령 `(대상),B` → 응답 `(요청자),Z,B`(에코, G/P/Y/W/Q와 같은 계열).
- 피제어 측은 ack를 보낸 뒤 `start_blackout("원격 요청")`. 암막은 tkinter 창이라
  **메인 스레드**에서 만들어야 하는데 `_handle_command` 자체가 메인 스레드다.
- 제어 측 버튼은 로컬에선 상시, 원격에선 응답 대기 중에만 봉인(`_apply_ui_locks`
  의 `always_ok` 그룹 = 예약종료·즉시회수·수량확인·취침모드·종료).

### 모바일(안드로이드)에서도 보낼 수 있다 (260915a, 구현됨)

PC판에만 있고 앱에는 없어서 폰으로는 취침 모드를 걸 수 없었다. 같은 `B` 명령을
앱에서도 보내도록 붙였다 — **프로토콜은 한 글자도 바뀌지 않았다.**

| 층 | 바뀐 것 |
|---|---|
| `domiman_m.py` | `cmd_blackout()` = `send_command("B", "B")` |
| `domiman_m.py` | `dispatch_result`의 에코 목록에 `"B"` 추가(`G/P/W/Q/Y/B`) — 안 넣으면 응답이 `parse_status`로 흘러가 조용히 버려지고 pending이 15초 동안 안 풀린다 |
| `DomimanRepository.kt` | `sendBlackout()` = `cmd("cmd_blackout")` |
| `MainScreenViewModel.kt` | `onBlackout()`(markPending) + 에코 `"B"` 로그 |
| `MainScreen.kt` | '실시간 수량확인' 줄 아래 새 줄에 **'취침 모드'** 버튼(반폭). `controlsEnabled` |

**'해제' 버튼은 일부러 두지 않았다.** 암막은 피제어 PC 앞에서 **사람이 직접
입력**해야 풀린다(주입 입력으로는 안 풀리는 것이 이 기능의 요점이다 — 위
검증 참고). 원격에서 푸는 길을 만들면 그 성질이 깨진다. 다만 다른 원격 명령을
보내면 피제어 측이 `stop_blackout(f"원격 명령 {cmd}")`로 먼저 걷는다.

로그 문구는 "취침 모드로 전환했습니다. 화면만 덮이고 낚시는 계속됩니다." —
낚시가 멈춘 것으로 오해하지 않게 한 줄에 같이 적는다.

### 검증 (260909b)

- **암막 유지한 채 수거 루틴 1회 완주** — 11초, 퀴즈 1회 통과, 살림망
  `128/470 → 0/470`, 낚시 재개까지 확인. 그동안 화면은 **검은픽셀 100%**
  (2초마다 4회 검사, 최저 100%). 합성 클릭 14건·키 16건이 전부 통과했다.
- `WDA_EXCLUDEFROMCAPTURE`: 미적용 pyautogui 100% → 적용 0% → 되돌리면 100%.
- 주입 입력으로는 해제되지 않음(마우스 ±3px 이동 10건 + 키 6건, 전부 `INJECTED`).
- 해제 배관: 다른 스레드에서 `_request_release` → 메인 스레드 `release` → 콜백
  → 창 파괴까지 **0.2초**. 가드 창은 0.15초에 정확히 만료.
- **창 크기 불변**: 같은 PC에서 수정 전(HEAD)과 후를 각각 띄워 재보니
  **758x495 → 758x495**로 동일했다. 문서 다른 곳의 `595x399`는 **DPI가 다른
  환경에서 잰 값**이라 절대값 비교는 무의미하다 — 반드시 **같은 PC에서 수정
  전/후**를 재야 한다(HEAD 사본을 임시 폴더에 풀어 `winfo_reqwidth/reqheight`).
- 남은 것: **실제 사람 입력으로 해제되는 순간**은 아직 확인하지 못했다(계수기
  `key_real`/`ms_real`가 0인 채로 대기 시간이 지났다 = 그 시간에 입력이 없었다).
  배관·플래그 판정은 전부 확인됐으므로 남은 것은 비트 하나지만, 실사용에서 한 번
  확인할 것.

---

## (참고) 옛 ntfy 원격 제어 프로토콜 — 메시지 규격만 유효

같은 ntfy 채널의 domiman끼리 서로 제어한다. 발신자 식별은 ntfy Title
(=발신 PC 이름, 영문+숫자, **채널 내 중복 금지**). 대소문자 구분,
규격 외 메시지는 무시. 이름/채널/PC 리스트는 `domiman_config.json`
(exe/스크립트 옆)에 보존, 없으면 기본값(호스트명 규칙, domi_fishing_9714).

**'ntfy 메시지' 체크박스는 기본 켜짐(260809b부터).** 켜짐/꺼짐은 설정
파일에 보존하지 않으므로 매 실행 이 기본값에서 출발한다. 기본값은
`self.var_ntfy = tk.BooleanVar(value=True)` 한 곳에만 있고, 전역
`_ntfy_enabled`는 `__init__` 끝에서 `on_ntfy_toggle()`을 그대로 한 번 불러
거기서 맞춰간다(둘을 따로 True로 적어 두면 어긋나기 쉬움 — 체크박스만
켜지고 실제 수/발신은 꺼져 있는 상태가 됨). 스트림 스레드는 0.5초 주기로
`_ntfy_enabled`를 다시 보므로 스레드를 먼저 띄워도 곧바로 구독을 시작한다.

**메시지 규격:**
- 명령: `(대상PC),(명령)[,인자]` — S(상태질의) G(시작) P(중지) Y(예약확인)
  Y,n(예약 n분, 0=해제) W(즉시회수) Q(종료) V,a|1080|1440(해상도)
  T,n(타이머) C,로그[,낚싯대,미끼](체크박스, t/f) N(실시간 수량질의)
  I(스크린샷 — 260821a 추가, 아래 전용 절 참고)
  B(취침 모드 = 암막 — 260909b 추가, 위 전용 절 참고)
- 응답: `(요청자),Z,...` — 요청자 무명(휴대폰)이면 `,Z,...`.
  G/P/Y/W/Q/**B**는 에코, S/V/T/C는 상태 응답
  `,Z,(타이머),(1080|1440),(a|m),(로그t/f),(실행중t/f)[,(낚싯대),(미끼)]`
  (낚싯대/미끼 필드는 타이머=0(감시 모드)일 때만. 실행중은 감시모드 여부와
  무관하게 항상 5번째 고정 위치 — 낚싯대/미끼처럼 있다 없다 하면 자리가
  밀려 파싱이 꼬이기 때문).
- 수량 응답(N): `,Z,N,(cur),(mx)` 또는 `,Z,N,fail`. **항상** 창을 앞으로 불러
  3초 뒤 파싱해 응답한다(실패면 fail). OCR/해상도 미준비도 fail. 단일 왕복
  (W처럼 ack+보고로 안 나눔), 최대 ~5초. 로컬 버튼은 로그로만 출력.
  ※ "마지막 파싱값(`_last_tank`)이 있으면 즉시 응답" 하던 캐시 우선 방식은
  260728b에서 폐기됐다(아래 `_tank_check_and_resume` 항목 참고) — 이 줄은
  그 서술이 남아 있던 것을 바로잡은 것.
- 스크린샷 응답(I): `,Z,I`(ack — 지금 찍는다) 또는 `,Z,I,fail`(캡처·전송 불가).
  사진 자체는 메시지가 아니라 **방에 올라오는 이미지**로 온다(아래 절).
- **수량 방송(무명 브로드캐스트, 260828a 추가): `,Z,N,(cur),(mx)` /
  `,Z,N,fail`** — 피제어 PC가 **수량을 읽을 때마다**(감시 사이클마다 =
  최소 획득 시간마다) 요청 없이 자기 방으로 흘려보낸다. 요청자 자리가 비어
  있어 위의 N **응답**과 구분된다(아래 전용 절 참고).
- 보고(무명 브로드캐스트): `,Z,F,코드` — s(루틴시작) g(회수성공) f(회수실패)
  rs/bs(낚싯대/미끼 교체 **시작**) y,r/y,b(낚싯대/미끼 교체 성공)
  x,d/x,r/x,b(튕김/낚싯대/미끼 교체 실패).
  **교체 실패 = 대상 미감지로 좌상단 폴백 사용**(낚싯대도 폴백 있음).
  기존 영어 알림은 전부 이 코드로 대체됨. rs/bs는 260728a에서 추가(모바일
  '낚싯대/미끼 교체 시작 시 알림' 옵션이 받을 신호 — 교체 루틴 진입 즉시 발신).

**제어 측(dB) 동작:** 최상단 제어PC 버튼(로컬 낚시 실행 중엔 봉인)으로
대상 선택 → S 질의로 상태 동기화(15초 무응답이면 **로컬 복귀**).
명령 발송 후 응답까지 제어PC변경·로그·다크모드 외 전부 봉인, 15초
무응답 시 '응답이 없습니다' 로그 + '의식을 잃었습니다' 5초 표시.
시작/중지는 대기 중 회색 '대기' 표시, **응답 내용 우선**(G 보냈는데
Z,P 오면 시작 상태로). 예약 종료는 2단계(Y→Z,Y 후 창, Y,n→Z,Y,n에
창 닫힘, 취소/무응답이면 Y,0 발송). 타이머는 입력 정지 3초 후 T 발송.
Q 응답 수신 시 자동 로컬 복귀. 원격 중 이름/채널/ntfy 체크박스 봉인
(ntfy는 강제 on). **원격 제어 중 자기 앞으로 온 명령은 전부 무시.**
보고(,Z,F,*)는 제어 중인 PC의 것만 로그에 변환 표기.

**피제어 측(d3) 동작:** ntfy 체크박스 on + 로컬 모드일 때만 자기 이름
앞 명령을 처리·응답. 낚시 실행 중엔 V/T/C를 적용하지 않고 현재 상태만
응답(로컬 봉인 규칙과 동일). W는 수신 즉시 ack 후 3초 카운트다운 루틴.
Q는 응답을 동기 발신 후 종료. 상태 응답에 실행 중 여부(`self._running()`)
필드가 포함돼(구현됨), dB가 접속하면 S 질의 응답 한 번으로 시작/중지
버튼이 바로 실제 상태에 맞게 뜬다(과거엔 이 필드가 없어 일단 '시작'으로
표시됐다가 버튼을 눌러야만 교정됐음 — `_apply_remote_status`가
`remote_running_shown`을 갱신하고 `_set_start_button_remote()`를 재호출).

## ntfy 수신 = 스트리밍 구독 (폴링 아님, rate-limit 함정 주의)
수신은 **전용 데몬 스레드**(`_ntfy_stream_loop`)가 `GET {채널}/json`을 **연결
하나로 계속 열어두고**(스트리밍) 도착 메시지를 실시간으로 `_ntfy_queue`에 넣는다.
메인 스레드는 `_poll_ntfy_queue`(250ms `after`)로 **큐만 비워** `_dispatch_ntfy`로
넘긴다(tkinter는 메인 스레드에서만 조작 → 큐 경유 필수).

**왜 폴링을 버리고 스트리밍으로 갔나 (중요):** ntfy.sh 무료 한도는 **GET/POST가
같은 IP 버킷을 공유**하고(visitor=IP), 버스트 60 + **5~10초당 1개 충전**뿐이다.
과거 5초 폴링(반복 GET)은 이 충전 속도를 거의 다 먹어, **같은 공유기(같은 공인
IP)를 쓰는 휴대폰 앱의 발신(POST)이 429로 실패**했다. 스트리밍은 요청 토큰을
**연결당 1회만** 소비(그 뒤 도착 메시지는 요청 수에 안 잡힘, 동시연결 한도
`visitor-subscription-limit` 기본 30만 적용)하므로 한도에 여유가 크고 수신도
사실상 즉시다. → **폴링 주기를 줄이는 방향(2~3초)은 오히려 한도를 더 태우니 금지.**

**설계 원칙:** 전송 계층만 폴링→스트리밍으로 교체했고 프로토콜 처리
(`_dispatch_ntfy`/`_handle_command`/`_handle_remote_reply`)는 **그대로**다.
- `since` 미사용: 살아있는 연결은 메시지를 정확히 한 번만 전달 → 재연결 시
  과거 명령 **중복 실행 위험 없음**. 재연결 공백에 놓친 명령은 15초 타임아웃 후
  재시도(중복 실행보다 깔끔한 실패-재시도가 안전).
- 자기 발신분(Title=PC_NAME)은 스트림 스레드에서 걸러냄(ntfy는 발신자에게도 echo).
- `iter_lines`는 블로킹이라, ntfy 비활성화/채널 변경 시 반응이 keepalive(~45s)
  주기만큼 늦다 → `ntfy_stream_disconnect()`(연결 강제 close)로 즉시 깨운다.
  토글 off·종료 시 직접 호출, 채널 변경은 1.5초 디바운스 후 재연결.
- 발신 `ntfy_send`는 기존과 동일(POST, 기본 비동기 스레드).

## 발신 실패는 조용하다 — 상태코드를 반드시 볼 것 (260814a, 최대 함정)

**`requests.post`는 429·4xx·5xx에 예외를 던지지 않는다.** `status_code`를 보지
않으면 **서버가 버린 메시지를 `[ntfy 발신]` 성공으로 로그에 찍는다.** 이 한 줄
때문에 다음 증상을 오래 헤맸다(진단 기록):

- TR1이 원격 `S` 질의를 **정상 수신**해 `[원격 명령] TR1,S`를 찍고 응답을
  `[ntfy 발신] BGOD,Z,...`로 찍는데, 제어 PC(BGOD)는 15초 무응답으로 로컬 복귀.
- TR2·GOD3는 같은 채널에서 **0.8초 내 정상 응답.** "TR1이라는 문자열 문제인가"로
  오인하기 쉬웠다.
- 원인: TR1의 POST가 `{"code":42908,"error":"daily message quota reached"}`로
  **전부 거부**되고 있었다. ntfy.sh 무료 발신 한도는 **egress IP당 12시간 250건**
  (`GET /v1/account` 의 `"basis":"ip"`, `messages_expiry_duration:43200`).

**진단법(재사용):** 채널을 직접 구독하지 말고 **서버 캐시를 소급 조회**하는 게
가장 강력하다 — `GET https://ntfy.sh/{채널}/json?poll=1&since=12h`는 **발행에
성공한 메시지만** 12시간분 돌려주므로, "그 PC가 정말 보냈는가"를 사후에 판정할
수 있다(실측: TR1은 12시간 0건, 같은 시간 TR2는 13건). PC 쪽 상태코드는
`Invoke-WebRequest ... -Method POST` + **try/catch**로 확인한다(PowerShell 5.1은
4xx에서 예외를 던져 `$r.StatusCode`가 안 찍힘).

**같은 랜인데 한 PC만 막히는 이유:** ntfy 방문자 버킷은 **나가는 IP 단위**라,
같은 공유기라도 한 PC가 IPv6로, 다른 PC가 IPv4로 나가면 **한도가 분리된다**
(또는 CGNAT/VPN이면 모르는 남과 버킷을 공유해 남이 태울 수도 있다). 사양·랜이
동일해도 증상이 갈리므로 "환경이 같으니 한도 문제일 리 없다"는 추론은 틀린다.

**코드 대응(`ntfy_send`):** 상태코드 확인 → 실패면 HTTP 코드와 서버 사유를 그대로
로그(+429면 `_ntfy_quota_text()`로 남은 한도까지). 일시적 실패(네트워크 예외·5xx·
순간 한도 42901)는 `NTFY_SEND_DEADLINE=8.0`초 예산 안에서 재시도(요청자의 15초
응답 대기 안에 끝나야 하므로 이 예산을 늘리지 말 것). **일일 한도(42908)만은
재시도하지 않고 `NTFY_QUOTA_COOLDOWN=900`초 발신을 멈춘다** — 회복되지 않는 실패에
요청을 더 던지면 같은 버킷의 요청 토큰만 태워 상황을 악화시킨다. 냉각 중 발신은
`[ntfy 발신 보류]`로 남기고 서버를 때리지 않으며, 이후 발신이 성공하면
`[ntfy] 발신 한도가 회복되었습니다.`를 한 번 찍고 냉각을 푼다.

**모바일(`domiman_m.py`, Kotlin 포팅 레퍼런스):** `DomimanClient.stream()`이 권장
경로(연결 하나 유지). `poll()`은 폴백으로만 남김. Kotlin은 OkHttp 스트리밍
응답 줄읽기 또는 `/sse`+EventSource로 동일 구현하고 dispatch/parse는 재사용.
`parse_status`는 PC와 동일하게 `실행중` 필드(4번째 뒤, 감시모드 무관 고정
위치)를 포함하도록 갱신됨 — PC 프로토콜과 항상 같이 맞출 것.

**모바일 UI 전면 재설계(구현됨, `앱UI설명.xlsx` 기준 — Sheet1=로그인,
Sheet2=메인 제어, A-E열=대략 레이아웃/G열=상세설명):**
- **화면 3개**: 로그인(`Screen.LOGIN`) / 최근 로그인 목록(`Screen.RECENT_LOGINS`)
  / 메인 제어(`Screen.MAIN`, PC GUI와 거의 동일 — 이 화면 버튼들은 기존
  `DomimanClient.cmd_*`를 그대로 호출하면 되므로 domiman_m.py에서 다시
  감싸지 않음).
- **로그인/최근 로그인 규칙(`LoginStore`+`LoginFlow`)**: 같은
  (id,피제어PC,채널)로 재로그인하면 기존 행을 갱신 후 맨 위로(중복 없음,
  개수 상한 없음 — 사용자 확정). 로그아웃(Sheet2에서 뒤로가기 2번)은
  자동로그인 '무장'만 해제 — 최근 로그인 목록과 로그인 폼에 채워진 마지막
  값은 그대로 유지, 이 상태로 앱을 재시작해도 자동로그인은 발동하지
  않는다(로그아웃 ≠ 데이터 삭제). '최근 로그인' 저장은 캐시/앱데이터 삭제
  시 함께 지워지는 저장소 사용(계정 백업 등 더 오래 남는 저장소 금지).
- **업데이트 버튼 안 씀(함정 — Android는 PC처럼 무음 자동교체 불가):** 새
  APK를 받아와도 OS가 강제하는 '설치' 확인 탭이 매번 1회 필요해 PC의
  `os.replace` 방식과 동일한 완전 자동은 안 된다. 사용자가 이 제약을
  감수하지 않기로 확정 — Sheet2 G13 자리는 항상 '실시간 수량확인'
  버튼(`cmd_tank_query()` 연결)이고, G18의 풀투리프레시 제스처도
  구현하지 않는다.
- 로그 창은 PC(접기 가능)와 달리 항상 펼쳐진 상태로 마지막 8줄만
  유지(`MobileLogBuffer`).

---

## domiman_m.py — 모바일용 Python 모듈 (구조·API)

**정체:** 안드로이드 앱에 Chaquopy로 내장되는 Python 모듈이자 Kotlin 포팅
레퍼런스. 737행 5개 섹션. `domiman.py`에서 **데스크톱 전용 코드(tkinter,
pyautogui/win32 클릭, WGC 캡처, OCR, 낚시 자동화 루틴)를 전부 뺀** 것으로,
① ntfy 프로토콜 ② 로그인/최근로그인 저장 규칙 ③ 화면 상태·전환 규칙만 담는다.

**'제어(dB)' 역할만 맡는다(중요):** 자기 앞으로 온 명령을 받아 실행하는
**피제어(d3) 로직(`domiman.py`의 `_handle_command`)은 아예 없다.** 휴대폰은
PC를 조종하는 쪽이지 조종당하는 쪽이 아니므로 의도된 비대칭이다.

**의존성 제약(깨지 말 것):** `requests` + 표준 라이브러리(`json/re/time/
threading/queue/dataclasses/enum/collections`)만 쓴다. Chaquopy로 APK에 넣기
때문에 플랫폼 의존 패키지를 넣으면 빌드가 막힌다. 위젯 렌더링·화면 전환
애니메이션·뒤로가기 인터셉트·Toast·알림·다크모드 감지·실제 영속 저장소는
**전부 Kotlin 책임**이고, 이 모듈은 "무엇을 언제 어떻게 바꿀지" 규칙만 갖는다.

### 섹션·API 지도

| 섹션 | 내용 |
|---|---|
| 상단 (모듈 전역) | `is_valid_id`/`is_valid_channel`(PC와 같은 정규식), `STATUS_TEXT`·`REPORT_TEXT`·`REPORT_STATUS`(domiman.py에서 **그대로 복제**), `NOTIFY_KEYS`+`notify_key_for_report` |
| `[1]` 프로토콜 | `DomimanClient` — 세션 하나(`my_id`/`target_id`/`channel`) |
| `[2]` 로그인 저장 | `SavedLogin`(dataclass), `LoginStore` |
| `[3]` 화면 상태 | `Screen`(Enum: LOGIN/RECENT_LOGINS/MAIN), `LoginFormState` |
| `[4]` 화면 전환 규칙 | `LoginFlow` |
| `[5]` 로그 창 | `MobileLogBuffer`(deque, 기본 8줄) |

**`DomimanClient`** — 명령 빌더는 전부 `send_command(body, kind)` 한 겹 위에
얹혀 있고, 보내면서 `pending={"kind","sent"}`에 들어간다(`resolve_pending`,
`check_pending_timeout` = `PENDING_TIMEOUT_SEC` 15초, PC와 동일):
`cmd_login`(=`S`, **로그인은 상태 질의다**) `cmd_start`(G) `cmd_stop`(P)
`cmd_sched_exit_ask`(Y) `cmd_sched_exit_set`(Y,n) `cmd_collect_now`(W)
`cmd_exit_program`(Q) `cmd_set_resolution`(V,a|1080|1440) `cmd_set_timer`(T,n)
`cmd_set_flags`(C,로그[,낚싯대,미끼]) `cmd_tank_query`(N).
수신은 `stream()`(권장, 연결 1개 유지) / `poll()`(폴백) / `stream_disconnect()`
(다른 스레드에서 강제 close해 블로킹 `iter_lines`를 깨움 — PC의
`ntfy_stream_disconnect`와 같은 패턴). 파싱은 `dispatch()` →
`parse_status()` / `report_text()` / `parse_tank_reply()`.

**`dispatch()`가 발신자 필터까지 겸한다:** `title != target_id`면 버리므로
**내가 보낸 메시지(Title=my_id)가 저절로 걸러진다** — PC의 스트림 루프처럼
따로 자기 발신분 제외 코드를 두지 않는다. 응답은 `{my_id},Z,...`,
보고는 `,Z,F,...`(무명 브로드캐스트) 두 갈래만 인정한다.

### Chaquopy JSON 브리지 (설계 결정 — 이 패턴을 따를 것)

Kotlin이 `Map<PyObject,PyObject>` 변환을 다루지 않도록, **Python이 JSON
문자열로만 돌려준다.** 새 반환값을 Kotlin에 넘길 일이 생기면 PyObject API를
파헤치지 말고 같은 방식으로 `*_json` 헬퍼를 추가한다.
- `attempt_login_json(client)` — 실패 시 문자열 `"null"`
- `dispatch_json(client, title, body)` — 아래 스키마
- `LoginStore.to_json()` / `from_json()` — SharedPreferences에 문자열 하나로

`dispatch_json` 반환 키: `kind`(reply|report|null) · `status` · `tank`/`tank_fail`
· `echo` · `sched_minutes` · `report_text` · `report_status_key` ·
`report_notify_key`.

**`echo` 분기가 있는 이유(함정, 재발 방지):** `G/P/W/Q/Y` 응답은 domiman.py가
상태 필드 없이 **명령 글자만 되돌려주는 에코**다(시작 성공 → `,Z,G`). 예전엔
이 분기가 없어 그 응답이 `parse_status`에서 `None`으로 버려져, **모바일에서
'시작/중지'를 눌러도 running/pending이 갱신되지 않는 버그**가 있었다.
상태 응답(S/V/T/C)은 첫 필드가 타이머 숫자라 이 글자들과 겹치지 않으므로
`rest[0]`로 안전하게 구분된다.

**`parse_status`의 '실행중' 필드는 5번째 고정(PC와 반드시 같이 맞출 것):**
낚싯대/미끼 필드처럼 있다 없다 하면 자리가 밀려 파싱이 꼬인다. PC 프로토콜을
바꾸면 이 함수도 같이 고쳐야 한다.

### 실제로 쓰이는 것 / 레퍼런스로만 남은 것

`LoginFlow`는 **앱에서 쓰지 않는다** — 화면전환 책임이 Kotlin의
Navigation3/ViewModel과 겹쳐서, `DomimanRepository`는 그보다 낮은
`LoginStore`/`DomimanClient`/`attempt_login_json`만 직접 호출한다.
`Screen`/`LoginFormState`/`LoginFlow`는 **규칙을 읽기 위한 참고 구현**으로
남겨둔 것이고, 그 안의 정책(아래)은 Kotlin 쪽에 그대로 구현돼 있다:
- `add_or_bump` — 같은 (id,피제어PC,채널)이면 갱신 후 맨 위로(중복 없음, 상한 없음)
- `update` — 수정은 **맨 위로 옮기지 않는다**(재로그인이 아니라 정보 수정이므로)
- `logout` — 자동로그인 **무장만 해제**. 목록과 폼의 마지막 값은 유지
  (로그아웃 ≠ 데이터 삭제)
- `attempt_login` — 스트림을 열고 0.3초 뒤 `cmd_login()`, 15초 대기.
  앱에서는 Foreground Service가 이미 `stream()`을 돌리므로 그 큐를 재사용하면
  되고 매번 스트림을 여닫을 필요는 없다(콜드 스타트 자동로그인용 독립 버전)

**스모크 테스트:** `python domiman_m.py <내 ID> <대상PC> <채널>` — 로그인 →
최근 로그인 저장 → 로그아웃 → 자동로그인 재시도(무장 꺼져 시도 안 되는지)까지
한 번에 확인한다. 프로토콜을 고쳤을 때 PC 없이 1차 검증하는 수단.

### ⚠️ 사본 동기화 (자동화 없음)

`domiman_m.py`는 안드로이드 프로젝트의 `app/src/main/python/`으로 **손으로
복사**해야 반영된다(`Copy-Item`, gradle 자동 태스크 없음). **원본만 고치고
끝내면 앱은 옛 코드로 계속 돈다.**

**2026-08-28 정정 — Kotlin 소스는 이제 이 저장소 안에 있다.** 아래 ②는
2026-08-10 시점의 관찰이고 지금은 해당되지 않는다: 안드로이드 프로젝트가
`macro/domiman-android/`로 들어와 **macro 저장소가 63개 파일을 추적**하고 있으며,
그 트리가 최신·권위 사본이다(빌드만 ASCII 경로에서 한다 — 아래 [빌드·배포
워크플로] 참고). ①(배포 APK로 내용 검증)과 ③(가짜 폴더 목록)은 여전히 유효하다.

**① 배포된 APK가 문서 내용을 증명한다.** `C:\Users\windo\OneDrive -
한국교통대학교\domiman.apk`(약 47MB, 2026-07-28 13:32)를 열어 확인:
- 패키지 `com.example.domiman` (dex 내 클래스 참조 348건) — 문서 그대로
- 문서가 기술한 클래스가 **전부 존재**: `DomimanApplication` `MainActivity`
  `NavigationKt` `DomimanService` `DomimanWidgetProvider` `NotificationSettings`
  `data/{DomimanRepository, DomimanNotifications, NotificationPrefs, NotifyItem,
  DomimanStatus, DispatchResult, SavedLoginJson, LoginStoreJson}`
  `ui/{login, main, notif, recent}` `theme/`
- Chaquopy(Python 3.13, `libpython3.13.so`), dex 11개
- 내장 Python은 **`assets/chaquopy/app.imy` 안의 `domiman_m.pyc` 하나뿐**
  (44,422 bytes). `attempt_login_json`·`dispatch_json`·`notify_key_for_report`
  ·`NOTIFY_KEYS`·`stream_disconnect`·`parse_tank_reply`·`LoginFlow`
  ·`MobileLogBuffer` 심볼과 이 저장소 원본의 한글 docstring이 그대로 들어 있어
  **737행 최신 원본으로 빌드된 것**이 확인된다.

  ※ 검증 방법(재사용): apk는 zip이다. `unzip -o apk 'classes*.dex'
  'assets/chaquopy/app.imy'` 후 dex를 바이너리 그대로 훑어
  `Lcom/...;` 문자열을 뽑고, `app.imy`(역시 zip)에서 `domiman_m.pyc`를 꺼내
  심볼 문자열을 확인한다.

**② (낡음 — 2026-08-10 당시) Kotlin 소스가 이 PC에 없었다.** 지금은
`macro/domiman-android/`에 전부 있다. 이 항목은 "문서가 가리키는 경로가 비어
있으면 먼저 찾아보고 문서를 고쳐라"는 사례로만 남겨 둔다.

**③ 헷갈리기 쉬운 '가짜' 폴더 3개 — 문서의 그 프로젝트가 아니다:**
| 경로 | 정체 |
|---|---|
| `C:\Users\windo\DomimanApp` | 초기 시제품. 패키지 `com.domiman.app`, 소스 폴더 `kotlin/`, 화면 2개(`LoginScreen`/`ControlScreen`), `DomimanPollService`(폴링), 위젯·알림설정 없음, git 아님. `domiman_m.py` 사본이 **287행**(재설계 이전) |
| `C:\humble\workspace\domiman` | 또 다른 시제품(2026-07-20~21). 패키지 `com.domiman.control`, `App/Bus/ControlService/Prefs/StreamCallback.kt` + `bridge.py`. 상위 git은 무관한 ROS 워크스페이스 |
| `C:\Program Files\domiman`, `macro\dist\domiman` | **PC판 exe** 설치본/빌드 산출물. 안드로이드와 무관 |

---

**실제 안드로이드 프로젝트(구현됨) — 소스는 이 저장소 안, 빌드는 ASCII 경로에서
(2026-08-28 정정):**
- **소스(권위 사본) = `macro/domiman-android/`** — git으로 추적되고 GitHub
  원격(`cheongbaek/domiman`)까지 올라간다. **여기를 고친다.**
- **빌드 = `C:\Users\windo\dev\domiman-android`** — AGP/Chaquopy가 네이티브
  빌드 도구 특성상 **비ASCII 경로에서 빌드를 거부**하는데(`macro` 경로에
  "한국교통대학교" 등 한글이 있다) 그 제약은 그대로다. 그래서 이 폴더는 이제
  **빌드 전용 거울**이다: 소스를 여기로 복사해 넣고 gradle을 돌린다.
  `local.properties`(SDK 경로)와 `.gradle`/`app/build` 캐시가 여기 있어 빌드가
  빠르다. 이 폴더의 git은 옛 백업(원격 없음)이고 지금은 권위가 없다.
- 예전 문서는 "macro 안이 아니다"라고 못박아 뒀었다. 2026-08-21에 프로젝트가
  저장소로 들어오면서 그 서술이 뒤집혔다 — **소스는 macro, 빌드만 dev**다.

### ⚠️ 현재 안드로이드 앱 상태 = 유지 대상(권위 문서, 재설계 금지)
**새 세션에서 절대 UI/구조를 새로 설계하지 말 것.** 아래가 지금 실제로
빌드·동작 중인 최종 상태다. 사용자가 이 UI를 그대로 유지하길 원한다. 변경
요청이 있을 때만 해당 부분을 고치고, 그 외엔 이 스펙을 보존한다.

**로컬 git(백업, 원격 없음):** `C:\Users\windo\dev\domiman-android`에 git
초기화돼 있고 커밋으로 백업 중(원격 push 안 함 — GitHub 미연동). 매 기능
단위로 커밋해 둘 것. `.gitignore`가 `build/`·`.gradle/`·`local.properties`를
제외(소스+`gradle-wrapper.jar`만 추적). 최신 커밋 `e082da7`
(위젯 아이콘/토스트/런치신뢰성/앱아이콘)이 현재 상태.

**파일 트리(app/src/main):**
- `java/com/example/domiman/`
  - `DomimanApplication.kt` — Python.start + `repository` 앱 싱글턴 생성
  - `MainActivity.kt` — repository는 `(application as DomimanApplication).repository`,
    첫 실행 권한요청(알림→배터리예외), 다크모드 override 상태 보유
  - `Navigation.kt` — Navigation3. **시작 화면 = 이전 로그인돼 있었으면 Main,
    아니면 Login**(`if repository.isSessionConfigured() Main else Login`)
  - `NavigationKeys.kt` — `Login`/`RecentLogins`/`Main`/`NotificationSettings`
  - `DomimanService.kt` — 포그라운드 서비스(specialUse|dataSync)
  - `DomimanWidgetProvider.kt` — 4x1 홈 위젯
  - `data/DomimanRepository.kt` — 앱 싱글턴 핵심(세션·스트림·명령·알림·위젯 연동)
  - `data/DomimanModels.kt` — `@Serializable` DTO(DomimanStatus/DispatchResult/
    SavedLoginJson/LoginStoreJson), dispatch_json/attempt_login_json JSON 파싱
  - `data/NotificationPrefs.kt` — 알림 설정 영속(`NotifyItem` enum 10개 + master)
  - `data/DomimanNotifications.kt` — 알림 채널/발송, contentIntent로 앱 열기
  - `ui/login/`·`ui/recent/`·`ui/main/`·`ui/notif/` — 각 화면 Compose + ViewModel
  - `theme/` — 템플릿 기본 테마
- `python/domiman_m.py` — **이 저장소 `domiman_m.py`의 사본. 원본 고치면 반드시
  `Copy-Item`으로 재복사**(자동 동기화 없음).
- `res/drawable/` — `widget_bg.xml`(둥근 밝은 패널), `widget_btn_bg.xml`(누름
  하이라이트 셀렉터), `widget_ic_refresh/collect/play/pause.png`(제공된 초록 세트)
- `res/drawable-nodpi/ic_launcher_scene.png` — 앱 아이콘(록맨 낚시 장면, adaptive 배경)
- `res/layout/widget_domiman.xml`, `res/xml/domiman_widget_info.xml`(4x1)
- `res/mipmap-*` — 앱 아이콘(adaptive XML + 밀도별 png)

**화면별 정확한 UI(유지):**
- **로그인(`ui/login`, 앱UI설명 Sheet1 A1:E18):** 제목 "당신도 강태공이
  되어보세요!", 입력 3개(ID / 피제어 PC 이름 / ntfy 채널명), `[v] 자동 로그인`
  체크박스, 버튼 행 `[로그인][…]`. '…'=최근 로그인 화면. 실패 시 폼 비우고
  "입력값을 초기화하고 다시 입력하세요." edit 모드일 땐 체크박스 숨기고 버튼이
  `[수정][취소]`.
- **최근 로그인(`ui/recent`, Sheet1 A21:E39):** 상단 `< 최근 로그인`, 리스트
  행마다 `ID` + `피제어PC · 채널`. **짧게 탭=즉시 로그인**, **길게 눌러 드롭다운
  [수정]/[삭제]**. 로그인 화면에서 '자동 로그인' 체크 후 '…'로 들어와 탭하면
  그 항목도 자동로그인 무장(`repository.pendingAutoLoginArm`).
- **메인(`ui/main`, Sheet2, PC GUI와 유사):** 세로 스크롤. 순서 =
  해상도 라벨 + `[직접 설정][자동 감지]` / 타이머 입력(분, 디바운스 1.5초 발신) +
  "0을 입력하면 살림망 감시 모드로 작동합니다." / `[v]낚싯대 자동교체`
  `[v]미끼 자동교체` + "…감시 모드에서만…" / 큰 `시작|중지` 버튼(running이면
  '중지') / 상태 메시지 / `[예약 종료][즉시 회수]` / `[실시간 수량확인][다크모드]`
  / `로그` 헤더 + `x`(지우기) + 로그 8줄 패널 / 하단 `[로그아웃][알림 설정]`.
  예약 종료는 분 입력 다이얼로그→`Y,n`. 해상도 직접설정은 1080/1440 다이얼로그.
  버튼은 응답 대기(isPending) 중 잠기고 15초 무응답이면 자동 해제+"응답이
  없습니다." 로그.
- **알림 설정(`ui/notif`):** 상단 `< 알림 설정`, 마스터 `[v] 알림 켜기`(꺼지면
  아래 10개 봉인·미발송), 이하 10개 체크박스(순서 고정): 살림망 회수
  시작/성공/실패, 낚싯대 교체 시작/성공/실패, 미끼 교체 시작/성공/실패, 게임 튕김.
  기본값 전부 on, SharedPreferences 영속(앱데이터 삭제 시 소멸).

**빌드·배포 워크플로(2026-08-28 실측 기준):**
1. `domiman_m.py`를 고쳤으면 **사본을 먼저 갱신**한다(자동 동기화 없음):
   `Copy-Item domiman_m.py domiman-android\app\src\main\python\domiman_m.py`
2. **소스를 빌드 거울로 복사**한다(비ASCII 경로에서는 빌드가 안 되므로):
   `macro\domiman-android`의 `app\src`, `app\build.gradle.kts`, 루트 gradle
   파일들 → `C:\Users\windo\dev\domiman-android`의 같은 자리.
   (`app/src`는 통째로 지우고 새로 복사해야 지운 파일이 남지 않는다.
    `local.properties`·`.gradle`·`app/build`는 **건드리지 말 것** — 거울의 SDK
    경로와 캐시다.)
3. `cd C:\Users\windo\dev\domiman-android; $env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"; .\gradlew.bat assembleDebug`
   - **`buildPython`은 빌드하는 PC에 실제로 있는 python.org 인터프리터여야 하고
     `version`과 major.minor가 같아야 한다.** 260828a에 `3.12`로 적혀 있던 것을
     `3.13`으로 되돌렸다 — 이 PC에는 3.12가 없다(`py -0` = 3.14·3.13). 3.13은
     실제로 APK를 만들어낸 조합이다(APK 안 `libpython3.13.so`).
4. 빌드 결과 `app\build\outputs\apk\debug\app-debug.apk`를 **손으로 복사해
   `C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk`를 덮어쓴다**(사용자
   지정 배포 위치). gradle 자동복사 태스크는 두지 않음(사용자 요청).
- **구조**: `Login`/`RecentLogins`/`Main` 3화면(Navigation3), 각 화면=
  Compose 화면+ViewModel, 전부 `DomimanRepository`(Chaquopy 브리지) 공유.
  `domiman_m.py`는 `app/src/main/python/domiman_m.py`에 복사돼 있음 — **원본
  (이 저장소의 `domiman_m.py`)을 고치면 반드시 그 경로로도 재복사**할 것
  (자동 동기화 없음, 지금은 수동 `Copy-Item`).
- **Chaquopy 브리지는 JSON 문자열로만 주고받는다(설계 결정):** PyObject의
  `Map<PyObject,PyObject>` 변환이 불확실해, `domiman_m.py`에 Kotlin 전용
  헬퍼(`attempt_login_json`, `dispatch_json`, `LoginStore.to_json/from_json`)
  를 추가해 전부 JSON으로 왕복시킨다. Kotlin 쪽은 `kotlinx.serialization`으로
  파싱(`DomimanModels.kt`). 새 Python 반환값을 Kotlin에 넘길 일이 생기면
  PyObject API를 직접 파헤치지 말고 이 패턴(JSON 헬퍼 추가)을 따를 것.
- **역할 분리**: `domiman_m.LoginFlow`(화면전환 규칙)는 Kotlin의
  Navigation3/ViewModel과 책임이 겹쳐 **안 씀** — `DomimanRepository`는 그보다
  낮은 `LoginStore`/`DomimanClient`/`attempt_login_json`만 직접 호출한다.
  Python은 프로토콜+데이터 규칙, Kotlin은 화면/내비게이션 상태.
- **빌드 시 겪은 함정 3가지(재발 방지)**:
  1. 위에 적은 **비ASCII 경로 거부** — AGP가 빌드 자체를 막음(우회 설정
     `android.overridePathCheck=true`도 있지만, Chaquopy가 네이티브 링킹을
     하므로 진짜 실패 위험이 있어 경로 이전으로 해결).
  2. **Configuration Cache와 비호환** — Chaquopy가 구성 단계에서 외부
     프로세스(빌드용 python 탐지)를 실행해 Gradle configuration cache와
     충돌. `gradle.properties`의 `org.gradle.configuration-cache=false`로
     꺼둠(`android create` 템플릿 기본값은 `true`).
  3. **buildPython 자동탐지 실패** — Chaquopy는 호스트에서 빌드 스크립트를
     돌릴 Python(APK에 내장되는 타겟 Python과 별개)이 필요한데, 이 PC의 `py`
     런처 `-V:3.13` 슬롯이 (도미맨 PC 프로젝트에도 이미 기록된 함정과 같은)
     **Microsoft Store 스텁**이라 자동탐지가 실패했다. python.org 정식
     3.13(`winget install Python.Python.3.13`)을 설치해
     `chaquopy.defaultConfig.buildPython("C:/.../Python313/python.exe")`로
     경로를 명시해 해결.
  4. **DomimanRepository의 send\* 함수는 반드시 suspend + Dispatchers.IO**
     (실기기 테스트에서 발견 — 처음엔 `sendStart`/`sendSetResolution` 등이
     평범한 `fun`이라 Compose `onClick`에서 바로 호출됐는데, 이게
     `cmd_*→send_command()→requests.post()`로 이어지는 **블로킹 네트워크
     호출을 메인 스레드에서 그대로 실행**해 ntfy 발신이 버벅이거나 실패했다.
     `login()`/`attemptLogin()`은 처음부터 `withContext(Dispatchers.IO)`로
     감쌌었지만 Sheet2의 명령 버튼들은 빠뜨렸던 것 — Chaquopy로 Python
     네트워크 함수를 새로 노출할 때마다 이 패턴을 반드시 지킬 것.
- 빌드: `cd C:\Users\windo\dev\domiman-android; $env:JAVA_HOME =
  "C:\Program Files\Android\Android Studio\jbr"; .\gradlew.bat assembleDebug`
  (JAVA_HOME은 전역으로 안 걸어뒀으므로 매번 지정 필요 — [런처/코어 분리]
  절의 PATH 새로고침과 같은 이유).
- 에뮬레이터: SDK에 `emulator`+`system-images/android-36/google_apis/x86_64`
  설치 완료, `android emulator create`로 `medium_phone` AVD 생성됨(기본
  device profile 자동 선택 — 이후엔 `android emulator start medium_phone`).
  단, 실기기 테스트가 더 빨라 에뮬레이터 부팅은 중간에 보류한 적 있음.

**세션을 앱 싱글턴 + 포그라운드 서비스로 이관(구현됨 — 백그라운드 복귀
문제 해결 + 알림):** 실기기에서 "앱을 완전히 안 끄고 빠져나왔다 다시 들어가면
ntfy 발신·수신이 죽어 껐다 켜야 정상" 문제를 잡기 위해 세션 구조를 바꿨다.
- **`DomimanRepository`가 앱 싱글턴**이 됨(`DomimanApplication.repository`,
  Activity당 생성 X). 이유(함정): 예전엔 저장소가 `MainActivity`의
  `by lazy` 필드라 화면 회전/백그라운드 복귀로 Activity가 재생성되면
  `activeClient`(로그인 세션)가 사라져 이후 모든 명령이 no-op이었다.
- **스트림은 `appScope`(앱 수명 `CoroutineScope`)에서 돎** — ViewModel/Activity와
  독립. 세션 수명 = 로그인~로그아웃(더 이상 MainScreen VM의 onCleared에서
  세션을 끄지 않는다).
- **포그라운드 서비스 `DomimanService`(type=`specialUse|dataSync`)**: 로그인 시
  `beginSession()`이 서비스를 띄워 OS가 백그라운드에서 프로세스를 못 죽이게
  한다 → 스트림이 계속 살아 이벤트 알림이 오고, 다시 앱에 들어와도 세션이
  그대로. 서비스는 스트림을 직접 들지 않고(스트림은 appScope) 상시 알림으로
  프로세스만 살려둔다. `START_STICKY`라 예외적으로 죽어도 재시작되며 이때
  `reviveIfNeeded()`가 저장된 마지막 로그인으로 재접속(세션 소실 시).
  **함정 — dataSync는 Android 14+에서 하루 6시간 제한**이라 장기 방치 시
  종료→프로세스 사망했다. 그래서 34+는 시간제한 없는 `specialUse`를 쓰고(권한
  `FOREGROUND_SERVICE_SPECIAL_USE` + `<property PROPERTY_SPECIAL_USE_FGS_SUBTYPE>`),
  33 이하는 `dataSync` 폴백(매니페스트에 `specialUse|dataSync` 병기, 코드에서
  SDK로 분기). 그래도 삼성 One UI 공격적 절전은 변수라, 최후엔 설정→배터리→
  '제한 없음'에 앱 추가 권장(코드로 강제 불가).
- **위젯 탭 시 프로세스 보호(함정):** 위젯 버튼 broadcast(`onReceive`)는 반환되면
  프로세스가 곧 죽어, 15초 걸리는 재로그인이 끝나기 전에 종료돼 갱신이 안 됐다
  → `onReceive`에서 **먼저 `DomimanService.start()`로 프로세스를 붙잡은 뒤**
  명령을 appScope로 던진다.
- **복귀 리프레시**: MainScreen이 `LifecycleEventEffect(ON_RESUME)`에서
  `ensureSessionAlive()` 호출 — 세션 있으면 스트림 새로 붙이고 S 질의로 상태
  재동기화, 세션 없고 직전 로그인 상태였으면(`session_active` 영속 플래그)
  마지막 로그인으로 재접속, 그래도 실패면 로그인 화면으로.
- **스트림 즉시 중단**: `domiman_m.DomimanClient.stream_disconnect()`(열린
  응답 강제 close, PC의 `ntfy_stream_disconnect`와 동일)로 블로킹 `iter_lines`를
  깨워 로그아웃/재접속 시 옛 스레드가 안 남게 함.

**알림(구현됨):** 하단 '로그아웃' 옆 '알림 설정' 버튼 → `NotificationSettings`
화면(마스터 '알림 켜기' + 10개 이벤트 체크박스, 마스터 꺼지면 하위 봉인,
기본값 전부 on). `NotificationPrefs`(SharedPreferences, 앱데이터 삭제 시 소멸)에
영속. 실제 알림은 `DomimanRepository.onStreamMessage`가 보고(`,Z,F,*`) 수신 시
`NotificationPrefs.isEnabled(notifyKey)`면 `DomimanNotifications.postEvent`로
발송 — **화면이 아니라 저장소(앱 스코프)에서 띄우므로 백그라운드에서도 온다.**
어느 체크박스인지는 `dispatch_json`이 넣어주는 `report_notify_key`
(domiman_m.`notify_key_for_report`, NOTIFY_KEYS 역변환)로 판정. 채널 2개
(ongoing=서비스 상시/낮음, events=이벤트/기본). **알림·상시알림 탭 시 앱을
연다**(`contentIntent`=MainActivity, CLEAR_TOP).
- **첫 실행 권한 요청(MainActivity)**: POST_NOTIFICATIONS(13+) 요청 후 콜백에서
  배터리 최적화 예외(`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) 요청 —
  절전모드가 백그라운드 서비스/네트워크를 죽이지 않도록. `savedInstanceState
  == null`일 때만(회전 재요청 방지).

**홈 위젯 4x1(`DomimanWidgetProvider`, 구현됨):** `[새로고침]|수량|[스크린샷]|[재생/중지]`.
아이콘은 제공된 초록 원형 세트(`widget_ic_*`, `A_clean_2x2_grid_icon_set...png`를
사분면 크롭). 동작: 새로고침=`cmd_tank_query`(N, 토스트 "실시간 수량 확인"),
스크린샷=`ScreenshotActivity`(옛 '즉시 회수' 칸을 260821~에 바꿔 끼움 — 아이콘·위치는
그대로), 재생/중지=시작·중지 토글
(running이면 정지 아이콘 `widget_ic_pause`+토스트 "매크로가 중지되었습니다",
아니면 재생 `widget_ic_play`+"매크로가 시작되었습니다"). 각 버튼 누름 하이라이트
(`widget_btn_bg`). **수량 표시는 260828a부터 상시 갱신된다** — 피제어 PC가
사이클마다 보내는 방송(`,Z,N,cur,mx`)을 받아 **30초에 한 번씩 묶어** 다시 그린다
(값이 같으면·홈에 위젯이 없으면 아무 일도 하지 않는다. 위 [모바일 수신] 절 참고).
새로고침(N 응답)은 사람이 기다리므로 즉시 그린다. running 아이콘은 상태가 변할 때
갱신 — 전부 `DomimanRepository`가 prefs(`widget_qty`/`widget_running`)에 쓰고
`DomimanWidgetProvider.refresh()`를 호출하는 구조다.
**로그인 세션 없으면 수량칸에 '로그인' 표시**, 그 상태에선 모든 칸 탭이 앱을
연다(로그인 유도). **로그인 상태에선 수량칸 탭도 앱을 연다.** 위젯 명령은
`repository.widget*()` → `ensureClientReady()`(세션 없으면 마지막 로그인으로
재접속) 후 실행.

**앱 아이콘 = 둥근 록맨(구현됨):** PC의 `app.ico`(록맨 낚시 픽셀아트)를 adaptive
아이콘의 **배경 레이어로 꽉 채워** One UI가 자동으로 둥글게 마스킹
(`mipmap-anydpi-v26/ic_launcher*.xml` → background=`@drawable/ic_launcher_scene`,
foreground=투명). 구버전용 밀도별 `mipmap-*/ic_launcher(.round).png`도 둥근
버전으로 생성(기존 webp·기본 로봇 아이콘 삭제).

- **아직 안 한 것**: 시스템 뒤로가기 버튼 실제 인터셉트(`OnBackPressedCallback`
  — 지금은 화면 내 '로그아웃' 버튼으로만 2단계 확인), 위젯 아이콘 다크모드
  대비(초록 세트라 대체로 무난), 안드로이드 프로젝트 GitHub 원격 연동(현재
  로컬 git 백업만).

## 즉시 중지 · 기본 상태에서 재시작 (260815d, 구현됨)

예전엔 '중지'(버튼·원격 P)가 `stop_event`만 세워서, 진행 중이던 루틴(회수·교체)이
**끝까지 다 돌고 나서야** 멈췄다. 이제는 그 자리에서 버린다.

- **`request_abort()`가 `_abort_event`를 세운다.** `abort_sleep()`(루틴 안의 모든
  `time.sleep` 대체)과 `check_abort()`(`click_real`/`press_key` 진입점)가 이를 보고
  `RoutineAborted`를 던져 콜스택을 통째로 빠져나온다. 중지 지연은 길어야
  **OCR 한 번**(1초 미만).
- **`RoutineAborted`는 `Exception`이 아니라 `BaseException`을 상속한다(함정).**
  루틴 곳곳에 OCR·win32 호출을 감싼 `except Exception`이 있어서, 평범한 예외로
  만들면 그 블록들이 중단 신호를 **삼켜** 그 자리에서 '계속 진행'이 돼 버린다.
  KeyboardInterrupt와 같은 이유로 계열을 달리한다. (그래서 `except:` 맨몸 블록을
  새로 만들면 안 된다 — 현재 파일에 하나도 없다.)
- **하드웨어 입력의 누름/뗌 사이는 끊지 않는다.** `click_real`의 0.1초,
  `press_key`의 0.05초는 `time.sleep` 그대로다 — 여기서 던지면 마우스 버튼이나
  키가 **눌린 채로 남는다.** 그 둘만 예외이고 나머지는 전부 `abort_sleep`.
- **'시작'을 누르면 `reset_to_base_state()`로 화면부터 정리하고 출발한다.** 중지가
  퀴즈 창·리스트 창이 열린 도중에 걸렸을 수 있으므로 그 잔재를 이어받지 않는다.
  내용은 `bring_game_to_front` + **ESC 3번**(교체 루틴 진입·`_resume_fishing`과 같은
  '종류 불문 정리' 패턴). **낚시 취소/시작 버튼은 누르지 않는다** — 상태를 모르는
  채 그 좌표를 누르면 오히려 낚시가 새로 시작돼 버린다(`run_fishing_routine`의
  함정과 같음). 이후는 각 루프가 알아서 상태를 읽고 재개한다.
- **끝나면 `clear_abort()`로 원복**한다(`FishingWorker.run`의 finally). 안 풀면 다음
  '시작'이 첫 클릭에서 바로 끊긴다. `_start_fishing`도 방어적으로 한 번 더 푼다.
- 즉시 회수(`on_collect_now`)와 실시간 수량확인(`_tank_check_and_resume`)도 같은
  신호로 끊기며, 각자 `except RoutineAborted`로 조용히 접는다. 수량확인은 이미
  답을 보냈는지(`reported`)를 보고 **응답을 두 번 보내지 않는다**(원격 N).

---

## 값 대장 — 상수·횟수·지연의 의도 (건드리기 전에 여기부터 볼 것)

좌표는 위 '검증된 좌표·영역' 참고. 여기는 **좌표가 아닌 값**들의 근거다.
표에 있는 값은 전부 이유가 있는 값이므로 **임의로 바꾸지 않는다.**
근거가 '미확인'인 줄을 바꿔야 할 일이 생기면 **먼저 사용자에게 묻는다.**

| 값 | 위치 | 의도 |
|---|---|---|
| `TANK_COLLECT_MARGIN = 5` | 회수 조건 `cur+5>=mx` | 가득차기 5칸 전. **'살림망이 가득 차' 오류 예방**이 목적. 폴링 간격=최소 획득 시간이라 한 사이클에 최대 1마리 → 5칸 버퍼면 절대 못 넘김 |
| ESC **3번** | 교체 루틴 진입, `_resume_fishing` | 어떤 창이 떠 있을지 모르니 **무조건 정리**. 종류 불문 |
| `press_esc` 기본 `delay=0.5` | 전 호출부 | ESC 간격도 **0.5초로 통일**(260909a). 예전 기본값 1.0과 `_use_card`의 두 번째 ESC만 1.0이었는데, click_real·press_key와 같은 0.5로 맞췄다(문서에는 이미 '0.5초 간격'으로 적혀 있었다). 낚시 루틴 시작부의 Enter+ESC ×4는 **원래부터 0.5**였다 |
| ESC **2번** | `_use_card` ('사용하기' 직후) | **정확히 2개**를 닫는다 — 리스트 창 + 그 밑에 깔린 팝업. 리스트가 팝업 **위에** 겹쳐 뜨므로 1번이면 팝업이 최상단 모달로 남아 이후 클릭을 삼킨다. 진입의 3번과는 **무관한 상황**이니 맞추려 들지 말 것 |
| `range(4)` Enter+ESC 연타 | `run_fishing_routine` 초반 | 혹시 떠 있을 **게임 내부 창**을 닫기 위함. 게임 창은 전부 게임 자체 레이아웃이며 **별도 Windows 창으로 뜨는 경우는 없다** |
| `skip_cancel=True` | 낚싯대 교체 → 회수 경로 | 교체 루틴이 방금 ESC로 화면을 정리했고 '낚시 시작'도 안 눌렀으므로, 위 연타와 '낚시 취소' 클릭을 **판독 없이 무조건 생략**하고 '살림망 확인'으로 직행 |
| `minsec == 1` **and** `maxsec == 1` | 낚싯대 트리거 ① | 교체 필요 시 게임이 '1초'로 표시. 낚싯대 **전용** 신호라는 점이 핵심. 최대까지 보는 이유는 '11초'→'1초' 오인식 때문(확신도로는 못 걸러짐 — 위 절 참고). **최소만 보게 되돌리지 말 것** |
| `REGION_MAX_TIME=(1035,916,80,34)` | 만료 교차 확인 | 후보 3개를 표본 전부(60·1·70·12초)에 돌려 고름 — 다 맞히면서 최저 확신도 0.83으로 가장 높았다(다른 후보는 특정 표본에서 0.39까지 떨어짐) |
| `cur > mx` | 낚싯대 트리거 ② | 초과 상태(570/420)는 낚시가 안 걸려 회수만으론 재개 불가 → 최대치 큰 낚싯대로 교체. **`cur+5>=mx`로 넓히지 말 것** — 그러면 멀쩡한 낚싯대까지 갈아 끼운다(260811a에서 제거한 조건) |
| ESC **3번** + '살림망 확인' 재클릭 | 퀴즈 확인 실패 시 재시도 | 실패했다는 건 이전 시도의 창이 남아 있다는 뜻 — '마이룸 보내기'부터 다시 눌러선 같은 화면을 헛돈다. 화면을 정리하고 퀴즈 창을 새로 연다. ESC/클릭의 기본 0.5초 대기가 곧 재시도 간격(옛 `sleep(2.0)` 대체) |
| `BAIT_MAX_PAGE_MOVES = 4` | 미끼 페이지 넘김 | 총 5페이지 탐색 |
| `ROD_EXPIRY_CONFIRM_READS = 2` | 낚싯대 만료 확인 | 0.4초 간격으로 **다시 읽어** 계속 1초인지 본다. 한 프레임 튐(15초→1초)을 거르는 장치이므로 0으로 만들지 말 것 |
| 버튼 = '낚시 시작' **AND** | 낚싯대 트리거 ①②·미끼 교체 (260822a·b) | 교체가 필요한 상황이면 **낚시가 이미 멈춰 있다.** 값 판독과 원인이 다른 신호라, 기록에 남은 헛교체 사고가 이 조건 하나로 막힌다. **`None`(판독 불가)은 통과시키지 말 것.** **트리거 중 하나라도 이 게이트를 빼면 그 하나로 전부 무너진다** — 260822a가 ②를 빼놨다가 진행 중인 낚시를 계속 끊었다 |
| `run_rod_swap_routine(reason)` | 교체 루틴 로그 | 사유를 **인자로 받아** 찍는다. 고정 문구로 찍던 260822a에서 트리거 ②를 ①으로 오진해 원인을 한참 놓쳤다 |
| ~~`ROD_EXPIRY_FROZEN_CYCLES = 10`~~ | 제거됨 (260822a) | 낚시가 '진행 중'으로 보여도 수량이 멈춰 있으면 교체를 허용하던 **탈출구**. 위 버튼 AND를 우회하는 구멍이라 없앴다. 되살리지 말 것 — 버튼이 영구히 안 읽히는 경우의 통로는 트리거 ②(`cur > mx`, 버튼을 보지 않음)가 담당한다 |
| `same_count >= 3` | 낚시 정지 감지 | 수량이 몇 사이클 안 잡히는 건 정상이라 **약한 신호** — 그래서 바로 재개하지 않고 `is_fishing_active()`로 한 번 더 확인 |
| `retries=4, delay=0.3` | `read_tank_quantity` | 갱신 애니메이션/깜빡임을 건너뛰려 프레임 여러 번 재시도. 이제 **첫 성공이 아니라 다수결**로 고른다 |
| `OCR_NUM_SCALE = 3` = **폴백 전용** | `_prep_number_ocr` | 260815c가 x3를 주 경로로 골랐지만 그 측정은 **인위적 열화(0.5~0.6배 축소)** 기준이었고, 실제 실패 표본에서는 x3가 **또렷한 글자를 망가뜨렸다**(슬래시→'1', '11초'→'1초', '112'→'712'; 세 번 모두 원본은 정확). **260822c에 주 경로를 원본+흑백으로 뒤집고 x3는 흐린 화면용 폴백으로 내렸다.** 되돌리지 말 것. **숫자 화이트리스트는 여전히 넣지 않는다**(3→2/9로 나빠짐) |
| `BAIT_TARGET_PATTERN` 3갈래 OR | 미끼 카드 탐색 | '갯'은 실측 5/5 전부 오인식(개/켓/것/캠), '금'은 5/5 생존 → '금' 축(260822c). 260909a에 실측 오인식('급'·'지꼬'·'황금' 변형)을 OR로 넓혀 **5/5 · 오검출 0**. **`갯지렁`류를 넣지 말 것** — '갯지렁이 훈제 구이'(p1)·'다이아 갯지렁이'(p4)가 따로 있어 1페이지에서 먼저 걸린다. **사이도 `.{0,2}`로 벌리지 말 것** — '레볼루션'이 `허금루선지렇미`로 걸린다 |
| `ROD_TARGET_PATTERN` 4갈래 OR | 낚싯대 카드 탐색 | 실측 오인식 OR(260909a): 매직/미적, 장미/잠미, 푸른→푸튼·푸름·무릎·무른·꾸슨·꾸촌. **'스타'는 12/12로 안 깨지는 축.** 적중 12/12·12/12 · 오검출 0/60. **단음절 `푸`로 되돌리지 말 것**(깨지면 무력하고 새 낚싯대엔 오검출). **'낚싯대'는 모든 낚싯대에 있어 축으로 금지** |
| 크롭 크기로 **좌표 정규화** | `_find_cards_by_pattern` | `_watch_grab_region`이 WGC 원본에서 자르므로 QHD는 크롭이 1.252배(801x253). 안 나누면 **2행 전멸(오차 44>30) + 1행 열 밀림**으로 목표를 영구 미발견 → 좌상단 폴백으로 엉뚱한 카드를 쓴다(260909a, 라이브 5/5 미발견으로 확인). FHD는 ×1.0이라 결과 동일 |
| 이름 영역 전처리 **없음** | `_find_cards_by_pattern` | x2·x3·반전이진화를 다 재봤으나 **목표 매칭에 이득 없이 시간만 2~4배**(1.3초→5.2초). 글자 일치율 최고치도 65%로 어차피 정확히는 못 읽는다 → 전처리 대신 패턴으로 푼다 |
| `INK_WHITE_MIN = 185` | `_ink_span_fhd` | 흰 글자만 남기는 밝기 임계. 170~215에서 잉크 폭이 **완전히 평평**했고 230부터 흔들렸다 → 평평한 구간 가운데 |
| `INK_WHITE_PIXELS_MIN = 30` | 같은 곳 | 이만큼도 안 밝으면 글자가 없는 것으로 본다(진짜 '1초'는 흰 픽셀 80개라 안전) |
| `INK_DIGIT2_MIN = 26.0` | `_span_digits` | 1자리 21~22px와 2자리 29~32px **사이의 중간**. 양쪽으로 3px 이상 여유가 있어 임의값이 아니다. FHD 기준으로 정규화한 값이라 QHD에서도 같다 |
| 과반 아니면 `None` | `_read_gain_time` | 1표씩 갈린 동표를 `set` 순서대로 임의로 고르던 것을 막는다. 획득 시간을 못 읽어도 폴링 간격은 직전 값을 쓰고 교체도 안 하므로, 조용히 다음 사이클에 다시 읽는 것이 가장 안전 |
| 최대치 변화 시 **1회 재확인** | `read_tank_quantity` | `417/480`→`40/4` 같은 튐은 최대치가 바뀐 것처럼 보인다. 낚싯대 교체로 진짜 바뀔 수도 있으므로 버리지 않고 **같은 값이 두 번 나오는지만** 본다 |
| `OCR_DUMP_MAX = 40` | `_dump_ocr_crop` | 판독 실패·수상한 변화 때 크롭을 PNG로 남긴다. 화면을 못 보는 상태에서 원인(영역 밀림/프레임 튐/뭉갬)을 가릴 유일한 수단 |
| `mx > 0` (유일한 파싱 가드) | `read_tank_quantity` | 오인식 휴리스틱 **아님**. max가 0이면 `cur>=mx-5`가 항상 참이라 회수 무한 반복 |
| 값 범위 가드 **없음** | `read_tank_quantity` | `cur<=mx`, `cur<=mx*3` 모두 폐기됨. 정상 값(초과 상태)을 버려 영구 정체를 만든 전력 |
| `time.sleep(0.1)` / `(0.05)` | `click_real`·`press_key` 내부 | **여기만 `abort_sleep`이 아니다.** 누름/뗌 사이라 중간에 끊으면 마우스·키가 눌린 채 남는다 |
| ESC **3번** | `reset_to_base_state` ('시작' 직전) | 중지가 창이 열린 도중에 걸렸을 수 있어 종류 불문 정리. 낚시 버튼은 **누르지 않는다**(상태를 모른 채 누르면 낚시가 새로 시작됨) |
| `click_real` `delay=0.5` | **전 호출부 통일** | 클릭 후 대기는 **0.5초 하나로 통일**한다. 예전엔 대상마다 1.0/1.2/1.5/2.0/2.5로 제각각이었으나 통일하기로 결정됨. **호출부에서 `delay=`를 따로 넘기지 말 것** — 기본값을 쓴다 |
| 루틴 후 `stop_event.wait(0.5)` | 감시 루프 | **단순 대기다. OCR 시간이 아니다.** 루틴이 끝나고 화면이 안정될 여유. `time.sleep`이 아니라 `Event.wait`인 이유는 단 하나 — **'중지'를 누르면 즉시 깨어나기 위해서**. 예전 2.0/3.0에서 0.5로 통일됨 |
| `time.sleep(0.5)` | `_restart_or_collect_after_rod_swap` | 교체한 낚싯대 기준으로 수량 표시가 갱신될 여유. **갱신 자체는 즉시 된다** — 화면 지연·순간 오류 대비분 |
| `retries=3/delay=0.2`, `0<sec<=600`, `best_diff<80.0`, `max_retries=10`, `max(3.0,…)`, `last_interval=20.0`, `w*0.15`, `행Y 오차 30` | 각처 | **지연을 감안해 여유를 둔 값들이며 현재 문제없음.** 굳이 건드릴 이유 없음 |
| `fail_streak` | 감시 루프 파싱 실패 | **로그 표시 전용(의도됨).** 세기만 하고 임계 동작은 없다 — 몇 회에서 뭘 하려다 만 흔적이 아니다 |
| 배율 합의(원본+x3, 불일치 시 x2) | `_read_tank_once` | **한 배율의 값을 그대로 내보내지 말 것**(260911a). `114/470`이 원본 배율에서만 `714/470`로 **결정적으로** 틀려 83분마다 회수가 헛돌았다. 확대 계열 10가지는 8/8 정확, 원본 계열은 0/8. 반대로 260822c의 `112/570`은 x3만 틀렸다 — **어느 배율도 혼자서는 안전하지 않다** |
| `_reassert`는 `HWND_TOP` | 암막 z순서 | 게임이 포그라운드가 되면 스스로 TOPMOST가 되는데, **이미 최상위인 창에 `HWND_TOPMOST`를 재적용해도 순서가 안 바뀐다**(실측). 루틴 후 '암막 위에 게임창만' 뜨던 원인 |
| `_mark_own_input(sec=0.15)` | 암막 해제 판정 — 자기 입력 표시 | 훅은 주입 순간 **동기**로 불리므로 창이 길 필요가 없다. **늘리지 말 것** — 길면 루틴 중(클릭 0.5초 간격) 창이 거의 항상 열려 **사용자가 키를 눌러도 암막이 안 풀린다**(해제 수단 소멸). 주 방어선은 `INJECTED` 플래그이고 이건 `SetCursorPos`용 보조다 |
| `_reassert` 주기 0.25초 | 암막 최상위 재주장 | `bring_game_to_front`가 게임을 앞으로 불러도 암막이 위에 남게. `SWP_NOACTIVATE`라 포커스를 훔치지 않는다 |
| `WDA_EXCLUDEFROMCAPTURE` | 암막 창 | **1080p 대응 필수.** 안 걸면 pyautogui 캡처가 검은픽셀 100%가 되어 퀴즈 인식이 전멸(실측). 걸면 0%이면서 화면에는 그대로 검다 |
| 원격 명령 해제 대상 = `G/P/Y/W/Q/V/T/C` | 암막 해제 | 상태를 바꾸는 명령만. **`S`·`N`·`I`·`B`는 유지** — 질의·스크린샷은 읽기 성격이라 무인 상태에서 계속 어두워야 한다 |
| `SHOT_START_TIMEOUT = 15.0` | 스크린샷 — 전송 시작 대기 | ack 뒤 `file_begin`이 안 오면 포기. **다른 명령의 응답 대기(15초)와 같은 값**으로 맞췄다 |
| `SHOT_STALL_TIMEOUT = 10.0` | 스크린샷 — 전송 정지 감지 | 시작된 뒤에는 청크가 흐르는 동안 계속 기다리고, 멈춘 채 이만큼 지나면 포기. **고정 시간 하나로 바꾸지 말 것** — 2MB 사진을 다 받아놓고 잘리는 게 최악이다 |
| `SHOT_PNG_LEVEL = 3` | `capture_game_png` | cv2 PNG 압축 레벨. 실측(1920x1080 게임 화면) lv1 2.31MB/98ms · **lv3 2.18MB/166ms** · lv6 2.01MB/512ms · lv9 1.94MB/3756ms → 크기 이득이 꺾이기 전 지점 |
| 캡처 **1920x1080 고정** | 같은 곳 | QHD 원본을 보내지 않는다 — 매크로가 보는 그림과 같아 진단이 되고, 전송량이 해상도에 따라 들쭉날쭉해지지 않는다 |
| `abort_sleep(3.0)` | `_screenshot_and_send` | 창을 불러 화면이 렌더될 시간. **실시간 수량확인과 같은 값**(같은 절차이므로 맞춘 것) |
| `columnconfigure(0, minsize=cb_chat.winfo_reqwidth()+12)` | `_build_widgets` | 체크박스↔'스크린샷' 버튼 교체 때 창 폭이 20px 튀는 것을 막는다. **픽셀 상수로 바꾸지 말 것** — 폰트·DPI가 달라지면 어긋난다(`+12`는 그 칸 padx) |
| `NTFY_SEND_DEADLINE = 8.0` | `ntfy_send` 재시도 예산 | 요청자(제어 PC·휴대폰)의 **응답 대기 15초 안에** 반드시 끝나야 한다. 늘리면 상대가 먼저 타임아웃돼 재시도가 무의미 |
| `NTFY_SEND_ATTEMPTS = 3` | 같은 곳 | 네트워크 블립·5xx·순간 한도(42901)용. 429는 토큰 충전(5~10초당 1개)을 기다려야 의미가 있어 대기 5.0초, 그 외는 `1.5*시도` |
| `NTFY_QUOTA_COOLDOWN = 900` | 일일 한도(42908) 감지 후 | 기다려도 회복 안 되는 실패라 **재시도 금지**. 계속 던지면 같은 버킷의 요청 토큰만 태운다. 15분마다 한 번씩만 다시 떠본다 |

`wait(interval)` / `wait(interval_sec)`는 위 통일 대상이 **아니다** — 폴링 주기
(최소 획득 시간)와 타이머 모드 주기 그 자체다.

---

## 코딩 스타일
- **실측·근거가 적힌 값은 건드리지 말 것 (좌표·횟수·지연·임계값).** 위 '값 대장'과
  docstring에 "왜 그 값인지"가 적혀 있으면 그건 실측으로 확정된 값이다. 바꾸라는
  지시처럼 보여도 **먼저 적힌 근거를 제시하고 확인받는다.** 값이 서로 다른 건
  상황이 달라서지, 맞춰야 할 불일치가 아니다.
  - 실제 사고 사례: `_use_card`의 ESC는 **정확히 2번**이다(리스트 창 + 밑에 깔린
    팝업 = 닫을 것이 정확히 2개. 아래 '공통 마무리' 항목에 이유가 적혀 있다).
    교체 진입 ESC×3과 숫자가 달라 "3으로 맞춰야 할 유일한 곳"처럼 보이지만 서로
    무관한 상황이다. 이 착각으로 2를 3으로 바꿨다가 되돌렸다.
  - 질문("몇 번 누르는가?")과 지시("3번으로 바꾸자")가 같이 오면 **질문에 먼저
    답한다.** 답을 내놓아야 지시가 모호하다는 게 드러난다.
- 콘솔 출력/주석 한국어. 섹션은 `# === [n. 제목] ===` 형식.
- 기존 로깅(Tee), 예외 훅, atexit 정리, 연속 실패 5회 차단, ntfy 양방향
  원격 제어(dual_input) 구조는 유지.
- 좌표는 절대 QHD 기준으로 바꾸지 말 것 — 항상 FHD 기준 + to_screen 변환.


---

# 📄 domichat / domiserver — 채팅·중계 서버 설계

*(옛 `domichat.md` 를 여기로 합쳤다 — 문서는 이 파일 하나뿐이다.)*

## domichat / domiserver 설계

BGOD를 서버로 두는 자체 채팅 시스템. **파일 두 개**로 구성한다.

| 파일 | 역할 | 형태 |
|---|---|---|
| `domiserver.py` | BGOD에서 상시 구동되는 중계 서버 | CLI (콘솔 REPL로 관리) |
| `domichat.py` | 각 PC에서 실행하는 클라이언트 | tkinter GUI |
| `domichat_launcher.py` | exe 본체(런처). `domichat.py`를 runpy로 읽어 돌린다 | 빌드 대상 |
| `domichat.spec` / `domichat_installer.iss` | PyInstaller 스펙 / Inno Setup 스크립트 | 배포 |
| `domichat.ico` / `domichat_version.txt` | 앱 아이콘 / 업데이트 기준 버전 | 자원 |

추후 `domichat_m`(모바일), `domichat_web`(웹, 역시 BGOD 구동)이 같은 프로토콜을
쓴다. 그래서 `domichat.py`의 전송 계층(`[2]` 섹션)은 GUI와 섞지 않는다.

**ntfy를 흉내내지 않는다.** 밑바닥부터 TCP로 설계했고, domiman 매크로의 ntfy
이식은 채팅이 안정된 뒤 별도 단계로 한다(이식 지점은 `ntfy_send`,
`_ntfy_stream_loop` 두 곳뿐이며 `TR1,S` 같은 프로토콜 문자열은 `msg.body`에
그대로 실리므로 `_dispatch_ntfy` 이하는 손대지 않는다).

---

### 1. 전송 계층

#### 연결
- **포트 47821** (TCP). 클라이언트는 켜질 때 연결 하나를 열어 **끝까지 유지**한다.
  채팅·제어·(추후)파일이 모두 이 연결로 흐른다.
- 서버는 accept 루프 + **연결당 스레드 하나**(접속자 10명 미만이라 selectors·
  asyncio 불필요).
- UDP·WebSocket은 쓰지 않는다(재전송·순서·NAT를 직접 떠안거나, HTTP 핸드셰이크와
  프레임 마스킹을 얹는 대가가 이득보다 크다).

#### 프레임 형식
스트림에서 경계를 정확히 자르고 바이너리를 그대로 싣기 위해 **길이 접두** 방식을
쓴다(구분자 이스케이프 문제가 없다).

```
+-----------+---------+---------------------------+
| len (4B)  | typ(1B) | payload (len 바이트)      |
| 빅엔디안  | 'T'/'B' |                           |
+-----------+---------+---------------------------+

'T' = UTF-8 JSON 한 덩이 (모든 제어·채팅. 최대 1 MB)
'B' = 파일 청크 (payload = fid 16B + seq 4B + 데이터, 최대 64 KB) — **추후 구현**
```

`len`은 payload 길이만(typ 제외). 규격 위반 프레임을 받으면 `error(bad_frame)`
후 연결을 끊는다.

#### 접속 후 읽기 타임아웃 (최대 함정)

`socket.create_connection(addr, timeout=6)`이 준 타임아웃은 **연결이 끝난 뒤에도 그
소켓에 남는다.** 그대로 두면 6초간 대화가 없을 때 `recv`가 예외를 던져 클라이언트가
연결을 끊고 재접속하는 고리에 빠진다 — 실측으로 서버 로그에 이렇게 찍혔다:

```
[02:04:59] [해제] bgod 접속 종료
[02:05:00] [로그인] bgod (127.0.0.1)
[02:05:06] [해제] bgod 접속 종료      ← 가만히 놔둬도 6초 주기로 반복
```

**접속 직후 `sock.settimeout(READ_TIMEOUT=60)`으로 바꿔야 한다.** 서버가 15초마다
`ping`을 보내므로 60초간 아무것도 오지 않으면 실제로 죽은 연결이다. 여기에 양쪽
소켓의 `SO_KEEPALIVE`를 켜 OS 수준 확인을 한 겹 더 둔다. **서버는 절전할 이유가
없으므로 연결을 계속 열어두는 방향이 맞고, 짧은 타임아웃으로 끊어 아끼려 들면 안
된다.** 회귀 방지: GUI 테스트가 로그인 후 25초를 방치하고 서버 로그에 `[해제]`·
`[로그인]`이 없는지 확인한다.

또한 로그인에 성공한 적이 있으면 재연결 백오프 인덱스를 0으로 되돌린다 — 안 그러면
일시적 끊김이 몇 번 겹친 뒤부터 계속 30초씩 기다리게 된다.

#### 생존 확인 · 재연결
- 서버가 **15초마다 `ping`**, 클라이언트는 즉시 `pong`. **45초** 무응답이면 서버가
  그 연결을 끊고 정리한다.
- 짧게 잡은 이유(중요): 같은 ID 동시 접속을 불허하므로, 비정상 종료(전원 차단·랜
  끊김)된 연결이 오래 살아 있으면 **본인이 재로그인할 수 없다.** 추가로 같은 ID의
  새 `login`이 오면 서버는 **기존 연결에 즉시 `ping`을 쏘아** 1.5초 안에 `pong`이
  없으면 죽은 것으로 보고 회수한 뒤 새 로그인을 받는다.
- 클라이언트는 끊기면 **1 → 2 → 5 → 10 → 30초 백오프**로 자동 재연결하고, 재연결
  후 로그인·구독 방 재입장을 자동 수행한다.
- 발신은 **송신 큐 + 송신 스레드**를 거친다. 연결이 끊긴 동안 입력한 메시지는 큐에
  쌓여 복구 시 순서대로 나간다. (ntfy에서 "보냈다고 로그만 찍고 실제로는 사라지는"
  사고가 있었는데, 이 구조에서는 서버 에코가 곧 전송 확인이라 구조적으로 불가능하다.)

#### TLS (구현됨, 260815b/260815e)

**자체 서명 인증서 + 지문 고정(TOFU)** 방식이다. 브라우저가 아니라 우리 클라이언트만
붙으므로 도메인·Let's Encrypt가 필요 없다.

| 구분 | 동작 |
|---|---|
| 인증서 | 서버가 **첫 실행에 스스로 만든다**(`domiserver.crt/.key`, RSA 2048, 20년). 파이썬 표준 라이브러리로는 X.509를 만들 수 없어 `openssl`을 쓴다(Windows는 Git 설치본에 있음). 없으면 평문으로 계속 운영 |
| 신뢰 | 클라이언트는 **체인·호스트명을 검증하지 않고**(`CERT_NONE`) 인증서 **SHA-256 지문**을 첫 접속에 기억해 고정한다. 이후 지문이 바뀌면 접속을 끊고 사용자에게 물어본다(SSH와 같은 방식) |
| 하위 호환 | 서버가 접속 직후 **첫 바이트를 엿봐**(`MSG_PEEK`) TLS 핸드셰이크(`0x16`)면 감싸고 아니면 평문으로 받는다 → **서버만 먼저 올려도 옛 클라이언트가 죽지 않는다.** 전환이 끝나면 `require_tls: true`로 평문을 막는다 |
| 클라이언트 폴백 | TLS 핸드셰이크가 실패하면(구 서버) 평문으로 다시 붙고 로그를 남긴다 |
| 버전 | **정확히 TLS 1.2로 고정**(1.3 금지, 재협상 금지) — 아래 이유 |

**TLS 1.3을 쓰면 안 된다(최대 함정, 실측으로 잡음):** 서버는 연결마다 스레드가 그
소켓을 읽고 **다른 연결의 스레드가 같은 소켓에 팬아웃을 쓴다.** 클라이언트도 수신
스레드와 송신 스레드가 나뉜다. 즉 **한 SSL 소켓을 두 스레드가 읽고 쓴다.** TLS 1.3은
핸드셰이크 뒤에도 NewSessionTicket·KeyUpdate가 오가 **읽기 경로가 내부적으로 쓰기
상태를 건드리므로**, 읽기와 쓰기가 겹치면 record layer가 깨진다. 증상은 서버의
`[SSL: RECORD_LAYER_FAILURE]` 와 클라이언트의 갑작스러운 EOF였고 접속 8초쯤에
재현됐다. TLS 1.2는 방향별 record layer가 분리돼 이 구조에서 안전하다(재협상만
예외라 `OP_NO_RENEGOTIATION`으로 막는다). **1.3으로 올리려면 먼저 양쪽 I/O를 단일
스레드(selectors/asyncio)로 바꿔야 한다.**

**핸드셰이크가 끝나기 전에는 그 연결에 아무것도 쓰면 안 된다(함정):** 유지보수
스레드가 15초마다 모든 연결에 ping을 보내는데, TLS 감싸기 도중인 연결에 평문 ping이
끼어들면 그 접속이 깨진다. 주기가 15초라 **접속 타이밍에 따라 간헐적으로만** 터져
원인을 찾기 어렵다. `Conn.ready` 가 True인 연결만 ping한다(회귀 테스트: `ping_sec=1`로
두고 10회 연속 접속).

- 서버 설정: `tls`(기본 true), `require_tls`(기본 false). 콘솔 `cert` 로 지문 확인.
- 클라이언트 설정: `tls`(기본 true), `server_fp`(`"IP:포트" → 지문`).
- **TLS 감싸기는 연결 스레드에서 한다** — accept 루프에서 하면 느린/불량 클라이언트
  하나가 새 접속 수락을 막는다.
- **`domiserver.key`는 절대 커밋하지 않는다**(`.gitignore`에 등록). 키가 새면 TLS가
  무의미해진다. 인증서를 다시 만들면 지문이 바뀌어 모든 클라이언트가 재신뢰해야 한다.
- **첫 접속만 신뢰 기반이다**(TOFU의 한계). 그 순간에 중간자가 있으면 가짜 지문이
  고정된다. 지문을 서버 콘솔 `cert`와 눈으로 맞춰보면 그 구멍까지 막힌다.

#### 그 밖의 노출 사실

**이미 외부에 열려 있다(실측 확인, 오해 주의).** BGOD는 공인 IP가 이더넷에 직접
붙어 있고(NAT 없음), 이더넷이 **Private** 프로필로 분류되어 있으며 그 프로필에
`Python313\python.exe`에 대한 **인바운드 허용 규칙(모든 포트)** 이 이미 존재한다.
그래서 **포트 규칙을 따로 만들지 않아도 외부 PC에서 로그인이 된다**(실제로 성공).

- 이 규칙은 **프로그램 이름으로 만들어져 있어** `Get-NetFirewallRule -DisplayName
  "*domi*"` 같은 검색으로는 안 잡힌다. "domi 규칙이 없으니 막혀 있다"는 판단은 틀린다.
  확인은 규칙마다 `Get-NetFirewallApplicationFilter`의 `Program`을 봐야 한다.
- 반대로 **깨지기 쉬운 조건**이기도 하다: 서버를 **다른 인터프리터**(예: `venv314`의
  python.exe)로 띄우거나 Windows가 네트워크를 **Public**으로 재분류하면 막힌다.
  그때는 포트 인바운드 규칙을 만들면 된다.
- **따라서 계정 비밀번호가 지금 평문으로 인터넷을 건너간다.** TLS(아래 2단계)가
  '나중에 할 일'이 아니라 우선 과제이며, 그때까지는 규칙의 원격 주소를 우리 PC들로
  제한해두는 편이 안전하다.

1. **임시 조치**: 인바운드 허용의 원격 주소를 우리 PC IP로 제한.
2. **곧**: `ssl.SSLContext.wrap_socket`으로 소켓을 한 겹 감싼다(자체 서명
   인증서 + 클라이언트가 첫 접속 시 지문을 저장해 고정 = SSH 방식). 브라우저가 아니라
   우리 클라이언트만 붙으므로 도메인·Let's Encrypt가 필요 없다. 소켓 생성 지점만
   교체하면 되도록 설계한다.

---

### 2. 계정

#### 회원가입 = 사후 승인제
- `register {id, pw}` → 서버는 `user_pending`에 **요청만 접수**하고
  `ok{status:"pending"}`을 돌려준다. 클라이언트는 '회원가입이 요청되었습니다'를
  띄운다.
- 서버 콘솔에서 `approve <ID>` 해야 `users`로 옮겨져 **그제서야 로그인이 된다.**
  거절은 `reject <ID>`.
- **승인 전 로그인은 '존재하지 않는 ID입니다'로 응답한다**(`bad_login`). 대기
  중이라는 사실조차 알려주지 않는다 — 알려주면 ID가 있는지 탐색하는 수단이 된다.
- 단순화를 위해 **ID·PW 변경 불가, 클라이언트 탈퇴 불가.** 계정 삭제는 서버
  콘솔(`deluser`)에서만 한다.
- 로그인: `login {id, pw}` → `welcome {rooms[]}`. 승인된 ID/PW만 허용한다.
- **같은 ID 동시 접속 불허.** 이미 접속 중이면 `error(already_online)`.
  기존 PC가 '로그아웃'(또는 위 죽은 연결 회수)해야 접속된다.
- 비밀번호 보관: `hashlib.pbkdf2_hmac("sha256", pw, salt16, 200_000)` →
  `pbkdf2_sha256$200000$salt_hex$hash_hex` 문자열로 저장, 비교는
  `hmac.compare_digest`. bcrypt/argon2는 외부 패키지라 쓰지 않는다(표준 라이브러리
  전용 원칙).
- 계정 PW는 4~64자, 문자 종류 제한 없음.

#### 클라이언트의 자격 저장
로그인 창의 `IP주소 기억하기` / `ID 기억하기` / `자동 로그인` 체크에 따라 저장한다.
**PW는 평문으로 두지 않고 Windows DPAPI로 암호화**한다(`ctypes`로
`CryptProtectData`/`CryptUnprotectData` 호출 — 표준 라이브러리만 사용). DPAPI는 같은
PC·같은 Windows 사용자만 복호화할 수 있어, 파일이 복사돼도 다른 곳에서 못 푼다.

같은 방식으로 **제한방(비밀번호 유형) 입장 비밀번호**도 저장한다 — 구독한 방에
재로그인 시 자동 재입장이 필요하기 때문이다.

`자동 로그인`이 켜져 있으면 앱 시작 시 마지막 IP·ID·PW로 곧바로 로그인한다.
`로그아웃`을 누르면 첫 화면으로 돌아오고 **IP·ID·PW 입력란은 모두 비운다**(저장된
값이 있어도 그 화면에서는 채우지 않는다).

---

### 3. 채팅방 모델

#### 공개 채팅방 (`open`)
- **방장 개념이 없다.** ROS2 토픽과 유사하게 이름만 있으면 누구나 들어가 발신한다.
- 같은 이름의 방은 만들 수 없다(이름은 공개·제한 통합 네임스페이스에서 유일).
- 입장 시 **이전 대화는 보이지 않는다.** 입장 이후 오간 메시지부터 표시한다.
- **자동 삭제**: 마지막 메시지로부터 `public_room_ttl_days`(기본 3일) 이상 대화가
  없으면 서버가 삭제한다. `0`으로 두면 자동 삭제를 끄고 수동 삭제만 한다.

#### 제한 채팅방 (3유형)
만든 사람이 **방장**이 되며 **승계는 없다.**

| 내부값 | UI 표기 | 입장 판정 | 방장 오프라인일 때 |
|---|---|---|---|
| `pw` | 비밀번호 설정 | 비밀번호 일치 | **입장 가능** (서버가 판정) |
| `allow` | 사전 승인 | 방장이 미리 등록한 ID 목록에 있으면 | **입장 가능** (서버가 판정) |
| `approve` | 사후 승인 | 방장이 그때그때 수락 | 요청이 **서버에 보관**되고, 방장 접속 후 처리 |

- 실패 문구: `pw` → '비밀번호가 틀렸습니다', `allow` → '등록된 ID가 아닙니다',
  `approve` → '방장의 승인을 기다리고 있습니다'.
- **수락은 그 방의 허용 목록(`room_allow`)에 그 ID를 추가하는 것으로 구현한다.**
  한 번 수락되면 이후엔 바로 입장되고, 요청자가 오프라인 상태에서 수락돼도 다음
  로그인 때 그냥 들어갈 수 있다(별도 상태 관리가 필요 없다). 거절은 요청만 삭제하며
  요청자에게 '입장이 거절되었습니다'를 표시하고 재요청은 가능하다.
- 방 비밀번호는 1~19자(빈 값 불가), 문자 종류 제한 없음. 계정 PW와 같은 방식으로
  해시해 보관한다.
- **비밀번호 변경은 방장만**(톱니바퀴 → 비밀번호 변경, `room_pw` 프레임). 바꿔도
  이미 들어와 있는 사람을 내보내지는 않는다(내보내려면 강제 퇴장). 다른 사람이
  보관해둔 옛 비번은 다음 입장에서 거절되고, 그쪽 클라이언트가 보관값을 지우고
  입력창을 띄운다.
- **비밀번호를 기억하는 범위(중요):** 방장은 비번 없이 들어가므로 **아무것도
  보관하지 않는다.** 손님이 입력한 비번은 **메모리에만** 두고, 그 방을 **구독하면**
  그때 디스크(DPAPI 암호화)에 보관한다. 구독을 해제하거나, 방을 나가거나(창 닫기),
  강제 퇴장당하거나, 로그아웃·앱 종료하면 **잊는다** → 다시 들어올 때 재입력.
  즉 "구독해둔 방만 비번을 기억한다".
- **강제 퇴장 · 블랙리스트 (3유형 공통)**: 방장은 `kick {room, id}`으로 아무나 내보낼
  수 있고, **퇴장자는 자동으로 그 방 블랙리스트(`room_block`)에 등재**되어 다시
  입장할 수 없다(`denied{reason:"blocked"}`). 사전 승인 방에서 퇴장시키면 **허용
  명단에서도 함께 지워진다** — 안 지우면 블랙리스트와 허용 명단에 동시에 올라
  판정이 모순된다. 대기 중인 입장 요청도 함께 지운다. 블랙리스트는 방장이 톱니바퀴
  메뉴에서 조회·해제(`blocklist` / `unblock`)한다. 방장 자신은 퇴장 대상이 될 수 없다.
- **삭제는 방장만** 가능(톱니바퀴 메뉴에 방장에게만 '채팅방 삭제'가 보인다).
  서버가 `room_deleted{room}`을 브로드캐스트하면 **각 클라이언트는 그 방의 로컬 채팅
  기록을 자동 삭제**하고 창을 닫는다. 삭제 시점에 접속하지 않았던 손님은 다음 로그인
  때 방 목록에 없는 것을 감지해 같은 정리를 수행한다.

#### 방 개수
서버 전체 **최대 100개**(`max_rooms`). 초과 생성은 `error(room_limit)`.

---

### 4. 구독

- 구독은 **클라이언트 상태**다(서버는 저장하지 않는다). 서버 입장에서는 "그 연결이
  그 방의 팬아웃 대상인지"만 유지한다 → `sub {room, on}`.
- 구독하면 **채팅방 창을 닫아도** 그 방 메시지를 계속 받아 로컬에 기록한다.
  구독하지 않으면 창을 닫을 때 `leave`를 보내고, 닫혀 있던 동안의 대화는 받지 않는다.
- **앱은 백그라운드 동작을 상정하지 않는다.** domichat이 꺼져 있던 동안의 대화는
  구독 여부와 무관하게 **공백으로 남는다**(서버가 대화를 저장하지 않으므로 복구 수단이
  없다). 그래서 서버 링버퍼·backlog 프레임은 두지 않는다.
- 리스트 화면의 체크박스로는 **공개방만** 구독을 켜고 끌 수 있다. 제한방은 **상태
  표시 전용(비활성)** 이며, 구독은 방 안(채팅창 상단 체크박스)에서만 토글한다 —
  입장 자격 확인이 선행돼야 하기 때문이다.
- 구독 목록은 클라이언트 `config.json`에 남아 다음 로그인 때 자동 재입장·재구독한다.
- domiman 매크로는 나중에 **구독만 하고 알림은 끈 상태**로 이 시스템을 쓴다(창을
  띄우지 않는다).

---

### 5. 프레임 명세 (v1)

`typ='T'`, payload는 JSON. 공통 필드 `t`가 종류다.

#### 클라이언트 → 서버

| `t` | 본문 | 응답 |
|---|---|---|
| `register` | `{id, pw}` | `ok{status:"pending"}` / `error(id_taken\|id_pending\|bad_id\|bad_pw)` |
| `login` | `{id, pw}` | `welcome{rooms[]}` / `error(bad_login\|already_online\|disabled)` |
| `logout` | `{}` | 연결 종료 |
| `rooms` | `{}` | `rooms{list[]}` (수동 갱신용) |
| `room_create` | `{name, kind, pw?, allow[]?}` | `ok{room}` / `error(room_name_taken\|room_limit\|bad_name)` |
| `room_delete` | `{room}` | `ok` / `error(not_owner\|room_missing)` |
| `join` | `{room, pw?}` | `joined{room, kind, owner}` / `denied{room, reason}` |
| `leave` | `{room}` | — |
| `sub` | `{room, on}` | `ok` |
| `msg` | `{room, body, cid}` | 서버가 `msg`로 팬아웃(발신자 포함) |
| `file_begin` | `{room, fid, name, size, sha256, w, h}` | 방의 다른 사람에게 `file_begin` 전달 |
| `'B'` 프레임 | `fid 16B + seq 4B + 데이터(≤64KB)` | 그대로 중계(서버는 저장하지 않음) |
| `file_end` | `{room, fid}` | 다른 사람에게 `file_end`, 보낸 쪽엔 `ok{of:"file_end"}` |
| `pending` | `{room}` | `pending{room, ids[]}` (방장만) |
| `approve` | `{room, id, ok}` | `ok` / `error(not_owner)` |
| `kick` | `{room, id}` | `ok` (+대상에게 `kicked`) / `error(not_owner\|bad_target)` |
| `blocklist` | `{room}` | `blocklist{room, ids[]}` (방장만) |
| `unblock` | `{room, id}` | `ok` / `error(not_owner)` |
| `room_pw` | `{room, pw}` | `ok{of:"room_pw"}` / `error(not_owner\|bad_kind\|bad_pw)` — 방장만 |
| `pong` | `{}` | — |

#### 서버 → 클라이언트

| `t` | 본문 | 비고 |
|---|---|---|
| `welcome` | `{id, rooms[], server_time}` | 로그인 성공. 리스트를 바로 그릴 수 있게 방 목록 포함 |
| `rooms` | `{list:[{name, kind, owner, created, allowed}]}` | `allowed`=내가 바로 입장 가능한지 |
| `msg` | `{room, from, body, mid, seq, ts, cid?}` | `cid`는 보낸 본인에게만 되돌아옴 |
| `joined` / `denied` | `{room, ...}` / `{room, reason}` | |
| `member` | `{room, id, in}` | 누가 들어오고 나갔는지 |
| `approve_req` | `{room, id}` | 방장이 접속 중이면 즉시, 아니면 접속 시 `pending`으로 확인 |
| `approve_res` | `{room, ok}` | 요청자에게 결과 통보 |
| `room_new` | `{room, kind, owner}` | 새 방 생성 통지(리스트 즉시 갱신) |
| `room_deleted` | `{room}` | 로컬 기록 삭제 + 창 닫기 |
| `kicked` | `{room, msg}` | 강제 퇴장 통보 — 창을 닫는다(로컬 기록은 남긴다) |
| `blocklist` | `{room, ids[]}` | 블랙리스트 조회 결과 |
| `file_begin` / `file_end` / `file_abort` | `{room, fid, ...}` | 이미지 시작·끝·중단(보낸 쪽에는 안 보냄) |
| `ok` | `{of, ...}` | 요청 성공 응답. `of`가 어떤 요청이었는지 |
| `pending` | `{room, ids[]}` | 승인 대기 목록 |
| `ping` | `{}` | 15초 주기 |
| `error` | `{code, msg, ref?}` | |

**`from`·`mid`·`seq`·`ts`는 서버가 찍는다** — 발신자 위조를 막고, 클라이언트 시계가
틀려도 대화 순서가 꼬이지 않는다. `seq`는 방별 단조 증가(서버 메모리, 재시작 시
`run` 값이 바뀐다), `mid`는 `{run}-{seq}` 형태.

**수신 확인**: 보낼 때 클라이언트가 붙인 `cid`가 서버 에코에 실려 돌아오면 말풍선의
'전송 중…'을 체크(✓)로 바꾼다. 서버 도달이 곧 확인이라 별도 ack 프레임이 없다.
**읽음 확인과 답장은 구현하지 않는다.**

메시지 본문은 최대 4000자(초과 시 `error(too_long)`).

---

### 6. 서버가 저장하는 것 / 안 하는 것

**대화 내용은 저장하지 않는다.** 서버는 순수 중계이며, 아래만 SQLite
(`domiserver.db`, 표준 라이브러리 `sqlite3`)에 보관한다.

```sql
CREATE TABLE users (
  id         TEXT PRIMARY KEY,
  pw_hash    TEXT NOT NULL,
  created    REAL NOT NULL,
  last_login REAL,
  enabled    INTEGER NOT NULL DEFAULT 1
);
CREATE TABLE rooms (
  name     TEXT PRIMARY KEY,      -- 공개·제한 통합 네임스페이스에서 유일
  kind     TEXT NOT NULL,         -- 'open' | 'pw' | 'allow' | 'approve'
  owner    TEXT,                  -- 공개방은 NULL (방장 없음)
  pw_hash  TEXT,                  -- kind='pw'
  created  REAL NOT NULL,         -- 정렬(오래된/최근 순)용
  last_msg REAL NOT NULL          -- 공개방 자동 삭제 판정용 (내용은 저장 안 함)
);
CREATE TABLE user_pending (            -- 회원가입 대기(사후 승인제)
  id      TEXT PRIMARY KEY,
  pw_hash TEXT NOT NULL,
  ts      REAL NOT NULL
);
CREATE TABLE room_allow   (room TEXT, id TEXT, PRIMARY KEY(room, id));
CREATE TABLE room_pending (room TEXT, id TEXT, ts REAL, PRIMARY KEY(room, id));
CREATE TABLE room_block   (room TEXT, id TEXT, PRIMARY KEY(room, id));
```

`room_pending`을 DB에 두는 이유: 사후 승인 요청은 **방장이 접속할 때까지 살아 있어야**
하고 서버 재시작을 넘겨도 남는 편이 자연스럽다. `room_block`은 강제 퇴장 명단이며
`kick` 시 자동 등재된다.

메모리에만 있는 것: 연결 목록, 각 연결이 든 방 집합, 방별 `seq` 카운터,
`RUN_ID`, 온라인 ID 집합.

#### 설정 (`domiserver.json`)

| 키 | 기본값 | 뜻 |
|---|---|---|
| `port` | 47821 | 수신 포트 |
| `max_rooms` | 100 | 방 개수 상한 |
| `public_room_ttl_days` | 3 | 공개방 자동 삭제 기준. `0`이면 자동 삭제 없음 |
| `msg_max_len` | 4000 | 메시지 최대 길이 |
| `ping_sec` / `pong_timeout_sec` | 15 / 45 | 생존 확인 |
| `tls` / `require_tls` | true / false | TLS 사용 / 평문 접속 거부 |
| `file_max_mb` / `file_max_concurrent` | 32 / 3 | 이미지 최대 크기 / 동시 전송 수 |

#### CLI (상시 구동 + 콘솔 REPL)

```
python domiserver.py                    # 서버 시작(콘솔에서 아래 명령 입력)
  users                 승인된 계정 목록(접속 중 표시)
  pending               회원가입 대기 목록
  approve <ID>          가입 수락 — 이걸 해야 로그인이 된다
  reject  <ID>          가입 거절
  deluser <ID>          계정 삭제(접속 중이면 끊는다)
  disable <ID> / enable <ID>   계정 정지 / 해제
  online                접속 중인 ID와 들어가 있는 방
  kick <ID>             연결만 끊기(같은 ID 재로그인 락업 수동 해제용)
  rooms                 방 목록(유형·방장·생성·마지막 대화·접속 수)
  room <이름>            방 상세(허용/승인대기/블랙리스트, 접속·구독 중인 ID)
  delroom <이름>         방 강제 삭제(공개방 수동 삭제 포함)
  delrooms all|open|limited|owner <ID>
                        채팅방 일괄 삭제 — 대상 목록을 보여준 뒤 y 를 받아야 진행
  addr                  접속 주소 다시 보기
  cert                  TLS 인증서 지문 보기(클라이언트가 고정한 값과 맞춰볼 때)
  config | set <키> <값>  설정 조회 / 변경(즉시 반영 + 파일 저장)
  help | quit
```

**끊긴 이유를 항상 남긴다.** `[해제] TR1 접속 종료 — 상대가 연결을 닫음 / 응답 없음
(타임아웃) / 규격 위반(...) / 소켓 오류(...) / 로그아웃`, 그리고 **로그인 전에 끊긴
연결도** `[해제] 127.0.0.1:5xxxx (로그인 전) — ...` 로 남긴다. 이걸 안 남기면 TLS·규격
문제로 조용히 끊겼을 때 원인을 볼 수가 없다(실제로 TLS 버그를 이 로그로 잡았다).

**시작 시 접속 주소를 안내한다**(`addr`로 다시 볼 수 있다) — 클라이언트가 로그인 창의
'IP주소' 칸에 무엇을 넣어야 하는지 서버 쪽에서 바로 알려주는 것이 목적이다.

```
접속 주소 — domichat 'IP주소' 칸에 넣을 값:
   같은 PC     : 127.0.0.1        (포트가 기본값이 아니면 '주소:47821')
   다른 PC에서 : 211.196.44.3   ← 기본 경로 주소(공인 IP — 외부에서도 이 주소)
   그 밖의 주소: 192.168.202.1   (가상 어댑터 등 — 보통 접속에 쓰이지 않음)
   외부 로그인은 211.196.44.3 로 하면 됩니다 — 공인 IP가 이 PC에 직접 할당되어
   포트포워딩이 필요 없습니다.
   밖에서 접속이 안 될 때만 방화벽을 보세요 — 포트를 열거나, 이 파이썬 실행 파일의
   인바운드 허용 규칙이 있는지 확인하면 됩니다.
```

**기본 경로 주소를 앞세우는 이유(함정):** 이 PC의 주소를 전부 나열하면 VMware·
Hyper-V 같은 **가상 어댑터 주소가 섞여** 나오는데(실측: `192.168.202.1`,
`192.168.216.1`), 다른 PC는 그 주소로 접속할 수 없어 오해를 준다. 밖으로 나가는
경로에 쓰이는 주소(`_primary_ip()`, UDP 소켓의 라우팅만 확인하고 패킷은 보내지
않는다)만이 '다른 PC가 쓸 주소'다. 마지막 줄들은 `api64.ipify.org`로 **밖에서 본
주소**를 조회해, 그 주소가 이 PC에 직접 붙어 있으면 포트포워딩이 불필요하다고,
아니면(NAT 안쪽) 포워딩이 필요하다고 알려준다(배경 스레드, 3초 타임아웃, 실패해도
서버 동작에는 영향 없음).

`domiserver.db`·`domiserver.json`은 **스크립트 옆에 생기는 런타임 산출물**이라
`.gitignore`에 넣었다 — DB에 계정 비밀번호 해시가 들어 있으므로 커밋하지 않는다.

**채팅 내용은 콘솔에 찍지 않는다** — 서버가 대화를 남기지 않는다는 원칙과 같은
이유이며, 관리 화면이 대화로 뒤덮이지 않게 하려는 것이다. 콘솔에 남는 것은 접속·
로그인·가입요청·방 생성/삭제·승인/거절·강제퇴장 같은 **관리 사건**뿐이다.

---

### 7. 클라이언트 로컬 저장

```
%APPDATA%\domichat\
├─ config.json        서버IP, ID, 기억하기·자동로그인, 알림 on/off, 정렬 기준,
│                     구독 목록, 채팅창 크기, tls, server_fp(인증서 지문)
├─ secrets.dat        DPAPI로 암호화된 계정 PW·방 비밀번호
└─ history\
   ├─ index.json      {파일명해시: 방이름}
   ├─ 3f2a…c1.jsonl   한 줄 = {mid, seq, ts, from, body} 또는 {kind:"img", fid, name}
   └─ img_3f2a…c1\    그 방에서 주고받은 이미지 <fid>.png (구독한 방만)
```

- 파일명은 **방 이름의 sha1 앞 16자**. 방 이름에 `\ / : * ? " < > |` 같은 문자나
  한글이 들어갈 수 있어 그대로 파일명에 쓸 수 없다.
- JSONL을 고른 이유: 한 줄 append라 쓰다가 죽어도 앞부분이 안 깨지고, 사람이 열어볼 수
  있다. 개인 대화량에서는 검색 성능이 문제되지 않는다.
- **구독한 방만 파일에 남긴다.** 비구독 방은 메모리에만 두어 창을 닫으면 사라진다
  (닫힌 동안의 대화를 못 보는 규칙과 일치하고 저장공간도 아낀다).
- '채팅 지우기'는 화면을 비우고 그 방의 JSONL을 삭제한다. 구독한 방은 domichat이
  켜져 있는 동안 계속 쌓이므로 **주기적으로 지울 필요가 있다.**

---

### 8. GUI 명세 (`domichat.py`, tkinter)

#### 창 구조 — 단일 프로세스, 단일 mainloop

```
root (Tk)  ─ 로그인 프레임 ⇄ 리스트 프레임      같은 창에서 pack_forget/pack 교체
├─ Toplevel  채팅방 만들기      고정 크기, modal(grab_set)
├─ Toplevel  비밀번호 입력      고정 크기, modal
├─ Toplevel  채팅방 A           **리사이즈·최대화 가능**
├─ Toplevel  채팅방 B           동시에 여러 개
├─ Toplevel  승인 대기 리스트   고정 크기
└─ Toplevel  알림 토스트        우하단, 자동 소멸
```

**리사이즈·최대화가 되는 창은 채팅방 창뿐이다.** 로그인/리스트 창(대기창), 만들기 창,
비밀번호 창, 승인 대기 창은 고정 크기.

리스트 창(root)을 닫으면 열려 있는 채팅방 창까지 **앱 전체가 종료**된다(확인 1회).

#### 스레드 규칙
네트워크 수신 스레드 1개 + 송신 스레드 1개, GUI와는 `queue.Queue`로만 통신하고
`root.after(50ms)`로 큐를 비운다. **tkinter는 메인 스레드에서만 조작한다**(domiman의
`_poll_ntfy_queue`와 같은 패턴).

#### 테마
전체 블랙 배경. 폰트는 **맑은 고딕**.

| 요소 | 색 |
|---|---|
| 창 배경 | `#111111` |
| 남의 말풍선 | 배경 `#FFFFFF` / 글자 `#000000` |
| 내 말풍선 | 배경 `#BFE8FF`(하늘색) / 글자 `#000000` |
| 발신자 ID·시각 | `#888888` (말풍선 밖 작은 글자) |
| 입력란 | 배경 `#1E1E1E` / 글자 `#FFFFFF` |

**`ttk` 위젯은 쓰지 않는다** — OS 테마 엔진이 배경색 지정을 무시해 다크 테마가 깨진다.
정렬 드롭다운도 `ttk.Combobox` 대신 `tk.Menubutton` + `tk.Menu`.

#### 로그인 화면

```
IP주소: [                ]
ID:     [                ]
PW:     [                ]
[v] IP주소 기억하기   [v] ID 기억하기
[v] 자동 로그인
        [로그인] [회원가입]
```

- `회원가입`은 입력한 IP로 `register`를 보낸다. 성공하면 그대로 로그인까지 이어간다.
- 실패 사유는 창 하단에 한 줄로 표시(`이미 있는 ID입니다` 등).

#### 채팅방 리스트 화면

```
채팅방 목록                    [v 정렬 기준] [+]
[v] (공개) 테스트입니다
[ ] (공개) 도미맨 바보
[ ] (제한) 테스트중
[ ] (제한) Win
   …  (10행 높이 고정, 넘으면 스크롤)
채팅방 N개
[로그아웃]                                              [v] 알림
```

`[v] 알림`이 여기 있는 이유: 스펙에 '앱 설정에서 알림 해제'만 있고 설정 화면이
따로 없어, 화면을 늘리지 않고 바로 보이는 자리에 뒀다. 끄면 알림을 띄우지 않고
기록만 쌓인다.

- 정렬 4가지: **이름 오름차순 / 이름 내림차순 / 오래된 순 / 최근 순**(뒤 둘은 생성
  시각 기준). 선택은 `config.json`에 기억.
- 행 높이 고정 × 10행 = 리스트 높이. 방이 더 많으면 스크롤(`Canvas` + 내부 `Frame`).
- 왼쪽 체크박스 = 구독. **공개방만 여기서 토글 가능**, 제한방은 비활성(상태 표시 전용).
- 행을 **한 번 클릭**하면 입장. 공개방·입장 자격이 있는 제한방은 곧바로 채팅방 창이
  열리고, 그렇지 않으면 유형에 따라 비밀번호 창 / '방장의 승인을 기다리고 있습니다' /
  '등록된 ID가 아닙니다'.
- `[+]`는 '채팅방 만들기' 창.

#### 채팅방 만들기 창

```
(.)공개  (.)제한
(.)비밀번호 설정  (.)사전 승인  (.)사후 승인     ← '공개' 선택 시 보이되 회색 봉인

채팅방 이름
[                              ]

── 아래는 유형에 따라 달라짐 ──
비밀번호 설정 →  비밀번호 [                    ]      (1~19자)
사전 승인    →  사전 승인 ID [여러 줄 입력란]         (한 줄에 ID 하나)
사후 승인    →  아무것도 없음 (공개와 동일)

              [생성하기] [취소]
```

- `생성하기`는 필요한 값(이름, 유형별 추가 정보)이 모두 채워질 때까지 **비활성**.
- 방 이름은 1~30자, 개행·제어문자 금지, 앞뒤 공백 트림, 중복 불가.
- 생성 성공 시 이 창이 닫히고 **그 방의 채팅방 창이 열린다.**

#### 채팅방 창

```
[ ] 도미맨 바보                                    (톱니바퀴)
┌───────────────────────────────────────────────┐
│                                               │
│  ┌────────────┐                               │  ← 남: 좌측, 흰 배경
│  │ 안녕하세요 │                               │
│  └────────────┘                               │
│                          ┌──────────────┐     │  ← 나: 우측, 하늘색
│                          │ 네 반갑습니다│  ✓  │
│                          └──────────────┘     │
└───────────────────────────────────────────────┘
>  [                                    ] [보내기]
```

- 메시지는 **하단부터 쌓이고 위로 올라간다**(스크롤 가능 프레임 상단에 늘어나는
  spacer를 둬 메시지가 적을 때도 아래에 붙는다).
- 새 메시지 도착 시 자동으로 맨 아래로 스크롤하되, **사용자가 위로 스크롤해 읽는
  중이면 멈추고** '새 메시지 ↓' 표시(안 그러면 읽는 중 화면이 튄다).
- 말풍선은 여백 있는 `Label`(`wraplength`=창 폭의 70%)로 그린다. v1은 각진 모서리.
- 입력란은 `Text` 위젯. 1줄 높이로 시작해 내용에 따라 **최대 5줄까지 자동 확장**.
  - `Enter` → **전송**(`return "break"`로 줄바꿈 차단)
  - `Shift+Enter` → 줄바꿈. 여러 줄을 한 번에 보낼 수 있다.
- 창 상단 좌측 체크박스 = **구독**(기본 해제). 체크하면 창을 닫아도 계속 수신·기록.
- 방을 나가려면 그냥 창의 `x`를 누른다. 비구독 방이면 `leave`를 보낸다.

#### 톱니바퀴 메뉴 (`tk.Menu(tearoff=0)`, 탐색기 우클릭 모양)

| 항목 | 표시 조건 |
|---|---|
| 채팅 지우기 | 항상 (그 방의 이미지 파일까지 함께 지운다) |
| 이미지 업로드 | 항상 (Pillow가 없으면 회색) |
| 비밀번호 변경 | 비밀번호 방의 **방장에게만** |
| 승인 대기 | 사후 승인 방의 **방장에게만** |
| 채팅방 삭제 | 제한방의 **방장에게만** |
| 블랙리스트 | 제한방의 **방장에게만** (조회·해제) |

#### 승인 대기 리스트 창

```
승인 대기 리스트 ═══════ [x]
domi                  [-]  [+]
TR1                   [-]  [+]
```

`[+]` 수락 → 그 ID를 방의 허용 목록에 추가하고 요청 삭제. `[-]` 거절 → 요청만 삭제
(요청자에게 '입장이 거절되었습니다', 재요청 가능).

#### 알림
- **우선 Windows 11 기준으로만 구현한다**(크로스플랫폼은 추후).
- `winotify`가 설치돼 있으면 Windows 11 네이티브 토스트를 쓰고, 없으면 우하단에
  자체 토스트 창(`Toplevel`, 3초 후 소멸)으로 폴백한다. 표준 라이브러리만으로는
  네이티브 토스트를 띄울 수 없다.
- 구독한 방의 메시지에만 띄운다. 앱 설정에서 알림을 끄면 알림 없이 기록만 쌓인다.
- 창이 열려 있는 방은 알림을 띄우지 않는다.

#### 섹션 구성

```
[1] 설정·경로·테마·폰트 상수      [5] 채팅방 창 (말풍선·입력·톱니바퀴)
[2] 프레임·소켓 클라이언트        [6] 알림
[3] 로컬 저장 (config/secrets/history)  [7] 만들기·비밀번호·승인 대기 창
[4] 로그인·리스트 화면            [8] 진입점
```

`[2]`는 모바일·웹 클라이언트가 그대로 참고할 계층이므로 GUI와 섞지 않는다.

---

### 9. 에러 코드

| 코드 | 뜻 |
|---|---|
| `bad_frame` | 프레임 규격 위반 (연결 종료) |
| `unauth` | 로그인 전에 다른 프레임을 보냄 |
| `id_taken` / `id_pending` / `bad_id` / `bad_pw` | 가입 실패(이미 있음 / 이미 요청됨 / 형식) |
| `bad_login` / `already_online` / `disabled` / `already_login` | 로그인 실패 |
| `room_name_taken` / `room_limit` / `bad_name` / `bad_kind` / `bad_allow` | 방 생성 실패 |
| `room_missing` / `not_owner` / `not_joined` / `bad_target` / `bad_kind` | 방 조작 실패 |
| `bad_pw_room` / `not_allowed` / `await_approval` / `blocked` | 입장 거절 사유(`denied.reason`) |
| `too_long` / `rate_limited` | 메시지 길이(4000자)·속도(10초 20건) 제한 |
| `file_too_big` / `file_busy` | 이미지 크기 초과 / 동시 전송 수 초과 |
| `deleted` / `kicked_conn` / `server_error` | 계정 삭제 / 관리자가 연결 끊음 / 서버 내부 오류 |

---

### 10. 구현 순서

| 단계 | 내용 | 검증 |
|---|---|---|
| 1 ✅ | `domiserver.py`: 프레이밍·계정(사후승인)·방 4유형·팬아웃·강제퇴장·REPL | **완료** — 종단간 57항목 + TTL 6항목 통과 |
| 2 ✅ | `domichat.py`: 로그인/리스트 화면 + 공개방 채팅 | **완료** |
| 3 ✅ | 제한방 3유형 + 승인 대기·블랙리스트 창 + 방 삭제 | **완료** |
| 4 ✅ | 구독 + 로컬 기록 + 알림 + 채팅 지우기 | **완료** — GUI 종단간 64항목 통과 |
| 5 ✅ | exe 패키징(런처 분리) + ⟳ 업데이트 + 설치 프로그램(관리자 권한 없음) | **완료** — 설치·실행·제거 검증 |
| 6 ✅ | TLS(자체 서명 + 지문 고정) | **완료** — 20항목 검증 |
| 7 ✅ | 이미지 첨부(PNG 변환·중계·인라인·클립보드) | **완료** — 29항목 검증 |
| 7-1 | 임의 파일(이미지 아닌 것) 전송 | — 추후 |
| 8 | `domichat_m`(모바일), `domichat_web`(BGOD 구동) | — 추후 (웹은 아래 8-1로 일부 도달) |
| 9 ✅ | domiman 매크로 이식(ntfy 완전 제거) | **완료** — 종단간 25항목 통과 |

#### 8-1. 웹에서의 채팅 (domiman 웹앱 260913a — `domichat_web`은 아니다)

`cheongbaek.github.io/domiman` 웹앱이 **domiweb(원격제어 중계)를 통해** 이 시스템의
방에 들어가 글·이미지를 주고받는다. 별도 클라이언트(`domichat_web`)가 아니라 **제어
중계에 얹은 기능**이므로 성격이 다르다. 자세한 것은 `web.md`의 '채팅' 절.

| 항목 | 웹 |
|---|---|
| 계정 | **없다.** 중계의 계정(`web`) 하나를 브라우저 여러 대가 공유한다 → 입장 자격도 `web` 기준이고(사후 승인은 `web`을 승인), 다른 브라우저의 말도 서로에게 '내 말'로 보인다 |
| 할 수 있는 것 | 방 목록·입장(공개/비밀번호/사전·사후 승인)·대화·이미지 |
| 없는 것 | **방 만들기·방장 도구(승인 대기·블랙리스트·비번 변경·방 삭제)·구독·알림** |
| 이미지 | 보내는 쪽이 PNG로 바꿔 올리는 규칙 그대로. `'B'` 청크 조립은 **중계가** 하고 브라우저에는 완성된 PNG를 base64로 준다 |
| 제어용 방 | `domi_fishing_*`는 목록에서 감추고 입장도 막는다(수량 방송이 대화창을 뒤덮고, 명령 화이트리스트를 우회하는 통로가 된다) |

**와이어 포맷을 고치면 여기도 같이 고쳐야 한다** — 이제 이 규격의 사본이 넷이다:
`domichat.py` · `domiman.py`(스크린샷) · `domiman_m.py` · `domiweb.py`(웹 채팅).

---

### 9-1. 업데이트 · exe 패키징 · 설치 프로그램

#### 업데이트 (⟳ 버튼)
- 버튼 위치: **로그인 화면은 '회원가입' 오른쪽**, **로그인 후에는 '로그아웃' 오른쪽**.
  둘 다 같은 `check_update()`를 부른다.
- `APP_VERSION`(domichat.py) vs 리포의 `domichat_version.txt`를 비교해 더 크면
  `domichat.py` 원본을 받아 `os.replace`로 원자적 교체 후 재시작한다. domiman과 같은
  규약("YYMMDD"+알파벳, 고정 자릿수라 문자열 비교가 곧 버전 순서)이고, **리포는
  domiman과 공유하되 파일 이름으로 구분**한다(`domichat_version.txt`/`domichat.py`)
  — 리포를 새로 만들지 않아도 되고 domiman 업데이트와 섞이지 않는다.
- 네트워크는 `urllib`으로 한다(**domichat은 표준 라이브러리만 쓴다** — requests 없음).
- **업데이트 후 첫 화면**: 자동 로그인이 켜져 있으면 저장된 IP·ID·PW로 바로 로그인해
  **채팅방 목록**으로, 꺼져 있으면 **로그인 창**으로 간다(별도 코드가 아니라 평소
  시작 경로 그대로다).
- **작업 스레드는 tkinter를 건드리지 않는다(함정):** 처음엔 업데이트 조회 스레드가
  `root.after`로 결과를 넘겼는데 `RuntimeError: main thread is not in main loop`가
  났다. 네트워크 수신과 같은 방식으로 **큐(`ui_q`)에 넣고 메인 스레드의 `_pump`가
  꺼내 처리**한다. 종료 시에는 예약된 `_pump`를 `after_cancel`해야 Tk가
  `invalid command name ..._pump`를 뱉지 않는다.

#### ⚠️ 새 `import` 를 추가하면 스펙도 같이 고쳐야 한다 (실제로 배포본을 죽인 함정)

`domichat.py` 는 exe에 **데이터로만** 들어가 매 실행 `runpy` 로 읽히므로 **PyInstaller
정적 분석 대상이 아니다.** 그래서 그 파일이 새로 `import` 한 모듈이 번들에 없으면
**스크립트로는 멀쩡한데 exe에서만 시작조차 못 한다.**

실제 사고(260815b): 이미지 기능을 넣으며 `import uuid` 를 추가했는데 스펙에 안 적어
배포본이 업데이트 후 `ModuleNotFoundError: No module named 'uuid'` 로 죽었다.
**앱이 안 켜지니 ⟳ 버튼으로 되돌릴 수도 없었다.** 실측으로 확인한 결과, 그 번들에
없던 것은 `uuid` · `tkinter.filedialog` · `PIL` 셋이었다(`base64`·`io`·`hashlib`·
`ssl` 등은 이미 들어 있었다).

세 겹으로 막았다:
1. **`uuid` 를 아예 안 쓴다** — `new_fid()` 가 `os.urandom(16).hex()` 로 같은 32자리
   hex를 만든다. 선택적인 것(`filedialog`·`PIL`)은 `try/except` 로 감싸 없으면 그
   기능만 끈다.
2. **런처가 자동 복구한다** — 업데이트 시 이전 파일을 `domichat.py.bak` 으로 남기고,
   런처는 실행이 예외로 끝나면 `.bak` 으로 되돌린 뒤 사유를 `domichat_error.log` 에
   적고 이전 버전으로 다시 띄운다(`.bak` 이 없으면 안내 후 종료 — 무한 반복 없음).
   ※ 이 보호는 **런처가 exe에 컴파일돼 있으므로 재빌드해야 적용된다.**
3. **검사 스크립트** — `python scripts/check_spec_imports.py` 가 `domichat.py` 의
   최상위 import 를 파싱해 `domichat.spec` 의 `hiddenimports` 와 대조한다. 빠진 게
   있으면 종료코드 1. **import 를 추가하면 이걸 돌릴 것.**

#### 빌드 (domiman과 완전히 분리)
```
pyinstaller domichat.spec --noconfirm --clean      # → dist\domichat\
ISCC.exe domichat_installer.iss                     # → Output\도미챗_설치.exe
```
- **컴파일 대상은 `domichat_launcher.py`**이고 `domichat.py`는 `datas`로만 동봉해
  매 실행 `runpy`로 읽는다. 실행 중인 exe는 덮어쓸 수 없으니, 업데이트는 옆의
  평범한 `.py`만 교체하면 끝나게 하려는 구조(domiman과 동일한 이유).
- `domichat.py`는 정적 분석 대상이 아니므로 그 파일이 쓰는 의존성을 스펙의
  `hiddenimports`에 적어야 한다(`tkinter*`, `urllib.request`, `winotify`).
- 무거운 스택(numpy/torch/cv2/PIL/requests/pyautogui…)은 `excludes`로 잘라 27.9MB,
  설치 프로그램 10.9MB.
- 아이콘은 `domichat.ico` — 제공된 픽셀아트 PNG에서 **여백을 잘라내고 정사각으로
  맞춰** 16~256px 다중 크기로 변환했다(원본은 캐릭터가 가운데 60%뿐이라 그대로
  줄이면 작은 크기에서 알아볼 수 없다).

#### 관리자 권한 없음 (domiman과 다른 점)
- exe: `uac_admin` 미지정 = **asInvoker** (domiman은 `requireAdministrator` —
  게임에 입력을 보내야 해서 승격이 필요했지만 채팅은 필요 없다).
- 설치: `PrivilegesRequired=lowest` + `DefaultDirName={localappdata}\domichat`.
- **이 선택이 업데이트에도 필수다:** Program Files에 설치하면 ⟳ 버튼이
  `_internal\domichat.py`를 덮어쓸 때 관리자 권한이 필요해진다. 사용자 폴더
  설치라 권한 없이 교체된다(검증: 설치본의 `domichat.py`에 쓰기 권한 O).
- 검증 방법(재사용): Inno는 `lowest`면 승격 여부와 무관하게 사용자 모드로 동작해
  제거 정보를 **HKCU**에 남긴다(관리자 모드면 HKLM). 승격된 셸에서 테스트해도 이
  차이로 판별된다.
- domiman과 겹치지 않게 전용 `AppId`, 설치 폴더(`{localappdata}\domichat`), 시작
  메뉴 이름(도미챗), 출력 파일명(`도미챗_설치.exe`)을 모두 따로 뒀다.

---

### 9-2. 이미지 첨부 (구현됨, 260815b)

**보내는 쪽이 무조건 PNG로 바꿔 올리고, 서버는 저장 없이 흘려보내며, 받은 쪽 로컬에
그 방의 기록으로 남는다.** 대화 텍스트와 완전히 같은 규칙이다.

| 단계 | 동작 |
|---|---|
| 보내기 | 톱니바퀴 → **이미지 업로드**, 또는 입력란에서 **Ctrl+V**(클립보드 이미지·복사한 이미지 파일). JPEG·BMP·WebP 등 무엇이든 **PNG로 변환**해 보낸다 |
| 변환 | 긴 변이 `IMG_MAX_SIDE=2560px` 를 넘으면 줄인다(PNG는 사진에서 쉽게 수십MB가 된다). 변환 후 32MB 초과면 거부 |
| 전송 | `file_begin` → `'B'` 청크(64KB) × N → `file_end`. 청크마다 별개 프레임이라 **전송 중에도 채팅이 멈추지 않는다** |
| 서버 | **아무것도 저장하지 않는다.** 받은 청크를 방의 다른 접속자에게 즉시 중계하고 끝. 보낸 쪽에는 도달 확인(`ok{of:"file_end"}`)만 돌려준다 |
| 받기 | 진행률(`n%`)을 보여주다가 완료되면 크기·sha256을 확인하고 말풍선에 인라인 표시 |
| 보존 | **구독한 방만** `history/img_<방해시>/<fid>.png` 로 저장하고 기록에 `{"kind":"img"}` 한 줄을 남긴다. 창을 다시 열면 되살아난다 |
| 삭제 | '채팅 지우기'·방 삭제 시 그 방의 이미지 파일까지 함께 지운다 |
| 꺼내기 | 이미지 우클릭 → **클립보드로 복사(Ctrl+C)** / 다른 이름으로 저장. 더블클릭도 저장 |

- **왜 PNG로 통일하나:** tkinter의 `PhotoImage`는 PNG/GIF만 읽는다. 보내는 쪽에서 한 번
  바꿔두면 받는 쪽은 형식 문제를 겪지 않고, 채팅방에는 항상 PNG로 보인다.
- **Pillow가 필요하다**(변환·클립보드·표시 축소). 없으면 **이미지 기능만 비활성**되고
  채팅은 그대로 동작한다(`HAS_PIL`). ⟳ 업데이트는 `domichat.py` 하나만 바꾸므로
  **기존 exe 설치본에 Pillow를 넣지는 못한다** → exe에서 이미지를 쓰려면 Pillow가 포함된
  설치 프로그램으로 다시 설치해야 한다(`domichat.spec`에 이미 반영).
- **클립보드 복사는 CF_DIB로 직접 넣는다.** Pillow에는 클립보드 쓰기가 없어 BMP로
  인코딩한 뒤 앞 14바이트(파일 헤더)를 떼어 DIB로 만들어 넣는다.
  **`GlobalAlloc`/`GlobalLock`의 `restype`을 반드시 지정해야 한다(64비트 함정)** —
  ctypes 기본 반환형이 32비트 int라 핸들이 잘려 조용히 실패한다(실측).
- 파일 이름은 서버에서도 정화한다(경로 구분자·`..`·제어문자 제거) — 받는 쪽이 그 이름으로
  저장하기 때문이다.
- **이 전송 규격을 쓰는 클라이언트가 하나 더 있다(260821a):** `domiman.py`의 원격
  스크린샷(`I` 명령)이 게임 화면 PNG를 같은 방식으로 올리고 받는다. 클라이언트 계층을
  **복제**해 쓰는 관계라(공용 모듈 없음 — 위 '구현 시 주의' 참고) **와이어 포맷을 바꾸면
  domiman도 같이 고쳐야 한다**: `'B'` 프레임 머리(`fid` 16B + `seq` 4B), `file_begin`의
  `size`/`sha256`, `welcome.ver` 의미. domiman 쪽은 Pillow 없이 cv2로 PNG를 만들고
  읽는다(자세한 것은 `CLAUDE.md`의 '원격 스크린샷' 절).

#### ⚠️ 이미지를 보냈더니 1초 간격 재접속 고리에 빠졌다 (실제 사고, 260815d에서 고침)

증상: 이미지 업로드 직후 서버 로그에 `[로그인]`/`[해제]`가 **1초 간격으로 끝없이**
반복(실측 12초에 18번). 원인은 **돌고 있던 서버가 이미지 지원 이전 버전**이었던 것:

1. 옛 서버는 `'B'` 프레임을 "지원하지 않는 프레임 종류"로 보고 **연결을 끊는다.**
2. 끊기면 클라이언트는 재연결하는데, **송신 큐에 남은 청크를 다시 보낸다.**
3. → 다시 끊김 → 다시 보냄 … 큐가 빌 때까지 무한 반복.

세 겹으로 막았다:
- **프로토콜 버전 확인**: 서버가 `welcome` 에 `ver`(1=텍스트만, 2=이미지 지원)을 실어
  주고, 클라이언트는 `ver < 2`면 아예 보내지 않고 "이 서버는 이미지 전송을 지원하지
  않습니다"라고 알린다.
- **끊기면 남은 청크를 버린다**(`_drop_pending_chunks`). 서버 쪽 전송 상태가 사라져
  이어 보낼 수도 없다. 실패한 청크도 되돌려 넣지 않는다(채팅 메시지는 그대로 재전송).
- 보내던 이미지는 말풍선에 **✕(실패)** 로 표시하고 다시 보내도록 안내한다.

**변환은 작업 스레드에서 한다** — 큰 사진의 PNG 인코딩을 GUI 스레드에서 하면 창이
얼어붙는다. 결과는 `ui_q` 로 받아 메인 스레드가 전송을 시작한다.

#### 옛 구상에서 바뀐 것
서버가 썸네일을 만들어 내려보내는 방안(클라이언트 의존성 0)을 검토했지만, **보내는 쪽이
PNG로 변환**하는 쪽으로 정했다. 서버가 이미지를 디코딩하지 않아 악성 이미지 파싱 위험이
없고, 서버 보관이 아예 없어 "서버는 대화를 저장하지 않는다"는 원칙이 그대로 유지된다.
대가는 클라이언트에 Pillow가 필요해진 것뿐이다.

---

### 9-3. (참고) 임의 파일 전송 — 미구현

프레이밍은 이미 자리를 비워뒀다: `typ='B'` 프레임, payload = **`fid`(16B) + `seq`(4B) +
데이터(≤64KB)**. 청크마다 별개 프레임이라 **큰 파일을 보내는 중에도 채팅 프레임이 사이에
끼어들 수 있다**(대화가 멈추지 않는다).

#### 흐름

```
보내는 쪽 ──file_begin{fid, room, name, size, sha256}──▶ 서버(임시 파일로 받기 시작)
          ──'B' 청크 × N (64KB) ──────────────────────▶   진행률: seq/총청크
          ──file_end{fid}─────────────────────────────▶ 서버: 크기·sha256 검증
                                                          └─▶ 방 전원에 file{fid, from,
                                                              name, size, ts, thumb?}
받는 쪽  ──file_get{fid}──▶ 서버 ──'B' 청크 × N──▶ ──file_end──▶ 저장 후 sha256 검증
```

- 서버는 **받은 파일만 예외적으로 보관**한다(대화 내용은 여전히 저장하지 않음).
  `files/{fid}` + 메타는 DB. 보관 기간이 지나거나 총 용량 상한을 넘으면 오래된 것부터
  삭제한다. 순수 중계(무저장)로 하면 **양쪽이 동시에 켜져 있어야** 해서 실용성이 없다.
- 무결성은 sha256, 실패하면 전체 재전송(이어받기는 v1 제외 — 복잡도 대비 이득이 작다).
- 파일 이름은 정화한다(경로 구분자·`..`·예약 이름 제거, 길이 제한). 받는 쪽에서
  `%USERPROFILE%\Downloads\domichat\` 에 저장하고, 같은 이름이면 `(2)`를 붙인다.
- 실행 파일 확장자(`.exe .bat .cmd .ps1 .scr` 등)는 받을 때 경고를 띄운다.

#### 이미지 인라인 표시가 진짜 문제다 (여기가 설계의 핵심)

tkinter의 `PhotoImage`는 **PNG/GIF만** 읽는다. 휴대폰 사진·카톡 이미지는 대부분 JPEG라
그대로는 못 그린다. 그런데 domichat은 "표준 라이브러리만"이 원칙이라 Pillow를 넣으면
클라이언트 빌드가 ~15MB 늘고 원칙이 깨진다.

**제안: 썸네일을 서버가 만든다.** 보내는 쪽은 원본만 올리고, 서버가 Pillow로
**320px PNG 썸네일**을 만들어 `file` 알림에 base64로 실어 보낸다. 그러면 **클라이언트는
의존성 없이** PNG 썸네일을 `PhotoImage`로 바로 그릴 수 있고, 원본은 눌렀을 때 받는다.

- Pillow는 **서버(BGOD 한 대)에만** 설치하면 된다. 없으면 썸네일 없이 파일 카드로 표시
  (기능이 죽지 않는다).
- 썸네일이 수십 KB라 `'T'` 프레임 상한(1MB) 안에 넉넉히 들어간다.
- 대가: 서버가 이미지를 디코딩한다(악성 이미지 파싱 위험) → Pillow를 최신으로 유지하고
  크기·픽셀 수 상한을 둔다.

#### 결정이 필요한 것

1. **썸네일을 서버가 만드는 위 방식으로 갈까요?** (대안: 클라이언트에 Pillow 추가 →
   JPEG도 직접 표시 가능하지만 빌드가 커지고 모든 PC에 배포됨)
2. **최대 파일 크기와 보관 기간** — 제 기본값은 **200MB / 7일**(+ 총 용량 상한 넘으면
   오래된 것부터 삭제).
3. **클립보드 붙여넣기(Ctrl+V로 스크린샷 전송)를 지원할까요?** 표준 라이브러리로는
   클립보드 비트맵을 꺼낼 수 없어 Pillow(`ImageGrab`)나 pywin32가 필요합니다.
   v1은 `[파일 선택]` 대화상자만 두는 것을 권합니다(드래그&드롭도 tkinter 기본으로는
   안 되고 `tkdnd` 확장이 필요합니다).

---

### 10-1. 구현 노트 (스펙에 없던 판단)

- **서버와 같은 PC에서 자기 IP로 접속된다.** `connect_any()`는 접속에 실패했을 때
  그 주소가 이 PC 자신의 주소면 `127.0.0.1`로 한 번 더 시도한다. BGOD에서 자기
  공인 IP를 적어도 붙고(실측: 직접 접속도 됨), 공유기가 되돌림(헤어핀)을 막는
  환경에서도 폴백으로 붙는다.
- **IP 칸은 `IP:포트` 형식도 받는다**(`211.196.44.3:47899`). 기본 포트는 47821.
- **강제 퇴장은 '블랙리스트' 창에서 한다.** 스펙에 퇴장 버튼 위치가 없었고, 퇴장과
  블랙리스트는 같은 동작(퇴장 = 자동 등재)이라 한 창에 묶었다. 톱니바퀴 →
  블랙리스트에서 ID를 넣어 퇴장시키고, 목록에서 해제한다.
- **데이터 폴더는 `DOMICHAT_DIR` 환경변수로 바꿀 수 있다** — 테스트와 여러 프로필
  동시 실행용. 기본은 `%APPDATA%\domichat`.
- **채팅방 창 크기를 방별로 기억한다**(`config.json`의 `room_geom`).
- **재접속하면 열려 있던 방과 구독한 방에 자동 재입장·재구독한다.** 비밀번호 방은
  보관해둔 비번을 자동으로 쓴다(그래서 방 비번을 DPAPI로 저장한다).
- **비밀번호 방 재입장은 저장된 비번을 먼저 써본다.** 틀리면(방장이 비번을 바꿀 수는
  없으니 사실상 저장값 손상) 저장값을 지우고 입력창을 띄운다.
- 말풍선은 v1에서 **각진 모서리**(여백 있는 Label)다. 창 폭이 바뀌면 모든 말풍선의
  `wraplength`를 폭의 70%로 다시 계산한다.
- 전송 표시는 `···`(전송 중) → `✓`(서버 도달). 서버 에코에 실려 오는 `cid`로
  판정하며, **에코로 말풍선을 다시 그리지 않는다**(중복 방지).

---

### 10-2. domiman 이식 (구현됨 — domiman 260815a)

낚시 매크로(domiman)의 ntfy 원격 제어를 이 채팅 시스템으로 갈아끼운다. **채널 하나를
여러 PC가 공유하던 방식에서, 피제어 PC마다 방 하나를 쓰는 방식으로 바뀐다.**

| 항목 | 정해진 것 |
|---|---|
| 방 이름 | `domi_fishing_{피제어PC ID}` — 규칙이 고정이라 제어 PC가 이름을 계산해 찾는다 |
| 방 유형 | **비밀번호 방**, 비밀번호는 `domi_fishing_9714` 고정 |
| 방장 | 피제어 PC(낚시 루틴이 도는 쪽). 실행 시 없으면 만들고 있으면 입장 |
| 식별자 | **PC 이름 항목을 없애고 domichat 로그인 ID를 그대로 쓴다.** 기존 ntfy 이름 설정은 업데이트 때 무시 |
| 제어 흐름 | 제어 PC가 방 이름을 계산 → 고정 비번으로 입장 → **구독** → 기존처럼 `S` 질의를 보내 상태를 받아온다 |
| 종료 감지 | 피제어 PC가 꺼지면 방장이 방에서 빠지고, 제어 PC가 `member{in:false}`로 감지해 제어 모드를 끝낸다 |
| 동시 역할 | **허용**. 한 연결로 내 방(피제어)과 남의 방(제어)에 동시에 있을 수 있다. 기존 "원격 제어 중 자기 명령 무시" 규칙은 폐기 |
| 메시지 양식 | **그대로**(`TR1,S` / `BGOD,Z,...` / `,Z,F,g`). 방이 PC별로 갈려 섞일 우려가 없다 |
| 안드로이드 앱 | **포팅 완료(260815f)** — 아래 참고. 자세한 것은 `mobile.md` |

**왜 자동 승인이 아니라 비밀번호 방인가:** 사후 승인방에서 방장이 무조건 승인하면
사실상 공개방이 되어, 계정만 있는 남이 뻔한 방 이름을 찍어 들어와 `Q`(종료)·`W`(회수)
명령을 보낼 수 있다. 고정 비밀번호는 **일반 domichat 사용자의 오진입을 막는 용도**이며
(방 목록에 보이지만 들어올 수 없다), 승인 왕복이 없어 입장이 즉시라는 장점도 있다.

#### GUI 변경 (domiman 메인 화면)
| 기존 자리 | 바뀌는 것 |
|---|---|
| 'ntfy/SMS에 표시될 이름' 라벨 | **[로그인] 버튼** |
| 그 옆 입력란 | 로그인된 **IP 주소 표시**(읽기 전용) |
| 'ntfy 채널 이름' 라벨 | **[로그아웃] 버튼** |
| 그 옆 입력란 | 로그인된 **ID 표시**(읽기 전용) |
| 'ntfy 메시지' 체크박스 | **'domichat 메시지'** (의미는 그대로 수/발신 활성화) |

- [로그인] → 별도 창(IP·ID·PW + `[로그인] [취소]`). **회원가입 버튼은 두지 않는다**
  (계정은 domichat 쪽에서 만들고 서버 콘솔에서 승인한다).
- [로그아웃]을 누르지 않는 한 domiman을 재실행해도 **자동 로그인**한다.
- 자격 저장은 domichat과 같은 방식: IP·ID는 `domiman_config.json`, PW는 DPAPI로 암호화한
  별도 파일. 기존 `channel`·`name` 항목은 제거한다.
- 낚시 중에도 로그인/로그아웃은 잠그지 않는다(원격이 끊겼을 때 복구 수단이므로).
  단 로그아웃에는 확인 창을 둔다.

#### 구현 결과 · 주의
- 검증(종단간 25항목, 실제 domiserver + 실제 `DomimanApp`): 로그인·내 방 생성(비밀번호
  방)·구독 / 피제어 역할(S·T·C 명령 응답, 상태 문자열) / 보고 브로드캐스트(`,Z,F,s`) /
  제어 역할(상대 방 입장 → S 질의 → 상태 UI 반영) / **방장 이탈 감지 후 제어 자동 해제** /
  로그아웃(자격 삭제·서버 접속 해제).
- **클라이언트 계층은 `domiman.py` 안에 복제한다**(프레이밍 + 접속·재연결). `domichat.py`를
  import하면 두 앱의 업데이트가 묶여 "자기 .py 하나만 교체" 규약이 깨진다. 규격의 단일
  기준은 이 문서다.
- **자기 발신분 걸러내기**는 `from == 내 ID`로 한다(서버가 발신자에게도 에코하며, 예전엔
  ntfy Title로 걸렀다).
- 강제 종료(전원 차단·랜 끊김)된 방장은 서버가 **최대 45초** 뒤에 이탈로 처리한다
  (ping 15초/타임아웃 45초). 정상 종료는 즉시.
- domichat 서버가 꺼져 있어도 **낚시 자체는 계속돼야 한다.** 로그인 실패는 원격 기능만
  막고, 재연결 백오프로 계속 시도한다.

#### 10-3. 안드로이드 앱 이식 (구현됨 — domiman 모바일 260815f)

`domiman_m.py`(Chaquopy로 앱에 내장되는 모바일 레퍼런스)에서 ntfy를 걷어내고 이
문서의 클라이언트 계층을 **한 번 더 복제**했다. 상세 스펙은 `mobile.md`.

- **제어 전용이라 방을 만들지 않는다.** `room_create`를 보내지 않으므로, 대상 PC가
  domichat에 한 번도 접속한 적이 없으면 `join`에 `error(room_missing)`이 오고 앱은
  "'seoul'가 아직 domichat에 접속한 적이 없습니다."를 띄운다.
- **로그인 입력이 IP·ID·PW로 바뀌었다.** PC 이름 항목은 없고, 제어할 PC는 메인
  화면 최상단 박스에서 고른다(기본 목록 seoul/chungju/galaxy, 추가·삭제 가능).
  고를 때마다 이전 방을 `sub off`+`leave` 하고 새 방에 들어가므로 **로그아웃 없이
  대상만 갈아탄다**.
- **`requests` 의존성이 사라졌다** — 표준 라이브러리(socket/ssl/struct/…)만 쓴다.
- **Kotlin에는 콜백을 넘기지 않는다.** 파이썬이 이벤트를 큐에 쌓고 Kotlin이
  `poll_event_json(0.5)`로 꺼내간다. 로그아웃 때 죽은 스코프의 람다를 파이썬이
  붙잡아 UI가 멈추던 사고(ANR)를 구조적으로 막기 위한 것이며, `stop()`도
  스레드 join 없이 소켓을 `shutdown`+`close`하고 즉시 반환한다.
- **TLS 1.2 고정·지문 고정(TOFU)은 그대로**이고, 지문은 앱의 SharedPreferences
  (`LoginStore.fingerprints`)에 "IP:포트 → SHA-256"으로 남는다. 서버가 평문이면
  평문 폴백도 그대로 동작한다(가짜 서버로 확인함).

---

### 11. 확정된 판단 (되돌리기 전에 이유를 볼 것)

- **회원가입은 항상 사후 승인제.** 공인 IP라 가입을 열어두면 외부인이 계정을 만들 수
  있어서다. 서버 콘솔 `approve` 전까지는 로그인이 '존재하지 않는 ID'로 응답한다
  (대기 중임을 알려주면 ID 존재 여부를 캐는 수단이 된다).
- **사후 승인 수락은 영구**(허용 명단에 추가). 매번 다시 승인받게 하려면 별도 세션
  상태가 필요해 복잡도만 늘고, 강제 퇴장·블랙리스트가 있어 되돌릴 수단이 이미 있다.
- **강제 퇴장되면 구독도 해제되고 방 비번도 잊는다.** 다시 들어갈 수 없는 방을
  계속 구독하고 있을 이유가 없고, 블랙 해제 후 재입장할 때 비번을 새로 받는 편이 맞다.
- **강제 퇴장은 블랙리스트 등재를 겸한다.** 퇴장만 시키면 곧바로 다시 들어올 수 있어
  의미가 없다. 사전 승인 방에서는 허용 명단에서도 지운다(두 명단에 동시에 올라 판정이
  모순되는 것을 막는다).
- **같은 ID 동시 접속 불허 + 죽은 연결 자동 회수.** 불허만 하면 비정상 종료 뒤
  본인이 못 들어오는 사고가 난다 → ping 15초/타임아웃 45초, 그리고 같은 ID 로그인이
  오면 기존 연결을 즉시 찔러 1.5초 무응답이면 회수한다(수동 수단은 콘솔 `kick`).
- **서버는 대화를 저장하지 않는다.** 앱이 꺼져 있던 동안은 공백으로 남는다 —
  백그라운드 동작을 상정하지 않기로 했으므로 서버 링버퍼·backlog 프레임을 두지 않는다.
- `rooms.last_msg`만은 예외적으로 저장한다. **공개방 자동 삭제 판정에만 쓰는 시각이며
  내용과 무관하다.**


---

# 📄 도미맨 모바일 앱 (Android)

*(옛 `mobile.md` 를 여기로 합쳤다 — 문서는 이 파일 하나뿐이다.)*

## 도미맨 모바일 앱 (Android) — 인터페이스·구조·빌드 총정리

테일즈러너 낚시 매크로(PC판 `domiman.py`)를 **휴대폰에서 원격 제어**하는
안드로이드 앱. PC를 조종하는 "제어(dB)" 역할만 한다(자기가 조종당하지는 않음).
통신 로직·데이터 규칙은 `domiman_m.py`(Python)를 Chaquopy로 앱에 내장해 그대로
쓰고, 화면·위젯·알림·서비스는 Kotlin/Compose로 구현했다.

> 이 문서 하나로 다음 세션에서 앱을 그대로 이어갈 수 있게 정리한 것.
> **UI는 아래 스펙대로가 최종이며, 변경 요청이 없으면 새로 설계하지 말 것.**
> (PC판/프로토콜 세부는 같은 폴더 `CLAUDE.md`, domichat 규격은 `domichat.md`.)

### 0. 260815f — ntfy를 걷어내고 domichat으로 이식 (현재 버전)

PC판이 260815a에서 domichat으로 갈아탄 것을 모바일에도 이식했다. **메시지
규격(`대상,명령` / `요청자,Z,...` / `,Z,F,코드`)은 글자 하나 안 바뀌었고 전송
계층만 통째로 교체**됐다.

| | 이전 (ntfy) | 지금 (domichat) |
|---|---|---|
| 전송 | `https://ntfy.sh/{채널}` HTTP 스트리밍 | domiserver **TCP 47821** + TLS 1.2(지문 고정) |
| 발신자 식별 | ntfy `Title` 헤더(위조 가능) | 서버가 찍는 `from` = domichat 로그인 ID |
| 대상 구분 | 채널 하나를 여러 PC가 공유 | **피제어 PC마다 방 하나** `domi_fishing_{PC ID}` |
| 방 유형 | — | 비밀번호 방, 고정 비번 `domi_fishing_9714` |
| 로그인 입력 | ID / 피제어PC / 채널명 | **서버 IP / domichat ID / domichat PW** |
| 제어 대상 지정 | 로그인 때 고정 | **메인 화면 최상단에서 선택**(로그아웃 없이 교체) |
| 파이썬 의존성 | `requests`(pip) | 없음 — 표준 라이브러리만(socket/ssl/struct/…) |

**휴대폰은 방을 만들지 않는다**(제어 전용). 방장은 피제어 PC이며, 그 PC가 한
번도 domichat에 접속한 적이 없으면 방 자체가 없어 `error(room_missing)`이 온다
→ "'seoul'가 아직 domichat에 접속한 적이 없습니다."로 안내한다.

---

### 1. 위치 — 프로젝트·도구·산출물이 각각 어디 있나

| 항목 | 경로 |
|---|---|
| **안드로이드 프로젝트** | `C:\Users\windo\dev\domiman-android` |
| Python 원본(레퍼런스) | `C:\Users\windo\OneDrive - 한국교통대학교\문서\Python\macro\domiman_m.py` |
| 프로젝트 내 Python 사본 | `…\domiman-android\app\src\main\python\domiman_m.py` |
| **빌드 결과 APK(기본)** | `…\domiman-android\app\build\outputs\apk\debug\app-debug.apk` |
| **배포 APK(수동 복사 대상)** | `C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk` |
| 앱 아이콘 원본 | `…\macro\app.ico` (록맨 낚시 픽셀아트) |
| 위젯 아이콘 원본 | `C:\Users\windo\OneDrive - 한국교통대학교\A_clean_2x2_grid_icon_set_...png` |

**⚠️ 빌드만 `macro` 폴더 밖에서 하는 이유(중요):** AGP/Chaquopy는 네이티브
빌드 도구라 **경로에 비ASCII(한글)가 있으면 빌드를 거부**한다. `macro` 폴더가
`…\한국교통대학교\…`라 한글을 포함해, ASCII 전용 경로 `C:\Users\windo\dev`로
옮겨 빌드한다.

> **정정(2026-08-21 이후):** 위 표의 "안드로이드 프로젝트 = `dev\domiman-android`"는
> **빌드 위치**를 말한다. 2026-08-21에 프로젝트가 저장소로 들어와 지금은
> **소스 권위 사본이 `macro\domiman-android\`** 이고 `dev` 쪽은 빌드 전용 거울이다.
> 고치는 곳은 `macro`, 돌리는 곳은 `dev`. 맨 위 **📍 빠른 참조** 표가 최신이다.

**⚠️ Python 사본 동기화(수동):** 원본 `macro\domiman_m.py`를 고치면 반드시
`Copy-Item`으로 프로젝트 안 `app\src\main\python\domiman_m.py`에도 덮어써야
반영된다. gradle 자동 동기화 태스크는 없다.

---

### 2. 패키징/빌드 도구 — 무엇이 어디 설치돼 있나

winget으로 설치했고, 필요한 SDK 컴포넌트는 커맨드라인으로 받았다.

| 도구 | 위치 / 버전 |
|---|---|
| Android Studio | `C:\Program Files\Android\Android Studio` |
| JDK(빌드용, Studio 내장 JBR) | `C:\Program Files\Android\Android Studio\jbr` |
| Android SDK 루트 | `C:\Users\windo\AppData\Local\Android\Sdk` |
| ├ cmdline-tools | `…\Sdk\cmdline-tools\latest` (22.0.0) — `sdkmanager`/`android` |
| ├ platform-tools | `…\Sdk\platform-tools` (37.0.0) — `adb` |
| ├ build-tools | `build-tools/36.1.0` |
| ├ platform | `platforms/android-36` |
| └ emulator + 시스템이미지 | `emulator` + `system-images/android-36/google_apis/x86_64`, AVD `medium_phone` |
| **buildPython** | `C:\Users\windo\AppData\Local\Programs\Python\Python312\python.exe` (python.org 정식 3.12) |
| Chaquopy(Gradle 플러그인) | `com.chaquo.python` 17.0.0, `version = "3.12"` |

**⚠️ buildPython 버전은 `chaquopy.defaultConfig.version`과 major.minor가 같아야
한다.** 2026-08-15 시점 windo PC에는 3.13이 없고 python.org 3.12.8만 있어
`version = "3.12"`로 내렸다(Chaquopy 17.0.0이 그대로 받아준다 — 빌드 확인함).
다른 PC에서 빌드하려면 **두 값을 함께** 그 PC 기준으로 고칠 것.

**pip 의존성은 이제 없다.** domichat 이식으로 `requests`가 필요 없어져
`chaquopy { pip { install("requests") } }` 블록을 지웠다. 그만큼 빌드가 빨라지고
buildPython이 하는 일도 바이트코드 컴파일뿐이다.

**환경변수(사용자 범위 영구 설정됨):** `ANDROID_HOME` / `ANDROID_SDK_ROOT` =
`C:\Users\windo\AppData\Local\Android\Sdk`, PATH에 `platform-tools`·
`cmdline-tools\latest\bin` 추가. **단 `JAVA_HOME`은 전역 설정 안 함** — 빌드할
때마다 세션에 `$env:JAVA_HOME`을 Studio JBR로 지정해야 한다(아래 빌드 명령).

**빌드 스택:** AGP 9.0.1, Kotlin 2.3.20, compileSdk/targetSdk 36, minSdk 24,
Jetpack Compose(BOM 2026.03.01) + Navigation3, kotlinx.serialization,
Chaquopy(Python 3.13 내장, pip: `requests`).

#### 이 PC(windo)에서 빌드하는 법 — ASCII 미러

`macro` 폴더는 경로에 한글이 있어 **그 자리에서는 빌드가 거부된다.** 그래서
소스는 리포 안(`macro\domiman-android`)에서 고치고, 빌드만 ASCII 경로로 복사해
돌린다. `local.properties`는 기계마다 다르므로 미러 쪽에만 만든다(gitignore됨).

```powershell
$src = "C:\Users\windo\OneDrive - 한국교통대학교\문서\Python\macro\domiman-android"
$dst = "C:\Users\windo\dev\domiman-android"
robocopy $src $dst /E /XD build .gradle .idea      # 소스만 미러링
# $dst\local.properties 에 sdk.dir=C\:\\Users\\windo\\AppData\\Local\\Android\\Sdk
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
cd $dst; .\gradlew.bat assembleDebug
Copy-Item "$dst\app\build\outputs\apk\debug\app-debug.apk" `
          "C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk" -Force
```

**미러는 단방향이다** — 소스 수정은 반드시 리포 쪽에서 하고 미러는 매번 다시
복사할 것. 미러에서 고치면 리포에 안 남는다.

#### 빌드 시 겪은 함정 4가지(재발 방지)
1. **비ASCII 경로 거부** — 위 참고. ASCII 경로로 이전해서 해결.
2. **Gradle Configuration Cache 비호환** — Chaquopy가 구성 단계에서 외부
   프로세스(빌드용 python 탐지)를 돌려 충돌. `gradle.properties`에
   `org.gradle.configuration-cache=false`로 꺼둠(템플릿 기본은 true).
3. **buildPython 자동탐지 실패** — 이 PC `py -3.13`이 Microsoft Store 스텁이라
   탐지 실패. python.org 정식 3.13을 깔고
   `chaquopy.defaultConfig.buildPython("C:/.../Python313/python.exe")`로 경로 명시.
4. **파이썬 호출은 예외 없이 suspend + Dispatchers.IO** — 소켓을 든 파이썬
   스레드와 GIL을 다투면 UI가 통째로 멈춘다. `DomimanRepository`의 모든
   파이썬 접근은 suspend + `withContext(Dispatchers.IO)`이고, ViewModel은
   `viewModelScope.launch`로 부른다. (자세한 것은 아래 §2-1.)

#### 2-1. 로그아웃 '응답 없음'(ANR) 대응 — 260815f에서 고친 것

**증상:** 로그아웃을 누르면 앱이 종종 '응답 없음' 상태가 됐다.

**원인:** 예전 `DomimanRepository.logout()`은 **일반 함수**라 Compose `onClick`
= 메인 스레드에서 그대로 실행됐는데, 그 안에서 파이썬을 세 번 만졌다
(`stream_disconnect`, `LoginStore.put`, `to_json`). 파이썬 호출은 GIL을
가져와야 하고 그 순간 스트림 스레드가 블로킹 소켓 읽기를 물고 있으면 메인
스레드가 그대로 멈춘다 → 5초를 넘기면 ANR.

**고친 방식(세 겹):**
1. **메인 스레드에서 파이썬을 아예 부르지 않는다.** `logout()`은 값싼
   SharedPreferences/StateFlow만 건드리고 **즉시 반환**한다. 소켓·서비스·
   파이썬 정리는 `appScope`에서 뒤따라 하며 `withTimeoutOrNull(4초)`로 막혀도
   포기한다. 화면은 이미 로그인 화면으로 넘어가 있다.
2. **파이썬 쪽 `stop()`도 블로킹하지 않는다.** `ChatClient.logout()`은
   송신 잠금을 `acquire(timeout=0.5)`로 시도해 못 잡으면 예의상 보내는 logout
   프레임을 건너뛰고, 소켓을 `shutdown(SHUT_RDWR)` + `close()`로 끊어 수신
   스레드를 즉시 깨운다. 스레드 join은 하지 않는다.
3. **재연결 백오프를 `time.sleep`에서 `Event.wait`으로** 바꿔, 30초 대기 중인
   세션 스레드도 로그아웃 순간 바로 깨어 끝난다.

또 **콜백을 파이썬으로 넘기지 않는다.** 예전엔 `stream(kotlinLambda, …)`처럼
Kotlin 람다를 파이썬 안에서 호출했는데, 지금은 파이썬이 큐에 이벤트를 쌓고
Kotlin이 `poll_event_json(0.5)`로 꺼내간다(IO 코루틴). 죽은 스코프의 람다를
파이썬이 붙잡고 있을 여지가 구조적으로 없어진다.

**회귀 확인:** 로그인 → PC 선택 → 로그아웃을 반복해도 멈추지 않아야 하고,
파이썬 단위로는 `stop()`이 1.5초 안에 반환하는지 확인한다(스모크 테스트 항목).

---

### 3. 빌드·배포 워크플로 (현재 방식, 유지)

```powershell
cd C:\Users\windo\dev\domiman-android
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
.\gradlew.bat assembleDebug
```
그 뒤 **빌드된 APK를 배포 위치로 손으로 복사(덮어쓰기):**
```powershell
Copy-Item "C:\Users\windo\dev\domiman-android\app\build\outputs\apk\debug\app-debug.apk" `
          "C:\Users\windo\OneDrive - 한국교통대학교\domiman.apk" -Force
```
gradle 자동 복사 태스크는 두지 않는다(사용자 요청 — 빌드는 기본 경로에, 배포
복사는 수동). 설치는 이 `domiman.apk`를 폰으로 옮겨 사이드로드.

**로컬 git 백업(원격 없음):** 프로젝트에 git이 초기화돼 있고 기능 단위로
커밋해 백업 중. GitHub 등 원격에는 push하지 않는다. `.gitignore`가
`build/`·`.gradle/`·`local.properties` 제외(소스 + `gradle-wrapper.jar`만 추적).

---

### 4. 아키텍처 (왜 이렇게 짰나)

```
[Compose UI/ViewModel] ─호출─▶ [DomimanRepository(앱 싱글턴)] ─JSON문자열─▶ [domiman_m.DomimanSession]
        ▲                              │  │                                        │ TCP 47821 + TLS1.2
        │ StateFlow/SharedFlow         │  └─▶ [DomimanService(포그라운드)]          ▼
        └──────────────────────────────┘  └─▶ [알림]/[위젯]              domiserver ◀─▶ PC domiman.py
                    ▲                                                   (방 domi_fishing_{PC})
                    └── poll_event_json(0.5) 루프(IO 코루틴)로 이벤트 수신
```

- **파이썬 세션이 스레드를 직접 굴린다.** `DomimanSession`은 내부에 세션
  스레드(접속·수신·재연결), 송신 스레드, 펌프 스레드를 두고, 앱 이벤트를 큐에
  쌓는다. Kotlin은 그 큐를 `poll_event_json`으로 비우기만 한다.

- **`DomimanRepository`는 앱 싱글턴**(`DomimanApplication.repository`). Activity가
  아니라 앱 수명에 매인다 — 화면 회전/백그라운드 복귀로 Activity가 재생성돼도
  로그인 세션과 수신이 살아남게. (예전엔 Activity `by lazy` 필드라 재생성 때
  세션이 날아가 모든 명령이 no-op이 되던 버그가 있었다.)
- **이벤트 펌프는 `appScope`(앱 수명 CoroutineScope)에서** 돎. 세션 수명 =
  로그인~로그아웃. MainScreen ViewModel의 onCleared에서 세션을 끄지 않는다.
- **Chaquopy 경계는 JSON 문자열로만 왕복(설계 결정).** PyObject의
  `Map<PyObject,PyObject>` 변환이 불확실해서, `domiman_m.py`에 Kotlin 전용
  헬퍼(`wait_login_json`, `wait_target_json`, `poll_event_json`, `status_json`,
  `LoginStore.to_json/from_json`)를 두고 전부 JSON으로 주고받는다. Kotlin은
  `kotlinx.serialization`으로 파싱(`DomimanModels.kt`). 새 반환값이 생기면
  PyObject API를 파헤치지 말고 이 패턴(JSON 헬퍼 추가)을 따를 것.
- **LoginStore는 prefs JSON이 단일 진실.** 파이썬 객체를 오래 들고 있지 않고,
  바꿀 때만 `from_json → 변경 → to_json → prefs` 하는 `mutateStore {}`를 쓴다
  (메모리 사본이 낡을 여지를 없앤다). 첫 화면 표시용 초기값만 Kotlin이 직접
  디코드해 앱 시작을 파이썬으로 막지 않는다.
- **역할 분리:** Python=프로토콜+데이터 규칙, Kotlin=화면. `domiman_m`의
  화면전환 참고용 클래스(`Screen`/`LoginFormState`)는 Navigation3/ViewModel과
  겹쳐 **안 쓴다**.

---

### 5. 파일 트리 (`app/src/main`) 와 역할

```
java/com/example/domiman/
  DomimanApplication.kt      # Python.start + repository 앱 싱글턴 생성
  MainActivity.kt            # repository 획득, 첫 실행 권한요청(알림→배터리예외), 다크모드 override
  Navigation.kt              # Navigation3. 시작화면 = 이전 로그인돼 있었으면 Main, 아니면 Login
  NavigationKeys.kt          # Login / RecentLogins / Main / NotificationSettings
  DomimanService.kt          # 포그라운드 서비스(specialUse|dataSync)
  DomimanWidgetProvider.kt   # 4x1 홈 위젯
  data/
    DomimanRepository.kt     # 핵심: domichat 세션·제어PC 선택·명령·알림·위젯·최근로그인 전부
    DomimanModels.kt         # @Serializable DTO (DomimanStatus/DomimanEvent/LoginResult/
                             #                    TargetResult/SavedLoginJson/LoginStoreJson)
    NotificationPrefs.kt     # 알림 설정 영속(NotifyItem enum 10개 + master), SharedPreferences
    DomimanNotifications.kt  # 알림 채널 2개 + 발송 + 탭 시 앱 열기(contentIntent)
  ui/
    login/  LoginScreen.kt + LoginScreenViewModel.kt
    recent/ RecentLoginsScreen.kt + RecentLoginsScreenViewModel.kt
    main/   MainScreen.kt + MainScreenViewModel.kt
    notif/  NotificationSettingsScreen.kt + NotificationSettingsViewModel.kt
  theme/   Color/Theme/Type.kt (템플릿 기본 테마)
python/domiman_m.py          # macro\domiman_m.py의 사본(수동 동기화)
res/
  drawable/widget_bg.xml           # 위젯 배경(둥근 밝은 패널 #F2F2F2)
  drawable/widget_btn_bg.xml       # 위젯 버튼 누름 하이라이트(원형 셀렉터)
  drawable/widget_ic_refresh|collect|play|pause.png  # 제공된 초록 원형 아이콘 세트
  drawable-nodpi/ic_launcher_scene.png               # 앱 아이콘(록맨 장면, adaptive 배경)
  layout/widget_domiman.xml        # 위젯 4칸 레이아웃
  xml/domiman_widget_info.xml      # 위젯 메타(4x1, updatePeriodMillis=0)
  mipmap-anydpi-v26/ic_launcher(.round).xml  # adaptive: 배경=장면, 전경=투명
  mipmap-*/ic_launcher(.round).png           # 밀도별 둥근 아이콘
AndroidManifest.xml          # 권한/서비스/위젯 리시버 선언
```

---

### 6. 화면별 인터페이스 (최종 — 이대로 유지)

기준 목업은 `앱UI설명.xlsx`(Sheet1=로그인, Sheet2=메인). 화면 4개를
Navigation3로 전환하며, 각 화면은 Compose 화면 + ViewModel이고 모두 앱 싱글턴
`DomimanRepository`를 공유한다.

#### 6-1. 로그인 (`ui/login`) — **domichat 이식으로 입력이 바뀐 화면**
- 제목: **"당신도 강태공이 되어보세요!"**
- 입력 3개: **domiserver IP 주소**(`IP` 또는 `IP:포트`, 기본 47821) /
  **domichat ID** / **domichat PW**(가려서 표시)
- 체크박스: **`[v] 자동 로그인`** + 안내 "자동 로그인을 체크해야 최근 로그인
  목록에 저장됩니다."
- 버튼 행: **`[로그인]` `[…]`**
  - `로그인`: domiserver에 접속·로그인(최대 20초). 성공하면 메인으로.
    **실패하면 서버가 준 사유를 그대로 보여준다**('존재하지 않는 ID입니다.',
    '이미 접속 중입니다.', '서버에 접속할 수 없습니다.' 등). 이때 ID/PW만 비우고
    **IP는 남긴다**(대부분 맞는 값이라 다시 치기 번거롭다).
  - `…`: 최근 로그인 화면으로. **이때 '자동 로그인' 체크 상태를 저장소에 전달**
    (`repository.pendingAutoLoginArm`)해서, 최근 로그인 항목으로 로그인해도 그
    체크대로 자동로그인이 무장되게 한다.
- **회원가입 버튼은 두지 않는다** — 계정은 domichat에서 만들고 서버 콘솔에서
  `approve` 한다(PC판과 같은 규칙).
- **edit(수정) 모드**(최근 로그인에서 '수정' 선택 시): 자동 로그인 체크박스를
  숨기고 버튼이 **`[수정]` `[취소]`** 로 바뀜. 값을 고쳐 저장하거나 취소.

#### 6-2. 최근 로그인 (`ui/recent`)
- 상단: **`< 최근 로그인`** (뒤로가기)
- 리스트: 행마다 **`domichat ID`** + 그 아래 **`서버 IP`**
  (PW도 저장하지만 화면에는 보이지 않는다)
- **짧게 탭 = 즉시 그 정보(IP·ID·PW)로 로그인**
- **길게 눌러 = 드롭다운 `[수정]` / `[삭제]`**
- **저장 규칙(사용자 확정):** **'자동 로그인'을 체크하고 로그인했을 때만**
  목록에 남는다. 체크하지 않고 로그인한 자격은 그 세션에서만 쓰이고
  **로그아웃하면 기억하지 않는다.** 같은 (서버IP, ID)면 중복 행 없이 갱신 후
  맨 위로. 개수 상한 없음. 앱데이터/캐시 삭제 시에만 소멸.
- 구현 메모: 현재 세션 자격은 `active_ip/active_id/active_pw` prefs에 따로 두고
  **로그아웃 때 지운다.** 이것이 있어야 프로세스가 죽었다 살아나도(위젯 탭,
  sticky 재시작) 다시 붙을 수 있고, 지우기 때문에 위 규칙도 지켜진다.

#### 6-3. 메인 제어 (`ui/main` — PC GUI와 유사, 세로 스크롤)
위에서 아래로:
0. **[제어 PC 선택 박스] (최상단, domichat 이식으로 추가된 것)**
   - 기본은 **아무것도 선택되지 않은 상태**이고 **"제어 PC 선택하기"** 문구가
     떠 있다. 고르면 그 자리에 PC 이름(예: `seoul`)이 굵게 뜬다.
   - 누르면 **PC 목록 다이얼로그**: 행마다 `[이름]  [삭제]`, 맨 아래
     `PC 이름` 입력칸 + `[추가]`. **입력받는 것은 PC 이름 하나뿐**이며 그 이름은
     그 PC의 domichat 로그인 ID다(`[A-Za-z0-9_-]{1,20}`).
   - **기본 목록: `seoul` / `chungju` / `galaxy`.** 추가·삭제는 즉시 영속
     (prefs `pc_list_json`), 선택값도 영속(`selected_pc`).
   - 고르면 **`domi_fishing_{이름}` 방에 입장(고정 비번) → 구독 → S 질의**로
     상태를 받아온다. **로그아웃 없이 대상만 갈아탄다** — domichat 로그인 세션은
     그대로 두고 이전 방만 `sub off` + `leave` 한다. 전환할 때 화면에 남아 있던
     이전 PC의 타이머/해상도/실행중 값은 지운다.
   - 실패 안내: `room_missing`="'X'가 아직 domichat에 접속한 적이 없습니다." /
     `no_response`="'X'가 응답하지 않습니다. PC가 켜져 있는지 확인하세요." /
     `bad_pw_room` / `blocked`.
   - **제어 PC를 고르기 전에는 아래 컨트롤이 전부 잠긴다**(보낼 곳이 없다).
   - 서버 연결이 끊기면 박스 밑에 "서버와 연결이 끊어졌습니다. 다시 접속하는
     중..."이 뜬다(자동 재연결 1→2→5→10→30초).
1. **해상도** 라벨(`1920 x 1080 (자동 감지됨)` 등) + **`[직접 설정]` `[자동 감지]`**
   (직접 설정 = 1080/1440 선택 다이얼로그)
2. **타이머** 입력(분). "0을 입력하면 살림망 감시 모드로 작동합니다." 안내.
   **입력마다 발신하지 않고 1.5초 디바운스 후 T 명령 발신**(과발신 방지).
3. **`[v] 낚싯대 자동교체`** / **`[v] 미끼 자동교체`** +
   "낚싯대, 미끼 자동교체는 살림망 감시 모드에서만 사용 가능합니다."
4. 큰 **`시작` / `중지`** 버튼(running이면 '중지' 표시)
5. **상태 메시지** 한 줄
6. **`[예약 종료]` `[즉시 회수]`** — 예약 종료는 분 입력 다이얼로그 → `Y,n`
   (※ **이 자리는 `[취침 모드]`로 대체 예정** — 위 12-1 TODO 참고)
7. **`[실시간 수량확인]` `[다크모드]`** — 수량확인은 N 질의(PC가 창 불러 확인·재개)
8. **`로그`** 헤더 + **`x`**(지우기) + 로그 패널(**항상 펼침, 마지막 8줄만**)
9. 하단 **`[로그아웃]` `[알림 설정]`**
- **버튼 잠금:** 명령 발신 후 응답 대기(isPending) 동안 컨트롤이 잠기고, **15초
  무응답이면 자동 해제 + "응답이 없습니다." 로그**.
- **로그아웃:** '로그아웃' 버튼 → "한 번 더 누르면 로그아웃됩니다." 확인 →
  로그아웃(자동로그인 무장 해제, 세션 자격 삭제, 세션·서비스 종료). 최근 로그인
  목록과 제어 PC 목록은 유지. **화면은 즉시 넘어가고 정리는 뒤에서 한다**(§2-1).
  (시스템 뒤로가기 2번 인터셉트는 아직 미구현 — 화면 내 버튼으로 대체.)

#### 6-4. 알림 설정 (`ui/notif`)
- 상단 **`< 알림 설정`**
- 마스터 **`[v] 알림 켜기`** — 꺼지면 아래 10개가 봉인(disabled)되고 알림도 안 뜸.
- 이하 **10개 체크박스(순서 고정):** 살림망 회수 시작 / 회수 성공 / 회수 실패 /
  낚싯대 교체 시작 / 교체 성공 / 교체 실패 / 미끼 교체 시작 / 교체 성공 /
  교체 실패 / 게임 튕김.
- **기본값 전부 on.** SharedPreferences 영속(앱을 껐다 켜도 유지, 앱데이터
  삭제 시 소멸).

---

### 7. 홈 위젯 4×1 (`DomimanWidgetProvider`)

레이아웃: **`[새로고침] | 수량 | [다운로드] | [재생/중지]`**
아이콘은 제공된 초록 원형 세트(`A_clean_2x2_grid_icon_set...png`를 사분면 크롭 →
`widget_ic_refresh/collect/play/pause.png`). 좌상=새로고침, 우상=낚싯대(회수),
좌하=재생물고기, 우하=정지물고기.

- **새로고침** → `cmd_tank_query`(N). 응답이 오면 옆 수량 칸 갱신. 토스트
  "실시간 수량 확인". **수량은 새로고침을 눌렀을 때만 갱신**(자동 폴링 없음).
- **다운로드** → `cmd_collect_now`(즉시 회수). 토스트 "즉시 살림망 회수".
- **재생/중지** → 시작/중지 토글. running이면 정지 아이콘(`widget_ic_pause`) +
  토스트 "매크로가 중지되었습니다", 대기면 재생 아이콘(`widget_ic_play`) +
  "매크로가 시작되었습니다".
- 각 버튼 누름 시 하이라이트 깜빡임(`widget_btn_bg` 셀렉터).
- **로그인 세션 없으면 수량 칸에 '로그인', 로그인은 됐지만 제어 PC를 아직 안
  골랐으면 'PC선택'** 을 표시하고, 그 상태에선 어느 칸을 눌러도 앱을 연다.
  **정상 상태에선 수량 칸을 눌러야 앱이 열린다.**
- 상태 영속: `DomimanRepository`가 prefs `widget_qty`/`widget_running`에 쓰고
  `DomimanWidgetProvider.refresh()`로 다시 그린다.
- **위젯 탭 시 프로세스 보호(함정):** broadcast `onReceive`는 반환되면 프로세스가
  곧 죽어, 15초 걸리는 재로그인 전에 종료돼 갱신이 안 됐다 → `onReceive`에서
  **먼저 `DomimanService.start()`로 프로세스를 붙잡은 뒤** 명령을 던진다. 세션이
  없으면 `ensureClientReady()`가 저장된 마지막 로그인으로 재접속 후 실행.

---

### 8. 알림 (백그라운드 포함)

- 하단 '로그아웃' 옆 **'알림 설정'** 버튼 → 알림 설정 화면(위 6-4).
- 실제 발송: `DomimanRepository.handleEvent`(이벤트 펌프)가 PC의 보고(`,Z,F,*`)를
  받으면 `NotificationPrefs.isEnabled(notifyKey)`일 때 `DomimanNotifications.postEvent`.
  **화면(Compose)이 아니라 저장소(앱 스코프)에서 띄우므로 앱이 백그라운드여도
  온다.** 어느 체크박스인지는 `dispatch_result`가 넣어주는 `report_notify_key`
  (`domiman_m.notify_key_for_report`, NOTIFY_KEYS 역변환)로 판정.
- 채널 2개: `ongoing`(서비스 상시 알림, 낮은 중요도) / `events`(이벤트 알림).
- **알림/상시 알림 탭 시 앱을 연다**(contentIntent = MainActivity, CLEAR_TOP).
- **보고 코드 ↔ 알림 항목(NOTIFY_KEYS, domiman_m.py):**
  `s`=회수시작, `g`=회수성공, `f`=회수실패, `rs`=낚싯대교체시작,
  `y,r`=낚싯대성공, `x,r`=낚싯대실패, `bs`=미끼교체시작, `y,b`=미끼성공,
  `x,b`=미끼실패, `x,d`=게임튕김. (rs/bs는 PC 260728a에서 추가된 '시작' 신호.)

---

### 9. 세션·백그라운드 생존 (핵심 함정)

- **포그라운드 서비스 `DomimanService`**: 로그인 성공 시 띄운다.
  OS가 백그라운드에서 프로세스를 못 죽이게 해 domichat 연결이 계속 살아 알림이
  오고, 다시 앱에 들어와도 세션이 그대로. `START_STICKY`.
  **타입 함정:** `dataSync`는 Android 14+에서 **하루 6시간 제한**이라 장기 방치
  시 종료됐다 → 34+는 시간제한 없는 **`specialUse`**, 33 이하는 `dataSync`
  폴백(매니페스트 `specialUse|dataSync` 병기, 코드에서 SDK 분기). 삼성 One UI의
  공격적 절전은 변수라, 최후엔 **설정→배터리→'제한 없음'에 앱 추가** 권장(코드로
  강제 불가).
- **복귀 리프레시:** MainScreen이 `LifecycleEventEffect(ON_RESUME)`에서
  `ensureSessionAlive()` — 세션이 있으면 파이썬 `resync()`를 부른다(방에 못 들어가
  있으면 **다시 입장 시도**, 들어가 있으면 S로 상태 재동기화). 세션이 없고 직전
  로그인 상태였으면(`session_active` 플래그) 저장된 세션 자격으로 재접속, 그래도
  실패면 로그인 화면으로.
  → **대상 PC를 나중에 켰을 때 앱이 다시 붙는 유일한 경로가 이 `resync()`다.**
- **재연결은 파이썬이 알아서 한다:** 끊기면 1→2→5→10→30초 백오프로 재접속하고,
  `welcome`을 다시 받으면 제어 중이던 방에 **자동 재입장·재구독**한다(서버는
  구독 상태를 저장하지 않는다). 한 번이라도 붙었으면 백오프 인덱스를 0으로
  되돌린다.
- **런치 신뢰성:** 시작 화면 = `isSessionConfigured()`(직전 로그인 유지 중)면
  **바로 Main**, 아니면 Login. 과거엔 실행 시 최대 15초 네트워크 로그인을 기다린
  뒤 이동해 느리거나 간헐 실패 시 로그인창이 떴는데, 지금은 즉시 Main으로 가고
  재접속은 백그라운드로 처리(실패 시에만 Login으로).
- **연결 즉시 중단:** `domiman_m.DomimanSession.stop()`이 소켓을
  `shutdown`+`close`해 블로킹 `recv`를 깨우고, 백오프 대기 중인 세션 스레드도
  `Event`로 깨운다(로그아웃/재접속 시 옛 스레드가 안 남게). **join하지 않으므로
  즉시 반환한다** — §2-1 참고.
- **접속 후 읽기 타임아웃(domichat 최대 함정):** `create_connection`이 준 6초
  타임아웃이 소켓에 남으면 조용할 때마다 끊겨 6초 주기 재접속 고리에 빠진다 →
  접속 직후 `settimeout(60)`으로 바꾸고 `SO_KEEPALIVE`를 켠다(서버 ping 15초).
- **TLS는 1.2로 고정.** 수신·송신 스레드가 한 소켓을 나눠 쓰는 구조라 1.3의
  핸드셰이크 후 메시지 때문에 record layer가 깨진다(`domichat.md` §1).
  자체 서명 인증서라 검증은 끄고 **지문 고정(TOFU)** 으로 신뢰하며, 지문은
  `LoginStore.fingerprints`("IP:포트" → SHA-256)에 저장한다. 지문이 바뀌면
  접속을 끊고 "서버 인증서가 바뀌었습니다…"를 띄운다. 서버가 평문이면 평문으로
  폴백한다(하위 호환).
- **첫 실행 권한 요청(MainActivity):** POST_NOTIFICATIONS(13+) → 콜백에서 배터리
  최적화 예외(`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`). `savedInstanceState
  == null`일 때만(회전 재요청 방지).

---

### 10. 앱 아이콘 = 둥근 록맨

PC의 `app.ico`(록맨 낚시 픽셀아트)를 **adaptive 아이콘의 배경 레이어로 꽉 채워**
One UI가 자동으로 둥글게 마스킹하게 했다(`mipmap-anydpi-v26/ic_launcher*.xml` →
background=`@drawable/ic_launcher_scene`, foreground=투명). 구버전용 밀도별
`mipmap-*/ic_launcher(.round).png`도 둥근 버전으로 생성(기존 webp·기본 로봇
아이콘 삭제). 위젯 아이콘과는 별개(위젯은 초록 원형 세트).

---

### 11. 권한 (AndroidManifest)

- `INTERNET` — domiserver TCP 47821(domichat 수/발신)
- `POST_NOTIFICATIONS` — 알림(13+)
- `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_SPECIAL_USE`,
  `FOREGROUND_SERVICE_DATA_SYNC` — 포그라운드 서비스
- `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — 절전 예외 요청
- `<service .DomimanService foregroundServiceType="specialUse|dataSync">` +
  `<property PROPERTY_SPECIAL_USE_FGS_SUBTYPE>`
- `<receiver .DomimanWidgetProvider>` + APPWIDGET_UPDATE + 위젯 메타

---

### 12. 다른 PC에서 재현하기 (프로젝트 소스는 이 리포에 포함됨)

안드로이드 프로젝트 소스가 이 리포의 **`domiman-android/`** 폴더에 들어 있다
(빌드 산출물 `build/`·`.gradle/`·기계별 `local.properties`는 제외 — 각 PC에서
생성됨). 다른 PC에서 같은 앱을 만들려면:

1. **리포를 ASCII 경로에 클론**하고 `domiman-android/`를 ASCII 전용 경로로
   옮긴다(예: `C:\Users\<계정>\dev\domiman-android`). ⚠️ **경로에 한글/비ASCII가
   있으면 AGP/Chaquopy 빌드가 거부된다.**
2. **도구 설치**(§2 참고): Android Studio(→ 내장 JBR), Android SDK 컴포넌트
   `platform-tools`·`build-tools/36.1.0`·`platforms/android-36`(+ 원하면
   `emulator`+`system-images/android-36/google_apis/x86_64`), python.org 정식
   Python(buildPython용, 현재 3.12). `ANDROID_HOME` 환경변수 지정.
3. **`local.properties` 생성**: `sdk.dir=<그 PC의 SDK 경로>` 한 줄(예:
   `C\:\\Users\\<계정>\\AppData\\Local\\Android\\Sdk`).
4. **buildPython 경로/버전 수정**: `app/build.gradle.kts`의
   `chaquopy.defaultConfig.version`과 `buildPython("C:/.../PythonXYZ/python.exe")`를
   그 PC에 있는 python.org 인터프리터로 맞춘다(**둘의 major.minor가 같아야 한다**).
5. **빌드**: `$env:JAVA_HOME="<Studio>\jbr"; .\gradlew.bat assembleDebug`
   → `app/build/outputs/apk/debug/app-debug.apk`.

빌드에 pip 의존성이 없으므로(§2) 오프라인에서도 파이썬 쪽은 걸리지 않는다.

원본 `domiman_m.py`는 리포 루트에 있고, 프로젝트 안 사본
(`domiman-android/app/src/main/python/domiman_m.py`)이 실제 빌드에 쓰인다 —
루트 원본을 고치면 사본에도 재복사(수동).

### 12-1. TODO — '예약 종료'를 '취침 모드'로 대체 (PC는 260909b에 완료)

PC판에 **취침 모드(암막)** 가 들어갔다(`CLAUDE.md`의 260909b 절 참고). 모바일도
같은 기능을 붙이기로 했고, **자리는 메인 화면의 `[예약 종료]` 버튼**이다.
아래는 다음 세션에서 그대로 집어 쓸 작업 목록이며 **아직 아무것도 구현하지
않았다**(이번 차례에는 PC만 고쳤다).

- [ ] **`[예약 종료]` 버튼의 기존 기능을 삭제**하고 같은 자리를 `[취침 모드]`로
      바꾼다. 분 입력 다이얼로그(`Y` → `Y,n` 2단계)와 `remote_exit_deadline`
      표시도 함께 걷어낸다. (PC판은 예약 종료를 그대로 유지한다 — 모바일만 교체)
- [ ] `domiman_m.py`에 **`cmd_sleep_mode()` = `B` 명령** 추가.
      PC 규격: 요청 `{대상},B` → 응답 `,Z,B`(에코). `send_command(body, kind)`
      한 겹 위에 얹고 `pending`은 다른 명령과 동일하게 15초.
- [ ] `dispatch()`/`dispatch_json()`의 **에코 분기에 `B` 추가**(`G/P/Y/W/Q`와
      같은 계열 — 상태 필드가 없는 에코다). 이 분기를 빼먹으면 응답이
      `parse_status`에서 버려져 pending이 안 풀리는 옛 버그가 재발한다.
- [ ] Kotlin: `DomimanRepository.sendSleepMode()` 추가 —
      **`suspend` + `Dispatchers.IO`** (블로킹 소켓 호출을 메인 스레드에서
      부르면 발신이 버벅인다는 기존 함정).
- [ ] `MainScreen`의 버튼 라벨·onClick 교체, `MainScreenViewModel`에서
      예약 종료 관련 상태 제거.
- [ ] 응답(`,Z,B`) 수신 시 로그 한 줄 + 필요하면 토스트. **알림 키는 만들지
      않는다**(사용자가 직접 누른 동작이므로).
- [ ] 예약 종료를 지우면 `Y`는 **PC에서는 계속 유효한 명령**이라는 점만 주의
      (다른 제어 측이 쓸 수 있으므로 PC 쪽 `Y` 처리는 건드리지 않는다).
- [ ] `domiman_m.py`를 고쳤으면 **사본 재복사**
      (`Copy-Item domiman_m.py domiman-android\app\src\main\python\`).

참고: 암막 자체는 **피제어 PC가 그리는 것**이므로 모바일이 할 일은 `B` 한 줄을
보내는 것뿐이다. 화면을 덮는 창·해제 조건(실제 입력·매크로 중지·원격 명령)은
전부 PC 쪽 `BlackoutOverlay`가 처리한다.

### 13. 아직 안 한 것 / 주의

- **실기기 종단간 검증은 아직 안 했다.** 260815f는 가짜 domiserver를 띄운
  파이썬 스모크 테스트(로그인/방 입장/room_missing/상태 파싱/G 에코/수량 응답/
  보고 변환/발신자 필터/`stop()` 즉시 반환/LoginStore 규칙 14항목)와 APK 빌드
  성공까지만 확인했다. 진짜 domiserver + 실제 폰 왕복은 다음 차례.
- **비밀번호는 앱 전용 SharedPreferences 평문 저장이다.** PC판은 DPAPI로
  암호화하지만 안드로이드에는 대응물이 없어(EncryptedSharedPreferences는
  deprecated) 앱 샌드박스 보호에 기대고 있다. 루팅된 기기에서는 읽힌다.
- 시스템 뒤로가기 버튼 실제 인터셉트(`OnBackPressedCallback`) — 지금은 화면 내
  '로그아웃' 버튼으로만 2단계 확인.
- `app/src/test`·`app/src/androidTest`의 두 파일은 **프로젝트 템플릿 잔해**로
  존재하지 않는 클래스를 참조한다(`assembleDebug`에는 영향 없음). 테스트를
  돌리려면 먼저 지우거나 새로 써야 한다.
- 안드로이드 프로젝트 GitHub 원격 연동(현재 로컬 git 백업만).
- 위젯 아이콘 다크모드 대비(초록 세트라 대체로 무난, 필요 시 벡터로 교체).
- **PC 프로토콜을 바꾸면** `domiman_m.py`의 `parse_status`(실행중 필드 위치),
  REPORT_TEXT/NOTIFY_KEYS를 반드시 같이 맞출 것. 그리고 원본 → 프로젝트 사본
  재복사 잊지 말 것.
- **domichat 규격이 바뀌면**(프레임·프레임 타입·에러 코드) `domiman_m.py`의
  복제된 클라이언트 계층(`ChatClient`)도 같이 고쳐야 한다. 규격의 단일 기준은
  `domichat.md`이며, `domichat.py`를 import하지 않는 이유도 거기 적혀 있다.


---

# 📄 도미맨 웹 원격제어 (GitHub Pages)

*(옛 `web.md` 를 여기로 합쳤다 — 문서는 이 파일 하나뿐이다.)*

## domiman 웹 원격제어 (`cheongbaek.github.io/domiman`)

브라우저에서 피제어 PC(낚시 매크로)를 조작한다. **모바일 앱(`domiman_m.py` +
Kotlin)의 메인 화면과 같은 기능**을 PC·휴대폰 한 화면으로 제공하며, 위젯·알림은
없다(브라우저에는 포그라운드 서비스가 없다).

**260913a부터 domichat 채팅도 같은 연결로 한다** — 상단 '채팅방 목록' 칸을 누르면
화면 전체가 방 목록으로 바뀐다(아래 '채팅' 절).

| 파일 | 역할 |
|---|---|
| `domiserver.py` [8. 웹 중계] | 중계가 **이 파일 안에** 있다(260914a 통합). 서버에는 `web` 계정으로 **127.0.0.1 에 붙어서** 말한다(260914d) |
| `web/` | 정적 웹앱(Vite + React + TS). GitHub Pages로 배포 |
| `.github/workflows/deploy-pages.yml` | `web/**` 변경 시 Pages 배포(`BASE_PATH=/domiman/`) |

```
브라우저 ──wss://domiman.duckdns.org:47822/ws──▶ domiserver.py (중계 + 채팅 서버)
  (GitHub Pages에서 받은 정적 파일)                     │  HubConn(가상 연결)
                                                        ▼
                                    방 domi_fishing_{PC} ◀── 피제어 PC (TLS 47821)
```

### 260914a — 따로 돌던 프로세스를 서버 안으로 넣었다

예전에는 `domiweb.py`가 옆에서 따로 떠서 **도로 이 서버에 127.0.0.1로 TLS 소켓을
열고** `web` 계정으로 로그인했다. 한 프로그램이 되면서 그 왕복이 통째로 사라진다.

중계는 소켓 대신 **`HubConn`(가상 연결)** 으로 서버 안에 자리를 잡는다 — 큐 하나를
달고 있을 뿐, 서버가 보기에는 평범한 로그인된 연결이라 입장 자격·방 비밀번호·
팬아웃·도배 제한·이미지 중계를 **기존 `handle_*` 가 그대로 처리한다.** 웹 전용
경로를 따로 만들지 않는 것이 요점이다(방 규칙의 주인을 둘로 늘리지 않는다).

없어진 것: 상류 접속·재접속 백오프·지문 고정(TOFU)·`web` 계정과 비밀번호·
`domiweb.json`·`already_online` 충돌. 브라우저 ↔ 중계 규격은 **한 글자도 바뀌지
않았다**(웹앱은 GitHub Pages에 이미 배포돼 있어 서버만 고칠 수 있다).

설정은 `domiserver.json` 의 `web`·`web_port`·`web_certfile`… 로 옮겼고, 옆에 옛
`domiweb.json` 이 남아 있으면 **첫 실행에 한 번 자동으로 옮겨 온다**
(`import_domiweb_config` — 손으로 적은 인증서 경로를 잃지 않기 위해서다).

### 한 파일 안에 있지만, 붙는 방식은 바깥 클라이언트와 같다 (260914d)

중계는 `domiserver.py` 안에 있다. 그렇다고 **서버 함수를 직접 부르지는 않는다** —
`WebUplink` 가 **127.0.0.1:47821 로 소켓을 열어 `web_id`/`web_pw` 계정으로 진짜
로그인**하고, 그 뒤로는 domichat 클라이언트와 똑같은 프레임만 주고받는다.

| | 260914a~c (가상 연결) | 지금 (127.0.0.1 로그인) |
|---|---|---|
| 서버에 말하는 법 | `HubConn` 을 만들어 `handle_*` 직접 호출 | 소켓 + `login` → 평범한 연결 하나 |
| 계정 | 이름만 `ONLINE` 에 올림(비밀번호 없음) | **진짜 계정**(`approve` 해 둬야 한다) |
| 방 목록 | DB(`rooms_snapshot`) 직접 읽기 | `rooms` 프레임으로 받음 |
| 서버 쪽 예외 | 가짜 연결(ready 강제·`last_rx` 수동 갱신·팬아웃 바이트 되풀기) | **없음** |

**왜 되돌렸나(사용자 확정):** 가상 연결은 계정을 없앤 것이 아니라 *계정 흉내*였다.
그럴 바에는 계정 하나로 진짜 붙는 편이 낫다 — 입장 자격·비밀번호·팬아웃·도배
제한·이미지 중계가 바깥 클라이언트와 **글자 하나 다르지 않은 길**을 타고, 서버가
자기 자신을 특별 대우하지 않는다. 중계가 죽어도 채팅 서버는 멀쩡하다.
대가는 계정 하나와 loopback 왕복인데 같은 기계라 무시할 수 있다.

**설치할 때 한 번 할 일:** 클라이언트에서 `web` 계정을 만들고 서버 콘솔에서
`approve web`, 그 다음 `set web_pw <비밀번호>`. 계정이 없거나 비밀번호가 다르면
중계를 **열지 않고**(채팅 서버는 그대로 돈다) 무엇을 해야 하는지 로그에 적는다.
서버 TLS 가 켜져 있으면 상류도 TLS 로 붙고, 자체 서명이라 **우리 인증서 지문과
직접 대조**한다(자기 자신이니 지문을 안다 — TOFU 파일이 필요 없다).

### 왜 중계인가 (설계 결정 — 되돌리지 말 것)

1. **브라우저는 raw TCP를 열 수 없다.** domichat은 길이접두 TCP 프레임이라
   그대로는 붙지 못한다 → WebSocket 한 겹이 반드시 필요하다.
2. **브라우저는 지문 고정(TOFU)을 할 수 없다.** domiserver의 자체 서명 인증서로는
   `wss://`가 **경고 없이 그냥 실패**한다(사용자가 예외를 누를 화면조차 없다).
   그래서 domiweb만 **브라우저가 신뢰하는 인증서**(Let's Encrypt)를 든다.
   GitHub Pages는 항상 https이므로 `ws://` 평문은 혼합 콘텐츠로 차단된다.
3. **domiserver는 같은 ID 동시 접속을 불허한다.** 브라우저마다 로그인시키면 기기
   하나만 쓸 수 있다 → 중계가 **연결 하나**(`HubConn`)로 자리를 잡고 브라우저
   여러 대를 그 하나에 **다중화**한다. 덕분에 **웹앱에는 계정도 비밀번호도
   없다**(공개 정적 사이트에 자격을 박는 문제가 구조적으로 사라진다).
   그 이름(`web_id`, 기본 `web`)은 `ONLINE`에 올려 두므로 **바깥에서 같은 ID로
   로그인하는 것도 막힌다** — 중계를 사칭할 통로를 남기지 않는다.
4. **domiman.py는 한 줄도 고치지 않는다.** 피제어 PC는 `{sender},Z,...`로 답하고
   sender는 서버가 찍는 `from`이므로, 중계가 끼어도 규격이 그대로다.

### 프로토콜

- 중계 ↔ 방: `domichat.md` 규격 그대로(방 `domi_fishing_{PC}`, 고정 비번).
  같은 프로세스 안이지만 **프레임은 그대로 흐른다** — `HubConn` 이 `handle_join`·
  `handle_msg` 를 그대로 통과하고, 팬아웃이 돌려주는 프레임을 큐로 받는다.
- 브라우저 ↔ 중계: JSON over WebSocket.
  - 받는 것: `hello` / `select{pc}` / `cmd{pc,body}` / `add_pc{pc}` / `del_pc{pc}`
  - 주는 것: `ready` / `snap` / `msg{pc,body}` / `pcs` / `pc` / `up` / `shot` / `err`
- **방에서 온 원문(`web,Z,...` 등)은 해석하지 않고 그대로 넘긴다.** 파싱은
  `web/src/lib/protocol.ts`가 한다 — 규격의 소유자를 늘리지 않기 위해서다.
  중계가 구분하는 것은 캐시를 위한 접두어 셋(상태 응답·수량 방송·보고)뿐이다.
- 예외는 **스크린샷**이다: `'B'` 이진 청크를 중계가 조립해 크기·sha256을 확인한
  뒤 완성된 PNG를 base64로 넘긴다(브라우저에 청크 조립 로직을 또 두지 않는다).
  사진은 **요청한 브라우저에게만** 준다(남이 찍은 3MB가 갑자기 뜨지 않게).
  채팅 이미지도 **같은 조립기**를 쓴다(`file_begin`의 방 이름으로 갈린다).

#### 채팅 프레임 (260913a)

  받는 것: `rooms` / `room_open{room,pw?}` / `room_close{room}` /
           `chat{room,body,cid}` / `img_begin{room,fid,name,size,sha256}` /
           `img_chunk{fid,seq,b64}` / `img_end{fid}`
  주는 것: `rooms{list}` / `room{room,state,...}` / `chat{...}` / `chat_img{...}` /
           `chat_hist{room,items}` / `chat_err{room,msg}`

`room.state` = `joined|denied|closed|deleted|kicked|member|approve_res`.
`denied.reason`은 domichat 규격 그대로(`bad_pw_room`·`not_allowed`·
`await_approval`·`blocked`)이며 문장으로 바꾸는 표는 `web/src/lib/chat.ts`에 있다.

### 웹 중계의 함정 (이미 값을 치른 것들)

| 항목 | 이유 |
|---|---|
| **브라우저용 TLS도 1.2 고정** | 연결마다 수신 스레드와 송신 스레드가 한 소켓을 나눠 쓴다 — 1.3의 NewSessionTicket/KeyUpdate가 record layer를 깬다(`_pin_tls12` 와 같은 이유, 같은 구조) |
| 발신 스로틀 `12건/10초` | 서버의 `MSG_BURST=20/10초`는 **연결 하나당** 걸리는데, 브라우저 여러 대의 명령이 중계 연결 **하나로 합쳐진다.** 한 프로그램이 되어도 `handle_msg` 를 그대로 통과하므로 이 여유는 그대로 필요하다 |
| 중계 연결도 `last_rx` 를 갱신한다 | `maintenance_loop` 가 15초마다 `ping` 을 보내고 45초 무응답이면 연결을 회수한다. `_pump_loop` 가 프레임을 받을 때마다 `last_rx` 를 갱신하지 않으면 **중계가 스스로 회수당한다**(실제 연결이 `serve_conn` 에서 하는 일과 같다) |
| 중계 프레임은 **한 스레드**(`_uplink_loop`)에서만 내보낸다 | `handle_msg` 의 도배 카운터(`conn.msg_times`)는 연결 하나를 자기 스레드만 건드린다고 보고 짜여 있다 |
| 명령 화이트리스트(`WEB_CMD_RE`) | 누구나 붙는 공개 중계다. 방에 흘려보낼 문자열을 domiman 명령 규격으로만 제한해 채팅방 스팸 통로가 되지 않게 한다 |
| 설정을 `utf-8-sig`로 읽음(승계) | PowerShell `Set-Content -Encoding UTF8`은 **BOM을 붙인다.** 그냥 utf-8로 읽으면 옛 `domiweb.json` 이 조용히 안 읽혀 인증서 경로가 비고 평문 ws로 열린다 |
| 인증서 mtime 감시 → 자동 재적재 | Let's Encrypt는 60~90일마다 갱신된다. 재시작이 필요한 구조면 어느 날 조용히 만료된다 |
| 포트를 못 열어도 **서버는 계속 돈다** | 채팅 중계가 웹 때문에 죽어서는 안 된다. `start_web_relay()` 는 `None` 을 돌려주고 로그만 남긴다 |
| **지목한 PC의 방에만 입장** | 시작할 때 목록의 방을 전부 잡아 두면 보지도 않는 PC의 수량 방송을 사이클마다 받고, 그 방 참가자 목록에도 `web`이 늘 떠 있게 된다. 동시에 여러 PC를 돌릴 일이 없다 |
| 마지막 사람이 떠나면 퇴장 + 캐시 폐기 | 다시 들어갈 때 옛 상태를 잠깐 보여주면 '지금 값'으로 오해한다(입장 직후 `S` 질의로 새로 받는다). 대신 **수량은 다음 방송까지 `–`로 보인다**(최대 한 폴링 사이클) — 바로 보려면 '실시간 수량확인'을 쓴다 |
| 방이 생기면 `room_new` 로 바로 붙는다 | 그 PC가 domichat에 접속한 적이 없으면 방이 없다. 예전에는 60초마다 목록을 다시 받아야 알 수 있었는데, 한 프로그램이 되면서 **방 생성 통지가 그대로 들어온다.** 60초 재시도는 그 통지를 놓쳤을 때의 안전망으로 남겼다 |

### 채팅은 **다른 사이트로 떼어 냈다** (260914e)

채팅은 이제 `https://cheongbaek.github.io/domichat/` 에 있다. 이 앱(`/domiman/`)에는
'채팅방 목록' 칸이 **없다** — 하단 안내줄의 '채팅 열기' 링크뿐이다.

| | 낚시 원격제어 | 채팅 |
|---|---|---|
| 주소 | `cheongbaek.github.io/domiman/` | `cheongbaek.github.io/domichat/` |
| 소스 | 이 저장소 `web/` | **`cheongbaek.github.io` 저장소의 `domichat-src/`** (빌드 결과가 같은 저장소 `domichat/` 에 떨어지고 브랜치 그대로 배포된다 — 워크플로가 없다) |
| 붙는 곳 | 같은 중계(`domiserver.py` [8]) — 프레임 규격도 같다 |

아래 절은 그 채팅 화면의 규칙이며, 옮겨간 곳에서도 **그대로 유효**하다(파일만
복사됐다). 규격을 고치면 두 곳을 같이 봐야 한다: 중계(`domiserver.py` [8])와
`domichat-src/src/`.

### 채팅 화면의 규칙 (260913a — 지금은 /domichat/ 에 있다)

상단 '채팅방 목록' 칸 → **화면 전환**(창이 아니다 — 휴대폰에서 더 많이 쓴다).
목록에서 방을 누르면 들어가고, 방 안에서는 글과 이미지를 주고받는다. 낚시 제어
화면으로는 `←`로 돌아오며, **그동안에도 제어 상태는 계속 갱신된다**(중계는 여전히
그 PC의 방을 보고 있다).

**폭에 따라 배치가 갈린다(모바일 우선이지 모바일 전용이 아니다):**

| 폭 | 배치 |
|---|---|
| < 900px (휴대폰) | 한 번에 하나 — 목록 ↔ 채팅방을 오간다. 방 머리의 `←`가 목록으로 돌아간다 |
| ≥ 900px (PC) | **목록(300px)과 대화가 나란히** 선다. 목록에서 바로 다른 방으로 옮길 수 있고, 열려 있는 방은 목록에 표시된다(왼쪽 강조선). 방 머리의 `←`는 숨고 오른쪽에 **[나가기]** 가 선다 — 목록으로 '돌아가는' 일이 없어졌으니 남는 동작은 **방을 뜨는 것**뿐이다(보는 사람이 없어지면 중계가 그 방에서 나온다) |

두 화면은 **항상 함께 그려 두고 CSS가 감춘다**(`.pane.hide`). 그래야 넓은 화면에서
방을 옮길 때 목록이 다시 그려지지 않고, 좁은 화면에서도 코드가 한 갈래로 유지된다.

| 정한 것 | 이유 |
|---|---|
| **입장만 한다 — 방 만들기·방장 도구는 없다**(사용자 확정) | 웹은 중계의 계정 하나(`web`)를 쓰므로 방장이 될 일이 없다. 승인 대기·블랙리스트·비번 변경·방 삭제는 domichat(PC)에서 한다 |
| 입장 자격은 **`web` ID 기준** | 사후 승인 방은 방장이 **`web`을 승인**해야 들어간다. 그래서 목록 화면에 내 ID를 크게 적어 둔다 |
| 다른 브라우저가 보낸 말도 '내 말'로 보인다 | 계정을 공유하는 구조의 필연. 웹앱에 자격을 두지 않기로 한 대가이며 제어 기능과 같다 |
| **구독·알림 없음** | 브라우저에는 포그라운드 서비스가 없다. 보는 사람이 없으면 중계가 방에서 나오고, 서버는 대화를 저장하지 않으므로 그동안의 대화는 공백으로 남는다(domichat.md §4와 같은 규칙) |
| **제어용 방(`domi_fishing_*`)은 목록에서 감추고 입장도 막는다** | ① 사이클마다 오는 수량 방송이 대화창을 뒤덮는다 ② 그 방에 아무 문자열이나 흘리면 명령 화이트리스트(`CMD_RE`)를 우회하는 통로가 된다 |
| 방 비밀번호는 **메모리에만** | 디스크에 남기지 않는다(domichat도 '구독한 방만' 기억하고 웹에는 구독이 없다). 중계 쪽은 마지막 사람이 방을 닫을 때 함께 버리고, 브라우저 쪽은 탭을 닫으면 사라진다. 상류가 끊겼다 붙을 때 **말없이 재입장**하는 데 쓴다 |
| 대화 기록은 **중계가 방마다 80줄** 들고 있다 | 나중에 붙은 브라우저·재입장에 그동안의 대화를 줘야 하기 때문(한 연결을 여러 브라우저가 나눠 쓰는 구조라 '입장 시점'이 브라우저마다 다르다). **이미지는 넣지 않는다** — base64가 메모리를 먹고, 사람은 그 자리에 없었던 사진까지 볼 이유가 없다 |
| 이미지는 브라우저가 **PNG로 바꿔** 올린다 | domichat.py와 같은 규칙(받는 쪽 tkinter가 PNG/GIF만 그린다). 긴 변 2560px로 줄이고, 그래도 상한을 넘으면 더 줄여 다시 인코딩한다 |
| 올린 이미지는 **중계가 자기 브라우저들에게 되돌려준다** | 서버는 보낸 연결을 팬아웃에서 제외한다(`exclude=conn`). 그래서 올린 본인도, **같은 중계에 붙은 다른 브라우저도** 상류에서는 받지 못한다 |
| 업로드는 **base64 48,000자 조각**으로 올린다 | 중계의 `WEB_MAX_RX`가 64KB다(원본 36,000바이트). 중계가 그것을 모아 방에는 64KB `'B'` 청크로 다시 나눠 보낸다 |
| 발신 스로틀이 **두 겹** | 상류 스로틀(`SEND_BURST`)은 연결 전체를 지키는 것이고, `CHAT_BURST=8/10초`는 **브라우저 한 대**가 그 몫을 다 먹어 제어 명령이 밀리는 것을 막는다 |
| 이미지 청크는 `_uplink_loop` 가 특별히 받는다 | `'B'` 프레임은 JSON 핸들러(`HANDLERS`)에 없다. 그래서 큐에 `("_chunk", fid, seq, data)` 로 실어 보내고 그 자리에서 `relay_file_chunk` 로 넘긴다 — 바깥 클라이언트의 이진 프레임이 가는 길과 같다 |
| `web_chat` 설정으로 끌 수 있다 | 공개 중계에 채팅을 얹으면 **누구나 중계 계정으로 공개방에 말할 수 있다**(명령 화이트리스트가 막아주던 범위 밖이다). 그게 싫으면 `domiserver.json`의 `"web_chat": false` — 목록·입장·발신이 전부 거절된다 |
| 중계가 **`ready`에 `chat: true`를 실을 때만** 웹앱에 칸이 뜬다 | 웹앱은 Pages로 먼저 배포되고 중계는 나중에 올라갈 수 있다. 그 사이에 버튼만 떠 있으면 눌러도 아무 방이 안 나오는 죽은 칸이 된다 — **서버가 준비되면 저절로 나타난다** |

**옛 서버(ver 1)에는 이미지를 보내지 않는다** — `welcome.ver`를 보고 `< 2`면 그
자리에서 거절한다. 'B' 프레임을 받는 즉시 연결을 끊는 서버가 있어 **1초 간격
재접속 고리**에 빠진 사고가 domichat.md에 남아 있다(상류가 끊기면 큐에 남은 청크도
`drop_chunks()`로 버린다 — 같은 이유).

### 웹앱 쪽 함정

- **서비스워커를 넣지 않는다.** 같은 오리진(`cheongbaek.github.io`)에 남은 옛 PWA
  워커가 화면을 캐시에서 돌려주던 사고가 `find_wc`에 기록돼 있다. `main.tsx`가
  기존 등록을 걷어내는 그물까지 둔다.
- **스냅샷 재생은 응답 대기(pending)를 풀지 않는다**(`applyBody(body, hist=true)`).
  나중에 붙은 브라우저가 옛 응답으로 pending을 소모하면 UI가 잘못 열린다.
- **수량 방송(`,Z,N,*`)도 pending을 풀지 않는다** — 요청 없이 사이클마다 온다.
- 모바일 브라우저는 백그라운드 탭의 소켓을 얼린다 → `visibilitychange`에서 즉시
  재접속하고 `select`로 상태를 다시 받는다(앱의 `resync`와 같은 취지).
- 사진 Blob URL은 새 사진을 받을 때·창을 닫을 때 `revokeObjectURL`한다(안 풀면
  사진마다 2MB가 남는다).
- 테마는 세 상태(system/light/dark)다. 색은 **모두 bare `:root`에 정의**하고
  다크에서 같은 토큰만 덮어쓴다 — 미디어쿼리 안에만 정의하면 토글이 깨진다.
- 접속 주소는 `web/src/lib/relay.ts`의 `DEFAULT_WS` 한 줄이며, 개발 중에는
  `?ws=ws://localhost:47822/ws`로 덮어쓴다.

### BGOD 설치 절차

**계정을 만들 필요가 없다**(260914a). 중계가 서버 자신의 일부라 로그인을 하지
않는다 — 옛 `web` 계정은 그냥 두어도 되고 `deluser web` 해도 된다.

1. **호스트명·인증서**: DuckDNS(`domiman.duckdns.org` → 211.196.44.3) +
   Posh-ACME(DNS-01, `-UseSerialValidation` 필수 — DuckDNS는 TXT를 하나만 둔다).
   `Submit-Renewal`을 매일 도는 작업으로 걸어 둔다.
2. `domiserver.json` 에 인증서 경로를 적는다
   (`web_certfile` 은 **`fullchain.cer`**, `web_keyfile` 은 `cert.key`).
   옛 `domiweb.json` 이 옆에 있으면 첫 실행에 자동으로 옮겨 오므로 손댈 것이 없다.
3. `domiserver.py` 를 평소처럼 띄운다 — **중계는 같이 뜬다**(끄려면 `set web 0`).
   콘솔 `web` 으로 수신 주소·브라우저 수·피제어 PC 상태를 볼 수 있고,
   `web add/del <PC ID>` 로 목록을 고친다.
4. 브라우저로 `https://domiman.duckdns.org:47822/` → `domiserver ... 웹 중계 —
   살아 있습니다`와 **경고 없는 자물쇠**가 보이면 인증서가 먹은 것이다.
   여기서 실패하면 웹앱도 못 붙는다.
5. GitHub: `Settings → Pages → Source = GitHub Actions`.

※ 관리 창(`domiserver_gui.py`)으로 띄워도 중계가 같이 뜬다(`start_server` 가
`start_web_relay()` 를 부른다). **둘을 동시에 띄우지 말 것** — 47821·47822 를
두 번 열 수 없다.

### 검증 기록 (260914d — 중계가 127.0.0.1 로 로그인)

- 평문 서버: 중계가 `web` 계정으로 붙는 것을 **서버 로그(`[로그인] web`)로 확인**한
  뒤 채팅 **22항목** 전부 통과(목록·입장·대화·cid 에코·이미지 양방향 sha256·승인
  왕복·방 삭제·제어용 방 거절·재입장 대화 복원·마지막 사람 퇴장·낚시 제어 회귀·
  길이/연타 차단).
- **TLS 서버**(자체 서명, 지문 대조): 상류 소켓이 실제로 TLS 이고 로그인 성공,
  브라우저까지 방 목록 왕복, **12초 방치 후에도 연결 유지**(읽기 타임아웃 함정).
- 실제 Chromium **28항목**(430px·1280px, 라이트/다크) 전 흐름 통과.

### 검증 기록 (260914b — 채팅, 통합된 서버 기준)

격리 서버 사본 하나(평문 47899 + 웹 47898, 중계가 그 안에 있다)에 피제어 PC 역할
domichat 클라이언트와 브라우저 흉내(RFC 6455 최소 구현)를 붙여 **22항목**:
- 방 목록(제어용 방 숨김·유형·입장 가능 여부), 공개방 입장, 발신·수신, **cid 에코**
- 이미지 **양방향 sha256 일치**(받기 120KB / 보내기 150KB), 올린 이미지를 **같은
  방의 다른 브라우저도** 받는 것(서버가 보낸 쪽을 팬아웃에서 제외하므로 중계가 직접 준다)
- 비밀번호 방(틀린 비번 → `bad_pw_room`, 맞는 비번 → 입장), 사후 승인
  (`await_approval` → 방장 승인 → `approve_res` → 입장), 방 삭제, 제어용 방 거절
- 나중에 붙은 브라우저에게 대화 되돌려주기, **마지막 사람이 나가면 방에서 나오고
  남은 사람이 있으면 지키는 것**
- 회귀: 낚시 제어 경로(입장·`S` 질의·상태 응답·수량 방송), 명령 화이트리스트,
  메시지 길이·연타 차단

실제 Chromium으로 **28항목**(휴대폰 폭 430px + PC 1280px, 라이트/다크):
상단 칸 → 목록 → 비번 창(오답·정답) → 채팅방 → 발신(✓ 표시) → 수신 → 이미지
받기/보내기 → 크게 보기 → 목록 → 낚시 화면 복귀(살림망 표시 유지), 그리고 폭별
배치(430px 한 칸 / 1280px 두 칸, 목록에서 바로 방 옮기기, 열린 방 표시,
`←`↔[나가기] 교체). 채팅을 끈 중계(`web_chat: false` 또는 옛 버전)에 붙여도 **칸은
그대로 있고** 들어가면 이유가 적히는 것까지 확인했다 — 상태에 따라 칸이 사라지면
쓰는 쪽에서는 UI가 고장 난 것으로 보인다.

**이 과정에서 고친 것:**
- 상단 바를 **접히게**(`flex-wrap`) 했다 — 칸이 하나 늘면서 430px에서 연결 상태
  글자가 세 줄로 쪼개지고 살림망 칸이 화면 밖으로 잘렸다.
- 이미지 버튼은 **글자('사진')** 다 — 그림 문자(🖼)는 글꼴에 따라 두부(□)로 나온다.
- 방 이름 **찾기 칸은 뺐다** — domichat 리스트에도 없고 방이 몇 개 안 된다.
- 통합본은 방 목록을 **DB에서 바로 만든다**(`rooms_snapshot`). 옛 중계처럼 서버에
  `rooms` 프레임을 보내 답을 기다리지 않으므로 왕복이 사라졌고, 옛 중계에서 겪던
  '입장 요청이 겹쳐 `S` 질의가 두 번 나가는' 자리도 없다(입장 자체에는 그대로
  5초 중복 방지를 둔다 — `submit` 은 비동기라 답이 오기 전에 또 청할 수 있다).

### 검증 기록 (260914a — 통합 후 e2e, 41항목 전부 통과)

격리 폴더에 `domiserver.py` 사본을 두고(실서버 DB를 건드리지 않는다) 평문 47899 /
웹 47898로 띄워, 피제어 PC 역할의 domichat 클라이언트와 브라우저 역할의 WebSocket
클라이언트를 실제로 붙여 확인했다:
- 방 개설 → 중계 자동 입장 → **입장 직후 `S` 자동 질의**까지 왕복
- 명령 `N` 전달, 상태 응답·수량 방송·보고 **원문 그대로** 전달 + 캐시
- 규격 밖 명령·목록에 없는 PC 차단
- 스크린샷 300KB: `file_begin` → `'B'` 청크 → `file_end` 조립, **바이트 완전 일치**
- 명령 16건 연속: 스로틀이 10초 창을 넘겨 내보내 **`rate_limited` 0건**
- 피제어 PC 종료 → `online=false` 통지 / 마지막 브라우저 퇴장 → 방 퇴장 + 캐시 폐기
- `ping_sec=3`·`pong_timeout_sec=9` 로 줄인 채 12초 방치 — **중계 연결이 회수되지
  않음**(`last_rx` 갱신이 듣는다)
- 방이 없는 PC는 `no_room`, 그 PC가 뒤늦게 켜지면 `room_new` 로 **즉시 입장**
- 바깥에서 같은 ID(`web`)로 로그인 시도 → `already_online` 거절
- 평범한 GET → 안내 페이지 200
- `domiweb.json` 승계: 포트·이름·PC 목록·인증서 경로가 옮겨오고 상류 전용 키
  (`server`/`pw`)는 안 옮겨오며, **두 번째 실행에서는 다시 덮어쓰지 않는다**

※ 이 검증은 전부 **loopback(127.0.0.1) 한 프로세스 안**이라 공인 IP·포트포워딩·
공인 인증서가 필요 없다. `wss` 실제 핸드셰이크는 아래 260902a 기록을 승계한다
(브라우저용 TLS 경로는 그대로 옮겨왔고 `_pin_tls12` 로 합쳐졌을 뿐이다).

### 검증 기록 (260902a — 통합 전, domiweb.py 시절)

격리 서버(`domiserver.py` 사본, 포트 47899 평문) + 피제어 PC 흉내로:
- 명령 왕복(S/G/P/W/N/V/C/T/Y/I), 상태·수량 방송·보고 전달
- 브라우저 2대 동시 접속 — 방 메시지는 둘 다, **사진은 요청자만**
- 스크린샷 150KB **sha256 일치**, 규격 밖 명령 차단, PC 목록 추가·삭제
- `wss`(TLS 1.2) 핸드셰이크, 인증서 파일 교체 시 재적재
- 실제 Chromium으로 라이트/다크 · PC(1280px)/모바일(430px) 두 폭에서 전 흐름
  (수량확인 → 시작/중지 → 스크린샷 창 → 해상도 대화상자 → 예약 종료 2단계)
  **콘솔 오류 0**


---

# 📄 PC판 빌드 & 배포 가이드

*(옛 `배포_가이드.md` 를 여기로 합쳤다 — 문서는 이 파일 하나뿐이다.)*

## 낚시 매크로 — 빌드 & 배포 가이드

`낚시.py`를 **PyInstaller onedir(분할)** 로 패키징하고, easyocr 모델을 미리
동봉해 **오프라인/네트워크 불안정 환경에서도 동작**하게 만든 뒤, **Inno Setup
exe 설치 프로그램**(권장) 또는 **MSIX**로 배포하는 전체 절차.

관련 파일(모두 리포지토리에 있음):
- `build_fishing.spec` — PyInstaller onedir 스펙 (UPX 금지, easyocr/torch/cv2/windows-capture 통째 수집)
- `requirements-build.txt` — 빌드용 의존성(핀 고정, Python 3.14 검증 완료)
- `installer.iss` — Inno Setup 설치 스크립트
- `ocr_model/` — 동봉할 easyocr 모델 (`craft_mlt_25k.pth`, `korean_g2.pth`)

> ⚠ **반드시 python.org 정식 Python(가상환경)에서 빌드**할 것.
> Microsoft Store Python으로는 PyInstaller가 정상 동작하지 않는다.

---

### 0. 빌드 환경 준비 (한 번만)

python.org Python 3.14 기준. 이미 만들어 둔 `venv314`가 있으면 이 절은 건너뛴다.

```powershell
# 3.14 가상환경 생성
& "C:\Users\<사용자>\AppData\Local\Python\pythoncore-3.14-64\python.exe" -m venv venv314
.\venv314\Scripts\Activate.ps1

# torch/torchvision은 CPU 휠로 별도 설치(easyocr 의존)
pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cpu

# 나머지 빌드 의존성
pip install -r requirements-build.txt

# ★ opencv 중복 정리 (중요)
#   opencv-python(GUI판)과 opencv-python-headless는 같은 'cv2' 폴더를 공유한다.
#   GUI판만 uninstall하면 cv2 파일까지 지워져 cv2가 통째로 죽으므로,
#   반드시 headless를 강제 재설치해 cv2를 복구한다.
pip uninstall -y opencv-python
pip install --force-reinstall --no-deps opencv-python-headless==5.0.0.93
```

**설치 검증(선택):**
```powershell
.\venv314\Scripts\python.exe -c "import cv2, easyocr, windows_capture, torch, win32gui; print('imports OK', cv2.__version__)"
```

---

### 1. easyocr 모델 사전 다운로드 (네트워크 불안정 대비) ★핵심

`낚시.py`는 OCR 리더를 이렇게 만든다:

```python
reader = easyocr.Reader(['ko', 'en'], gpu=False,
                        model_storage_directory=OCR_MODEL_DIR,   # <exe>/ocr_model
                        download_enabled=False)                  # 다운로드 금지
```

즉 **런타임에는 절대 인터넷에서 모델을 받지 않는다.** 그래서 배포 전에 `ocr_model/`
폴더에 모델이 반드시 들어 있어야 하고, 스펙이 이 폴더를 exe 안에 동봉한다
(`datas += [('ocr_model', 'ocr_model')]`). 이것이 "네트워크 불안정 환경 대응"의
핵심 — **모델을 패키지에 구워 넣어** 대상 PC에서 다운로드 자체를 없앤다.

#### 필요한 모델 (한글+영문)
| 파일 | 용도 | 크기 |
|---|---|---|
| `craft_mlt_25k.pth` | 텍스트 **검출**(CRAFT) | ~79 MB |
| `korean_g2.pth` | 한글+영문 **인식** (ko 모델이 en도 커버) | ~15 MB |

> `['ko','en']` 조합은 `korean_g2.pth` 하나로 한글·영문을 모두 인식한다.
> 별도의 `english_g2.pth`는 필요 없다.

#### 이미 있는지 확인
```powershell
Get-ChildItem ocr_model
```
위 두 파일이 보이면 **이 절은 끝**이다. 다음(2번)으로 진행.

#### 없거나 새로 받아야 할 때 — 인터넷 되는 PC에서 1회 실행
아래를 `download_ocr_models.py`로 저장해, **네트워크가 되는 환경에서 한 번** 실행하면
`ocr_model/`이 채워진다. 그 폴더를 그대로 리포지토리/빌드 머신에 복사한다.

```python
# download_ocr_models.py — 인터넷 되는 곳에서 1회 실행해 ocr_model/ 채우기
import os, easyocr
os.makedirs("ocr_model", exist_ok=True)
easyocr.Reader(['ko', 'en'], gpu=False,
               model_storage_directory="ocr_model",
               download_enabled=True)   # ← 여기서만 True (다운로드 허용)
print("완료: ocr_model/ 에 모델 저장됨")
```
```powershell
.\venv314\Scripts\python.exe download_ocr_models.py
Get-ChildItem ocr_model   # 두 .pth 확인
```

> 네트워크가 계속 끊긴다면, 다른 PC에서 위를 돌려 만든 `ocr_model/`을 USB 등으로
> 옮겨 넣어도 된다. 결국 필요한 건 저 두 `.pth` 파일뿐이다.

---

### 2. exe 분할 패키징 (PyInstaller onedir)

`venv314` 활성화 상태에서:

```powershell
pyinstaller build_fishing.spec --noconfirm --clean
```

- 결과물: **`dist\FishingMacro\`** 폴더
  - `FishingMacro.exe` (콘솔 실행 파일)
  - `_internal\` (모든 DLL/데이터/모델이 여기 분산 — "분할" 구조)
  - 모델은 `_internal\ocr_model\`에 들어가고, 런타임엔 `sys._MEIPASS/ocr_model`로 접근된다.

#### ★ OneDrive 잠금 회피 (이 프로젝트 필수)
이 프로젝트는 **OneDrive 동기화 폴더** 안에 있다. 산출물(약 875MB)을 OneDrive가
동기화하려고 파일 핸들을 잡는 순간, PyInstaller가 기존 `build\`·`dist\`를 지우려다
**접근 거부(WinError 5: Access denied)** 로 빌드가 실패한다. 안정적으로 빌드하려면:

1. **`build\`·`dist\`를 먼저 수동 삭제**(재시도 포함). `--clean`에 맡기면 잠금에 걸린다.
2. **작업 폴더(workpath)를 OneDrive 밖으로** 빼서 빌드.

```powershell
# 1) 남은 프로세스 정리 + 기존 산출물 수동 삭제(재시도)
Get-Process -Name FishingMacro -ErrorAction SilentlyContinue | Stop-Process -Force
foreach ($d in "build","dist") {
  for ($i=0; $i -lt 5 -and (Test-Path $d); $i++) {
    try { Remove-Item $d -Recurse -Force -ErrorAction Stop } catch { Start-Sleep 2 }
  }
}
# 2) workpath를 OneDrive 밖으로 지정해 빌드(distpath는 프로젝트 유지 → installer가 참조)
.\venv314\Scripts\python.exe -m PyInstaller build_fishing.spec --noconfirm --workpath "$env:LOCALAPPDATA\FMbuild"
```
> 그래도 간헐적으로 잠기면 OneDrive를 잠시 "동기화 일시 중지"한 뒤 빌드한다.

#### 관리자 권한 (uac_admin=True)
스펙의 `EXE(... uac_admin=True ...)`로 exe 매니페스트에 `requireAdministrator`가
박혀 있어, **실행 시 항상 UAC 승격**된다. 게임이 관리자 권한으로 돌 때 UIPI(User
Interface Privilege Isolation) 때문에 권한 없는 프로세스는 게임 창에 입력·포커스를
보낼 수 없으므로, 매크로도 관리자로 떠야 정상 동작한다. (설치 프로그램은 이미
Program Files에 쓰느라 승격되고, 설치 후 실행도 그 권한을 승계한다.)

#### 왜 onedir(분할)인가
- **AV 오탐 완화**: onefile은 실행 시 자기 자신을 임시폴더에 통째로 푸는 동작이
  백신에 랜섬웨어처럼 보여 오탐이 잦다. onedir는 그 표면이 작다.
- **UPX 금지**(`upx=False`): 압축이 AV 오탐의 주범이라 스펙에서 꺼 둠. 유지할 것.
- 시작 속도도 더 빠르다.

#### 빌드 직후 스모크 테스트 (반드시)
빌드한 PC에서 바로 실행해 **OCR이 오프라인으로 로드되는지** 확인:
```powershell
.\dist\FishingMacro\FishingMacro.exe
```
- exe가 `requireAdministrator`라 **실행 시 UAC 승격 창**이 뜬다(정상). 승인하면 콘솔이 뜬다.
- `[시스템] OCR 모델을 불러오는 중입니다...` 후 에러 없이 메뉴가 뜨면 성공.
- 여기서 다운로드 시도/모델 못 찾음 오류가 나면 → `ocr_model/`이 비었거나 스펙의
  `datas` 동봉이 누락된 것. 1번으로 돌아가 확인.
- 매니페스트 확인(선택): 아래가 `requireAdministrator`를 출력하면 관리자 권한이 박힌 것.
  ```powershell
  $t=[IO.File]::ReadAllText((Resolve-Path .\dist\FishingMacro\FishingMacro.exe))
  [regex]::Match($t,'requestedExecutionLevel[^>]*level="([^"]+)"').Groups[1].Value
  ```

#### 흔한 함정
- **cv2 죽음**: 0번의 opencv headless 강제 재설치를 안 하면 `ModuleNotFoundError: cv2`
  → easyocr/windows_capture 연쇄 실패. (0번 참고)
- **windows-capture(.pyd) 누락**: 스펙이 `collect_all('windows_capture')`로 잡으므로
  기본은 포함됨. QHD/감시 모드가 캡처를 못 하면 여기부터 의심.
- **Store Python으로 빌드**: 실패. python.org Python만 사용.

---

### 3-A. 배포: Inno Setup exe 설치 프로그램 (권장)

가장 단순하고, 서명 없이도 배포 가능. `installer.iss`가 이미 준비돼 있다.

1. **Inno Setup 설치** — `winget install --id JRSoftware.InnoSetup -e`
   또는 https://jrsoftware.org/isdl.php (6.x)
2. **컴파일** (둘 중 하나)
   - GUI: Inno Setup Compiler에서 `installer.iss` 열고 **Build → Compile**
   - CLI:
     ```powershell
     # winget 설치 시 ISCC는 사용자 폴더에 깔린다(표준 Program Files 아님):
     & "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" installer.iss
     # (전통적 설치 프로그램으로 깔았다면: "C:\Program Files (x86)\Inno Setup 6\ISCC.exe")
     ```
   - 875MB를 lzma2/max로 압축하므로 **6~7분** 걸린다(정상).
3. 결과: **`Output\낚시매크로_설치.exe`** (단일 설치 파일)
   - `dist\FishingMacro\*` 전체를 `%ProgramFiles%\FishingMacro`에 설치
   - 시작 메뉴/바탕화면 아이콘, 제거 프로그램, 한국어 마법사 포함
   - `Compression=lzma2/max` + `SolidCompression`으로 용량 최소화

> **버전 올릴 때**: `installer.iss`의 `#define MyAppVersion` 수정.
> **아이콘**: `.ico`를 만들면 `build_fishing.spec`의 `icon='app.ico'` 주석 해제 후
> 재빌드하면 exe·설치 프로그램에 반영된다.

배포 시 전달물: `Output\낚시매크로_설치.exe` **하나**.

---

### 3-B. 배포: MSIX (대안 — 서명 필요)

Store 배포나 조직 정책상 MSIX가 필요할 때만 선택. exe 설치보다 절차·제약이 많다.

#### 만드는 법 (요약)
- **MSIX Packaging Tool**(MS Store 무료)로 `Output\낚시매크로_설치.exe`(또는
  `dist\FishingMacro\`)를 감싸 `.msix` 생성, 또는 Advanced Installer 같은 도구 사용.
- 앱 유형은 **Full-trust 데스크톱 앱**(`windows.fullTrustProcess`)으로 패키징해야
  이 매크로에 필요한 권한(다른 창 제어)이 유지된다.

#### 이 앱 특유의 주의사항 (중요)
- **코드 서명 필수**: MSIX는 설치하려면 신뢰된 인증서 서명이 반드시 있어야 한다.
  내부용이면 자체 서명 인증서를 만들어 대상 PC의 "신뢰할 수 있는 사람/루트"에 미리
  설치해야 한다. → 개인/소규모 배포엔 부담이 크다. **이 경우 3-A(Inno)가 훨씬 낫다.**
- **입력·캡처 권한**: 이 매크로는 `SetCursorPos`/`mouse_event`/`SetForegroundWindow`로
  **게임(다른 프로세스)** 을 조작하고 WGC로 화면을 캡처한다. Full-trust MSIX면 대체로
  동작하지만, MSIX 컨테이너의 입력 제약에 걸릴 수 있어 **반드시 대상 환경에서 실측**할 것.
- **파일 쓰기 가상화**: MSIX는 앱의 파일 쓰기를 사용자별 가상 경로로 리다이렉트한다.
  - `ocr_model/`은 **읽기 전용**이라 문제 없음(설치 폴더에서 읽음).
  - `fishing_full_*.log` 로그는 현재 작업 폴더에 쓰는데, MSIX에선 이 경로가 가상화될 수
    있다. 로그 위치가 예상과 다르면 이 때문이다(기능엔 지장 없음).

#### 결론
- **개인/소규모/사내 배포 → 3-A(Inno Setup) 권장.** 서명 없이 즉시 배포 가능.
- **MSIX는** Store 등록·조직 정책 등 MSIX가 *요구되는* 상황에서만, 서명 인증서를
  갖춘 뒤 진행.

---

### 부록: 릴리스 체크리스트

1. [ ] `venv314` 활성화, 0번 의존성(특히 opencv headless 강제 재설치) 상태 정상
2. [ ] `ocr_model/`에 `craft_mlt_25k.pth` + `korean_g2.pth` 존재 (1번)
3. [ ] `build\`·`dist\` 수동 삭제 → `--workpath`를 OneDrive 밖으로 지정해 빌드 성공 (2번, OneDrive 잠금 회피)
4. [ ] exe 매니페스트에 `requireAdministrator` 박힘 확인 (2번, 스모크 테스트)
5. [ ] `dist\FishingMacro\FishingMacro.exe` 실행 → UAC 승격 후 OCR 로드·메뉴 정상 (오프라인)
6. [ ] (Inno) `installer.iss` 버전 갱신 → ISCC 컴파일 → `Output\낚시매크로_설치.exe` (3-A)
7. [ ] 배포 대상과 유사한 PC(가능하면 인터넷 차단)에서 설치 → **게임 켠 상태에서** 관리자 승격·매크로 동작 최종 확인
